==============================
🧠 ARSENAL HA — CHANGELOG v9.1.2
=======================================

Date de consolidation : 15/02/2026

## 🧭 Périmètre (sources _diff)
- Macro-report : `arsenal_v9.1.1__to__arsenal_v9.1.2.md`
- Digest      : `arsenal_v9.1.1__to__arsenal_v9.1.2__digest.md`

Indicateurs (macro) :
- Added (raw) 55 | Removed (raw) 1 | Changed 30
- Added (net) 55 | Removed (net) 1

📝 Note
- Le diff contient du bruit Git (`git/objects/*`) et `.storage/*`.
- Ce changelog ne revendique que les changements “Arsenal” (UI / docs / structure / lisibilité).

---

## 📌 UI — Couleurs (charte)
- `documentation_arsenal/ui/couleurs.md`
  - Clarification contractuelle : une couleur peut traduire une réalité **métier** OU une réalité de **structure UI contractuelle**.
  - Ajout d’un bloc “COULEURS DE STRUCTURE UI (HORS SÉMANTIQUE MÉTIER)”.
  - Nouvelle sémantique “NAV/HUB — NAVIGATION STRUCTURELLE”
    - Couleur officielle : `rgba(90, 110, 130, 0.08)`
    - Réservée aux tuiles de navigation (menu/hub), interdite pour encoder un statut.

---

## 📌 UI — Socle ACTION (TPL-06)
- `button_card_templates/socle/action/action_simple_sans_couleur.yaml`
  - Nouveau socle : `socle_action_simple_sans_couleur`
  - Variante “structurelle / sans fond” : ne fixe pas de `background-color`
  - Actions neutralisées (tap/hold/double_tap = none) : l’action reste portée par la carte dérivée.

- `documentation_arsenal/ui/socle_ui/02_action.md`
  - Documentation du nouveau socle + rattachement au catalogue TPL-06.

---

## 📌 UI — Navigation (génériques)
- `button_card_templates/generiques/navigation/navigation.yaml`
  - `bouton_navigation` rebasé sur `socle_action_simple_sans_couleur`
  - Fond défini localement (structure NAV/HUB) : `rgba(90, 110, 130, 0.08)`

---

## 📌 UI — Volets (templates & dashboard)
- `button_card_templates/dashboards/volets/action.yaml`
  - Refonte des tuiles d’action volets (72 canon) :
    - passage à une base `shutter_action_base` (hérite `socle_kpi_72`)
    - variantes explicites : `shutter_action_open`, `shutter_action_close`, `shutter_action_stop`
    - suppression de la logique “tone” au profit de variantes déterministes
    - couleurs Arsenal strictes (success/danger/warn), aucune décision métier.

- `lovelace/dashboards/volets.yaml`
  - Migration des tuiles “shutter_action” vers `shutter_action_open/close`
  - Réorganisation du dashboard + confirmations explicites par action (par pièce).

- Nettoyage
  - Suppression : `button_card_templates/generiques/section_headers.yaml`

---

## 📌 Diagnostics — Chauffage (verrou post-aération)
- `lovelace/dashboards/diagnostics/chauffage.yaml`
  - Enrichissement du bloc “⛔ Blocage thermique actif” :
    - si cause = aération/post-aération : mention d’une reprise automatique attendue après fin du verrou.
  - Ajout d’une section de lecture dédiée :
    - “🔒 Verrou post-aération”
    - exposition des états :
      - aération en cours / post-aération
      - override opérateur (`input_boolean.mode_confort_chauffage`)
      - aucun verrou actif
    - affichage des échéances (`input_datetime.chauffage_fin_blocage_aeration`, `input_datetime.analyse_deltat_disponible`)

---

## 📌 Diagnostics — Aération (effet chauffage)
- `lovelace/dashboards/diagnostics/aeration.yaml`
  - Renommage : “🔒 Blocage chauffage” → “🔒 Effet sur chauffage”
  - Formulation de conclusion normalisée :
    - “🟢 Aucun verrou chauffage lié à l’aération”

---

## 📌 Logs — Logger
- `logger.yaml`
  - Ajustements de niveaux pour réduire le bruit :
    - `custom_components.audiconnect` : `error`
    - `custom_components.roborock`    : `error`
    - `custom_components.netatmo`     : `warning`
    - `custom_components.vicare`      : `warning`
    - `homeassistant.components.overkiz`      : `warning`
    - `homeassistant.components.input_number` : `warning`

---

## 📌 Zigbee2MQTT — Backup coordinateur
- `zigbee2mqtt/coordinator_backup.json`
  - Mise à jour du backup (metadata `date` + `frame_counter`) — changement attendu (bruit opérationnel).

---

## 📌 STATUT
- Nature : consolidation **UI + docs + lisibilité diagnostics**
- Aucune modification de logique décisionnelle métier identifiable dans les fichiers touchés (principalement UI/docs/logging).
- Bruit repo : Git objects + `.storage/*` (attendu).
