============================================================
🧠 ARSENAL HA v9.2.3 — STABLE
------------------------------------------------------------
Date : 2026-02-17
Périmètre : Santé (UI), Withings (capteurs locaux), Documentation (gouvernance), Runtime (artefacts)
============================================================

## Résumé (diff v9.2.2 → v9.2.3)

- Added (raw) : 31
- Removed (raw) : 1
- Changed : 17
- Added (net) : 31
- Removed (net) : 1

Changements par dossier (net) :
- git : +29 / ~4
- storage : ~10
- button_card_templates : +1 / -1
- documentation_arsenal : +1 / ~1
- 11_template_sensors : ~1
- lovelace : ~1

---

## UI — Santé (Sommeil)

- Simplification du titre de section :
  - `Sommeil & récupération` → `Sommeil`
- Densification de la grille principale :
  - colonnes : `2` → `3`
- Ajout d’un KPI “Réveils” :
  - `sensor.withings_wakeup_count_local`
  - `template: carte_compteur_seuils_variables` + seuils 0/1/2/3
- Ajout d’un sous-bloc Ronflements :
  - “Ronflements (épisodes)” : `sensor.withings_snoring_episode_count_local` + seuils 0/1/2/3
  - “Ronflements (durée)” : `sensor.withings_ronflement_local` via `template: carte_duree_ronflements`
- Bar-card “Répartition des phases” : masquage des libellés d’entités (`name: " "`) pour sobriété visuelle.

---

## Capteurs — Withings / Locaux (robustesse + factorisation)

Objectif : fiabiliser et industrialiser les “_local” en supprimant la duplication.

- Refactor du calcul “source → fallback old” :
  - la source est désormais dérivée automatiquement depuis `this.entity_id` en retirant le suffixe `_local`
  - fallback : conservation d’un état précédent (via `this.state`) quand la source est `unknown/unavailable/""/None`
- Introduction d’ancres YAML (réutilisation) :
  - ex : `&withings_state_float_0` (float, défaut 0, arrondi)
  - ex : `*withings_state_int` pour les capteurs entiers (remplacement du bloc Jinja dupliqué)

---

## UI — Templates button-card

- Ajout :
  - `button_card_templates/dashboards/sante/duree_ronflements.yaml`
- Suppression :
  - `button_card_templates/generiques/status_72_timer_info_transitoire.yaml`

---

## Documentation — Gouvernance changelog

- Mise à jour “Dernière consolidation officielle” dans `documentation_arsenal/changelog/en_cours.md` :
  - `v9.2.0` → `v9.2.2` (alignement)
  - remplacement du contenu (axes : navigation sobre, bouclage ECS factorisé, diagnostics ouvertures lisibles, etc.)
- Ajout du changelog versionné :
  - `documentation_arsenal/changelog/changelogs/v09_2_2.md`

---

## Home Assistant — Runtime

- `storage/` : mises à jour internes HA (artefacts non fonctionnels) (~10 fichiers).
- `git/` : nouveaux objets + modifications internes (index/pack/refs) (classique d’un commit).

---

## Inventaire fichiers (zéro perte)

### Added (net)
- `button_card_templates/dashboards/sante/duree_ronflements.yaml`
- `documentation_arsenal/changelog/changelogs/v09_2_2.md`

### Removed (net)
- `button_card_templates/generiques/status_72_timer_info_transitoire.yaml`
