==================================================
🧠 ARSENAL HA — v10.3 STABLE
==================================================

---

# 📦 Synthèse technique

| Type          | Volume |
|---------------|--------|
| Ajouts        | +188   |
| Suppressions  | −30    |
| Modifications | 33     |

### Répartition principale

| Dossier                      | Impact                                |
|------------------------------|---------------------------------------|
| `09_scripts`                 | +6 scripts natifs                     |
| `custom_components/pyscript` | suppression complète                  |
| `pyscript/`                  | suppression logique métier            |
| `lovelace`                   | migration services + confirmations UI |
| `documentation_arsenal`      | alignement architecture               |
| `configuration.yaml`         | suppression intégration pyscript      |

---

## 📌 Suppression complète de Pyscript — Migration architecture

Arsenal abandonne totalement **Pyscript** au profit de **scripts natifs Home Assistant**.

### Suppressions

* `custom_components/pyscript/*` — suppression complète de la custom component
* `pyscript/ecs_autocorrect_offsets.py`
* `pyscript/ecs_sauvegarde_planning.py`
* `pyscript/ecs_restauration_planning.py`
* `pyscript/backup_reglages_ecs.py`
* `pyscript/automatisation_id.py`
* `pyscript/restauration_reglages_ecs.py`

### Nettoyage configuration

Suppression dans `configuration.yaml` :

```yaml
pyscript:
```

---

## 📌 ECS — Nouveaux scripts natifs

Remplacement 1:1 des scripts pyscript par des scripts YAML natifs HA :

```
09_scripts/ecs/
├── auto_correction_offsets.yaml
└── backup_and_restore/
    ├── planning_sauvegarde.yaml
    ├── planning_restauration.yaml
    ├── reglages_sauvegarde.yaml
    └── reglages_restauration.yaml
```

---

## 📌 ECS — Migration moteur auto-ajustement offsets

* `10_automations/ecs/auto_ajustement_offset.yaml` :
  * Appel migré de `pyscript.ecs_autocorrect_offsets` vers `script.ecs_autocorrect_offsets`
  * Description interne mise à jour (suppression de la mention Pyscript)
  * Principe Arsenal conservé : automation strictement consommatrice, acquittement du signal `input_boolean.ecs_fin_cycle_signal`

---

## 📌 ECS — Sauvegarde planning : capacité étendue

* `04_input_texts/ecs/backups/planning.yaml` :
  * `ecs_planning_sauvegarde_matin` et `ecs_planning_sauvegarde_soir` passent à `max: 255`
  * Nécessaire pour accueillir des snapshots JSON complets

---

## 📌 Système Arsenal — Génération d'ID

* Ajout de `09_scripts/system/generation_id_automatisation.yaml`
  * Remplace `pyscript.generate_next_id_from_prefix`
  * Utilisé dans `lovelace/dashboards/id_automatisations.yaml`

---

## 📌 UI/Lovelace — Migration services et confirmations

* `lovelace/dashboards/reglages/ecs_planning.yaml` :
  * Migration `pyscript.ecs_sauvegarde_planning` → `script.ecs_sauvegarde_planning`
  * Migration `pyscript.ecs_restore_planning` → `script.ecs_restore_planning`
  * Ajout de dialogues de confirmation explicites sur les deux boutons :
    * `"Confirmer la sauvegarde du planning ECS ?"`
    * `"⚠️ Confirmer la RESTAURATION du planning ECS ?"`

* `lovelace/dashboards/reglages/ecs.yaml` :
  * Migration `pyscript.ecs_sauvegarde_reglages` → `script.ecs_sauvegarde_reglages`
  * Migration `pyscript.ecs_restore_reglages` → `script.ecs_restore_reglages`

* `lovelace/dashboards/id_automatisations.yaml` :
  * Migration `pyscript.generate_next_id_from_prefix` → `script.generate_next_id_from_prefix`

---

## 📌 Documentation Arsenal

* `documentation_arsenal/architecture/structure_includes.md` :
  * Suppression de `pyscript/` dans la cartographie des includes
  * Suppression de la mention `pyscript` dans le principe fondamental Arsenal
  * Architecture Arsenal officiellement basée sur : `template`, `mqtt`, `automation`, `script`, `lovelace`

* `documentation_arsenal/contrats/ecs/11_ajustement_des_offsets.md` :
  * Titre mis à jour : `ECS — Auto-ajustement des offsets` (suppression de la mention pyscript)

---

## 📌 Zigbee2MQTT

* `zigbee2mqtt/coordinator_backup.json` :
  * Date de backup : `2026-03-04`
  * Frame counter réseau mis à jour : `1 323 984` → `1 356 751`

---

## 📊 STATUT DE VERSION

| Critère             | Statut            |
|---------------------|-------------------|
| Type                | refonte technique |
| Impact fonctionnel  | neutre            |
| Impact architecture | majeur            |
| Dette technique     | réduite           |

* Nature : assainissement architectural majeur
* Portée :
  * Suppression complète de la dépendance Pyscript
  * Migration vers scripts 100 % Home Assistant natifs
  * UI sécurisée par confirmations sur actions critiques ECS
  * Documentation alignée avec l'architecture réelle
* Invariants :
  * Aucune régression fonctionnelle connue
  * Aucune dette technique ajoutée
  * Conforme aux contrats Arsenal applicables

Date de consolidation : 2026-03-04

==================================================
