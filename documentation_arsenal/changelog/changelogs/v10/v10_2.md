==================================================
🧠 ARSENAL HA — v10.2 STABLE
==================================================

---

## 📌 ECS — Correctif cycles rapprochés (durée et horodatage)

* Ajout de `07_input_datetimes/ecs/debut_cycle_timestamp.yaml`
* Ajout de `10_automations/ecs/log/timestamp_debut.yaml` — capture explicite du début de cycle ECS

* `10_automations/ecs/log/fin.yaml` — correctif durée réelle :
  * Remplacement de la référence ambiguë `input_datetime.ecs_dernier_cycle_datetime` par `input_datetime.ecs_cycle_debut_timestamp` comme borne de début
  * Comparaisons sécurisées avec `as_timestamp(..., none)` et fallback explicite
  * Définition canonique de la durée : `tmax_timestamp − cycle_debut_timestamp`
  * Refonte de la validation du résumé : contrôles multi-critères (`duree > 0`, `temp_max > 0`, delta thermique ≥ 0.5 °C), en remplacement du test consigne ≤ 10 °C

* `10_automations/ecs/log/timestamp_fin.yaml` — correctif capture pic thermique :
  * Ajout d'un garde-fou : capture uniquement si `input_boolean.ecs_cycle_en_cours = on`
  * Durcissement de la condition de monotonicité : rejet des états `unknown`, `unavailable`, `none`, vide ou absence de `from_state`

---

## 📌 Chauffage — Courbe de chauffe : robustesse généralisée

* `10_automations/chauffage/courbe_de_chauffe/application.yaml` :
  * Mode `single` → `queued` (max 5) pour gérer les déclenchements simultanés
  * Triggers séparés par entité (`pente` / `parallèle`) avec filtre `to: ~`
  * Condition supplémentaire : rejet des états invalides sur `trigger.to_state`

* `10_automations/chauffage/courbe_de_chauffe/auto_ajustement.yaml` :
  * Ajout d'un garde-fou : suggestions disponibles et numériques (rejet `unknown`/`unavailable`)
  * Comparaisons en float (précision numérique) en remplacement des comparaisons string
  * Introduction de `apply_pente` et `apply_parallele` : blocage des baisses si le poêle est en fonction stable

* `10_automations/chauffage/courbe_de_chauffe/correction_demarrage.yaml` :
  * Variables brutes + garde-fou `valeurs_disponibles` avant tout `float` cast
  * Tolérance `0.01` introduite pour les comparaisons cloud/local (remplace égalité stricte)
  * Journalisation `logbook` ajoutée lors d'une correction effective au démarrage
  * Exécution des branches pente et parallèle découplées en `choose` indépendants

* `10_automations/chauffage/courbe_de_chauffe/log_auto_ajustement.yaml` :
  * Extraction sécurisée des variables d'événement avec `is defined` avant accès
  * Normalisation et arrondi à 2 décimales des valeurs avant/après pente et parallèle
  * Uniformisation : `action:` → `service:` (cohérence Arsenal)

* `09_scripts/chauffage/courbe_de_chauffe/application_pente.yaml` :
  * Remplacement de `repeat: count: 3` par boucle `while` conditionnée (API dispo + écart > tolérance)
  * Garde-fou pré-cast : rejet valeurs invalides avant `float`
  * Vérification finale bifurquée : log succès confirmé / log échec cloud

* `09_scripts/chauffage/courbe_de_chauffe/application_parallele.yaml` :
  * Même refonte que `application_pente` (symétrie stricte)
  * Log de non-confirmation cloud ajouté en cas d'échec

---

## 📌 Éclairage Garage — Autorisation saisonnière/périodique

* Ajout de `11_template_sensors/eclairage/garage_allumage_auto_autorise.yaml`
  * Binary sensor calculant l'autorisation contextuelle (saison + période météo)

* `10_automations/eclairage/garage/allumage_automatique.yaml` :
  * Ajout de la condition `binary_sensor.garage_allumage_auto_autorise = on` entre l'autorisation globale et la vérification d'état lumière
  * Mise à jour du titre et de la documentation interne pour refléter la double gate

* Ajout de `button_card_templates/dashboards/eclairage/intention_garage.yaml`

---

## 📌 Button Card — Éclairage Jardin : migration bandeau header

* `button_card_templates/dashboards/eclairage/intention_jardin.yaml` :
  * Migration de `socle_status_label_sans_nom` vers `socle_header_base`
  * Passage en bandeau pleine largeur ultra fin (lecture seule, sans icône, sans label secondaire)
  * Suppression du bloc `label` conditionnel (attente présence, extinction autorisée)
  * Harmonisation couleurs : jaune `rgba(255, 235, 59, 0.2)` (espaces canoniques), bleu `rgba(33, 150, 243, 0.2)`
  * Ajout des styles `card` et `name` (padding, font-size 13px, font-weight 500)
  * Actions désactivées (`tap`, `hold`, `double_tap` → `none`)

* Suppression de `button_card_templates/dashboards/alarme/diagnostic_divergence_persistante.yaml`

---

## 📌 Template Sensors — Refonte diagnostic thermique Chambres

* `11_template_sensors/chauffage/diagnostic_thermique/absence/temperature_plancher.yaml` :
  * Horodatage `tmin_timestamp` migré de `now()` vers `as_timestamp(trigger.to_state.last_changed)` (événementiel strict)
  * Invalidation aération : passage de contrôle ponctuel (fin de cycle) à latch intra-cycle (`aeration_vue_pendant_cycle`)

* `11_template_sensors/chauffage/diagnostic_thermique/presence/duree_overshoot_arret.yaml` :
  * Trigger B0 rebasé sur `attribute: t_arret` (canonical, anti-spam)
  * `t0` migré de `now()` vers `as_timestamp(trigger.to_state.last_changed)` (Option A time)
  * Introduction de `T_prev` pour détection de baisse sans race-condition (comparaison `T_cur < T_prev` vs ancienne `T_cur < temperature_max`)
  * Ajout de `T_peak` : pic thermique suivi pendant l'overshoot
  * `t_end_ref` figé à la première baisse confirmée
  * Neutralisation aération : aucune mise à jour, valeur figée (aucun "0 fantôme")
  * State = `none` hors cycle B0 valide

---

## 📌 Documentation Arsenal — ECS

* Ajout de `documentation_arsenal/contrats/ecs/11_ajustement_des_offsets.md`
* Suppression de `documentation_arsenal/contrats/ecs/99_changelog.md` (remplacé par le système de changelog versionné)
* Ajout de `documentation_arsenal/changelog/changelogs/v10/v10_1.md`
* Ajout de `documentation_arsenal/changelog/changelogs/v10/v10_2.md`

---

## 📌 Git — Objets

* 130 objets git ajoutés (commits, trees, blobs)
* 4 refs/config modifiés

---

## 📌 STATUT DE VERSION

* Nature : version corrective et de robustesse
* Portée fonctionnelle :
  * Correctif majeur ECS : durée de cycle calculée depuis un horodatage de début dédié et fiable
  * Robustesse généralisée courbe de chauffe : garde-fous invalides, tolérances float, boucles `while`, logs bifurqués
  * Autorisation saisonnière garage : nouveau binary_sensor + gate dans l'automation
  * Migration bandeau header jardin vers `socle_header_base`
  * Refonte diagnostic thermique Chambres : horodatages événementiels, latch aération, anti race-condition overshoot
  * Contrat ECS offsets ajouté ; changelog ECS unifié dans le système versionné
* Invariants :
  * Aucune régression fonctionnelle connue
  * Aucune dette technique ajoutée
  * Conforme aux contrats Arsenal applicables

Date de consolidation : 2026-03-04

==================================================
