==============================
🧠 ARSENAL HA — v9.2.4 STABLE
=======================================

---

## 📌 UI — ECS / Planning (refonte ergonomie)

- `lovelace/dashboards/reglages/ecs_planning.yaml`
  - Refonte complète du planning hebdomadaire :
    • remplacement des `horizontal-stack` + cartes `entities` par une structure `grid` (2 colonnes) + `vertical-stack`
    • standardisation : `sub_section_header` (Matin / Soir) + `tile` pour `input_boolean` et `input_datetime`
    • libellé heure compact : `name: "⏰"`
  - Ajustement de la “barre d’actions” :
    • ajout d’un spacer gauche supplémentaire (`template: bouton_spacer`) pour un centrage plus propre

---

## 📌 UI — Navigation (retour outillage + actions)

- `lovelace/dashboards/navigation.yaml`
  - Réintroduction d’un bloc “outillage” complet (accès directs) :
    • Sauvegardes (`/config/backup/overview`)
    • Automations (`/config/automation/dashboard`)
    • Scripts (`/config/script/dashboard`)
    • Logs HA (`/config/logs`)
    • États (`/developer-tools/state`)
    • Entités (`/config/entities`)
    • Dashboards (`/config/lovelace/dashboards`)
    • Intégrations (`/config/integrations/dashboard`)
    • Templates (`/developer-tools/template`)
    • File Editor (ingress)
  - Ajout d’un bloc 2 colonnes :
    • Reboot HA (call-service `homeassistant.restart` + confirmation)
    • Ressources (`/config/lovelace/resources`)

---

## 📌 UI — Volets (cohérence action + icônes)

- `lovelace/dashboards/volets.yaml`
  - Uniformisation des actions : `shutter_action_open` / `shutter_action_close` → `shutter_action_base`
  - Correction des icônes : remplacement des icônes “sofa” par `mdi:window-shutter-open` / `mdi:window-shutter`
  - Recomposition de la section Chambres :
    • entêtes “Arnaud / Matthieu” regroupées
    • actions regroupées en une grille 4 colonnes (ouvrir/fermer pour chaque chambre)

---

## 📌 UI — Button-card templates (Volets)

- `button_card_templates/dashboards/volets/action.yaml`
  - Nettoyage : suppression des variantes `shutter_action_open` (vert) et `shutter_action_close` (rouge)
  - Standardisation : `shutter_action_base` force un fond neutre canon
    • `background-color: "rgba(158, 158, 158, 0.2)"`

---

## 📌 Chauffage — Helper (icône)

- `05_input_booleans/chauffage/inhibition_geofencing_etat.yaml`
  - Changement d’icône :
    • `mdi:shield-thermometer` → `mdi:shield-check`

---

## 📌 Zigbee2MQTT — Résilience

- `zigbee2mqtt/coordinator_backup.json`
  - Rafraîchissement du backup coordinateur :
    • date interne mise à jour
    • frame_counter mis à jour

---

## 📌 Documentation — Gouvernance

- Ajouts :
  - `documentation_arsenal/changelog/changelogs/v09_2_3.md`
  - `documentation_arsenal/evolutions_futures/zigbee_reseau.md`
- Mise à jour :
  - `documentation_arsenal/changelog/en_cours.md`
    • bloc “Dernière consolidation officielle” aligné (référence v9.2.3)

---

## 📌 Home Assistant — Runtime / Artefacts

- `storage/` : ~9 fichiers modifiés (artefacts internes HA, non fonctionnels)
- `git/` : +33 nouveaux objets, ~4 fichiers modifiés (bruit de repo / commit)

---

## 📌 STATUT

- Nature : patch UI (planning ECS + navigation + volets) + normalisation templates + gouvernance doc + maintenance runtime
- Diff résumé :
  - Added (net) : 35
  - Removed (net) : 0
  - Changed : 20
  - Renamed/Moved : 0
- Changements par dossier racine (net) :
  - `git` : +33 / -0 / ~4
  - `storage` : +0 / -0 / ~9
  - `documentation_arsenal` : +2 / -0 / ~1
  - `lovelace` : +0 / -0 / ~3
  - `05_input_booleans` : +0 / -0 / ~1
  - `button_card_templates` : +0 / -0 / ~1
  - `zigbee2mqtt` : +0 / -0 / ~1
- Dette technique ajoutée : aucune détectée dans ce diff
- Date de consolidation : 2026-02-17

==========================================================

## 📎 ANNEXE — Inventaire fichiers (zéro perte)

### Ajouts (net, 35)

- `documentation_arsenal/changelog/changelogs/v09_2_3.md`
- `documentation_arsenal/evolutions_futures/zigbee_reseau.md`

### Suppressions (net, 0)

- Aucune

### Modifications (détaillées dans le digest)

- `lovelace/dashboards/reglages/ecs_planning.yaml`
- `lovelace/dashboards/navigation.yaml`
- `lovelace/dashboards/volets.yaml`
- `documentation_arsenal/changelog/en_cours.md`
- `button_card_templates/dashboards/volets/action.yaml`
- `zigbee2mqtt/coordinator_backup.json`
- `05_input_booleans/chauffage/inhibition_geofencing_etat.yaml`

### Modifications (non détaillées, mais comptées)

- `git/` : ~4 fichiers modifiés
- `storage/` : ~9 fichiers modifiés
