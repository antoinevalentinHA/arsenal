==============================
🧠 ARSENAL HA — v9.2.2 STABLE
=======================================

---

## 📌 Core — Secrets / Intégrations

- `secrets.yaml`
  - Mise à jour de `vicare_entry_id` (changement d’ID d’entrée ViCare)
  - Objectif implicite : réaligner la référence d’intégration (évite un “entry id” périmé)

---

## 📌 Capteurs métier — Ouvertures (icônes + lisibilité)

- `11_template_sensors/ouvertures/etages/etage.yaml`
  - Ajout d’icône : `mdi:window-open` sur `binary_sensor.fenetre_ouverte_etage`
- `11_template_sensors/ouvertures/etages/rdc.yaml`
  - Ajout d’icône : `mdi:window-open` sur `binary_sensor.fenetre_ouverte_rdc`
- `11_template_sensors/ouvertures/global/fenetres.yaml`
  - Ajout d’icône : `mdi:window-open` sur `binary_sensor.fenetre_ouverte_maison`
- `11_template_sensors/ouvertures/global/portes_et_fenetres.yaml`
  - Ajout d’icône : `mdi:lock` sur `binary_sensor.ouvertures_maison`

---

## 📌 UI — Diagnostics Ouvertures (refactor “markdown → cartes”)

- `lovelace/dashboards/diagnostics/ouvertures.yaml`
  - Remplacement des blocs `markdown` (états instantanés + minuteries) par une UI structurée :
    - Ajout d’une carte `contact_sensor` pour `binary_sensor.ouvertures_maison`
    - Section “🪟 Fenêtres” :
      - grille 2 colonnes : `binary_sensor.fenetre_ouverte_rdc` + `binary_sensor.fenetre_ouverte_etage`
      - carte dédiée : `binary_sensor.fenetre_ouverte_maison`
    - Section “⏳ États avec délais” :
      - grille 2 colonnes : `binary_sensor.fenetre_ouverte_maison_avec_delai` + `binary_sensor.fenetre_ouverte_etage_avec_delai`
        - templates : `status_72_on_off` + icônes dédiées
      - grille 2 colonnes : timers “grâce” (Séjour / Chambre parents)
        - templates : `status_72_info_transitoire` + icônes dédiées
  - Effet : UI plus lisible, plus homogène Arsenal, disparition des calculs/rendus “texte” au profit de cartes statut

---

## 📌 UI — Navigation (ajustement “outillage”)

- `lovelace/dashboards/navigation.yaml`
  - Réordonnancement : bouton **Historique** déplacé en tête du bloc
  - Retrait du bouton **File Editor** (ingress)
  - Ajout d’un bouton **Logs HA** pointant sur `/config/logs`

---

## 📌 UI — Button-card templates (socle / statuts)

- Ajouts :
  - `button_card_templates/generiques/status_72_on_off.yaml`
- Modifications :
  - `button_card_templates/generiques/bouclage_timer.yaml`
    - Retrait d’icônes explicites `mdi:water-boiler` (laisse le socle / la carte décider)
  - `button_card_templates/generiques/compteur_seuils_variables.yaml`
    - Passage `template: socle_diagnostic` → `template: socle_diagnostic_compact`

---

## 📌 Documentation — Changelog

- Ajout du changelog versionné :
  - `documentation_arsenal/changelog/changelogs/v09_2_1.md`

---

## 📌 Home Assistant — Runtime / Artefacts

- `storage/` : ~7 fichiers modifiés (artefacts internes Home Assistant, non fonctionnels)
- `git/` : +24 nouveaux objets, ~4 fichiers modifiés (bruit de repo / commit)

---

## 📌 STATUT

- Nature : patch UI “Ouvertures” (lisibilité + cartes statut) + enrichissement icônes capteurs + ajustement navigation + maintenance templates + historisation changelog
- Diff résumé :
  - Added (net) : 27
  - Removed (net) : 0
  - Changed : 20
  - Renamed/Moved : 0
- Changements par dossier racine (net) :
  - `git` : +24 / -0 / ~4
  - `storage` : +0 / -0 / ~7
  - `11_template_sensors` : +0 / -0 / ~4
  - `button_card_templates` : +2 / -0 / ~2
  - `lovelace` : +0 / -0 / ~2
  - `(root)` : +0 / -0 / ~1
  - `documentation_arsenal` : +1 / -0 / ~0
- Dette technique ajoutée : aucune détectée dans ce diff
- Date de consolidation : 2026-02-17

==========================================================

## 📎 ANNEXE — Inventaire fichiers (zéro perte)

### Ajouts (net, 27)

- `button_card_templates/generiques/status_72_on_off.yaml`
- `button_card_templates/generiques/status_72_timer_info_transitoire.yaml`
- `documentation_arsenal/changelog/changelogs/v09_2_1.md`
