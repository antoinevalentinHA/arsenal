==================================================
🧠 ARSENAL HA — v9.0.4
==================================================

## 📌 Résumé exécutif (signal vs bruit)

- SIGNAL : enrichissement **Météo / Pluie** (cumul hebdo + totalisation + UI) + structuration **Visite** (contrat + migration helper) + documentation/standardisation des en-têtes de capteurs.
- BRUIT MAJEUR : **import massif du dossier `git/` (+4034 fichiers)** dans le snapshot/diff (non fonctionnel pour HA, mais polluant et volumineux).
- BRUIT : `storage/` (runtime HA) + backup Zigbee2MQTT.

---

## 🌧️ Météo — Pluie : cumul & totalisation (SIGNAL)

### Nouvelles métriques (pluie hebdomadaire)
- Ajout `utility_meter.yaml` :
  - `pluie_semaine` (cycle weekly) basé sur `sensor.pluie_total_local`.

### Totalisation / calculs locaux
- Ajout :
  - `10_automations/meteo/pluie/totaliseur_cumulatif.yaml`
  - `11_template_sensors/meteo/pluie/capteurs_locaux.yaml`
  - `03_input_numbers/meteo/pluie_cumul.yaml`

### Observabilité / persistance
- `recorder.yaml` : ajout des entités pluie locales :
  - `sensor.pluie_aujourdhui_local`
  - `sensor.pluie_total_local`
  - `sensor.pluie_hebdomadaire`

### UI
- `lovelace/dashboards/meteo/meteo_precipitations.yaml` :
  - renommage “Précipitations — 7 jours” → **“Intensité (7 jours)”**
  - ajout d’un graphe **“Cumul hebdo (52 semaines)”** (sensor `sensor.pluie_hebdomadaire`, group_by 1w).

🎯 Effet : tu passes d’une vision “intensité courte” à une **lecture cumulative longue** (hebdo / 52 semaines) + socle de capteurs locaux intégrés au recorder.

---

## 👥 Présence — Visite : migration helper + contrat (SIGNAL)

- Migration d’un input_select (Alarme → Présence) :
  - supprimé : `06_input_selects/alarme/jour_visite.yaml`
  - ajouté  : `06_input_selects/presence/jour_visite.yaml`

- Ajout documentation :
  - `documentation_arsenal/contrats/visite.md`

🎯 Effet : “Visite” continue d’être **dés-alarme-ifiée** et ramenée au bon domaine (Présence), avec une opposabilité documentaire.

---

## 💡 Éclairage jardin — renommage “plage active” + heure d’allumage (SIGNAL MINOR)

### Renames / moves (contenu identique)
- `11_template_sensors/eclairage/jardin/matin/on.yaml`
  → `11_template_sensors/eclairage/jardin/matin/plage_active.yaml`
- `11_template_sensors/eclairage/jardin/soir/on.yaml`
  → `11_template_sensors/eclairage/jardin/soir/plage_active.yaml`

### Ajout
- `11_template_sensors/eclairage/jardin/soir/heure_allumage.yaml`

🎯 Effet : clarification sémantique (“on” → **plage_active**) + nouvelle brique d’horodatage/heure pour le soir.

---

## 🧩 Templates — standardisation des en-têtes (DOCTRINE / LISIBILITÉ)

- Série de modifications sur de nombreux `11_template_sensors/**` :
  - reformulation “Rôle / Objectif / Fonctionnement / Entité générée”
  - explicitation des sources et invariants
  - mise en conformité stylistique (capteurs de contexte, autorisations, cohérence, ouvertures, etc.)

✅ Lecture : **principalement documentaire** (améliore l’opposabilité et la maintenabilité), sans signal clair de changement de logique dans les hunks montrés.

---

## 📡 Zigbee — nouveau capteur “SDB parents” + backup (SIGNAL MINOR + BRUIT MAÎTRISÉ)

### Intégration “météo Zigbee SDB parents” (noms d’entités)
- Ajouts :
  - `sensor.meteo_zigbee_sdb_parents_battery`
  - `sensor.meteo_zigbee_sdb_parents_linkquality`
- Mise à jour groupes :
  - `02_groups/batteries.yaml` : remplacement de `sensor.sdb_parents_batterie` → `sensor.meteo_zigbee_sdb_parents_battery`
  - `02_groups/zigbee_lqi.yaml` : ajout `sensor.meteo_zigbee_sdb_parents_linkquality`
- Mise à jour customize :
  - `01_customize/batteries.yaml`
  - `01_customize/zigbee_lqi.yaml`

### Zigbee2MQTT — backup coordinateur (bruit maîtrisé)
- `zigbee2mqtt/coordinator_backup.json` :
  - date : `2026-02-10T06:35:13.432Z` → `2026-02-11T06:35:13.433Z`
  - `frame_counter` : `514648` → `558394`

---

## 📚 Changelog (format versionné)

- Ajout : `documentation_arsenal/changelog/changelogs/v09_0_3.md`

---

## 🧱 Bruit / anomalie de snapshot

### ⚠️ Dossier `git/` inclus
- +4034 fichiers sous `git/` (objets, hooks, logs, index, etc.)
- Non exploité par Home Assistant, mais :
  - gonfle artificiellement le diff
  - pollue la base documentaire/snapshot
  - augmente le coût de navigation et de sauvegarde

➡️ À traiter comme **artefact** (à exclure des snapshots/diffs), sauf intention explicite de versionner ce répertoire.

### Runtime HA
- `storage/` : ~14 fichiers modifiés (artefacts internes HA).

---

## 📊 Statistiques diff (à lire avec prudence)

- Added (raw): 4045 | Removed (raw): 5 | Changed: 41 | Renamed/Moved: 3
- Added (net): 4042 | Removed (net): 2
- Dominante : `git/` = +4034 fichiers (explique l’écrasement du signal).

---

## ✅ Vérifs post-release (anti-régression)

- Outils de dev → **États** :
  - valider `sensor.pluie_aujourdhui_local`, `sensor.pluie_total_local`, `sensor.pluie_hebdomadaire`
  - valider `pluie_semaine` (utility_meter) et cohérence des unités
  - valider `sensor.meteo_zigbee_sdb_parents_*` (battery + linkquality) et affichage groupes
- UI Météo :
  - vérifier l’affichage “Cumul hebdo (52 semaines)”
- Hygiene repo/snapshot :
  - décider du sort de `git/` (exclusion recommandée si non intentionnel).

<!-- Sources: :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1} -->
