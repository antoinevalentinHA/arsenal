ARSENAL HA v9.2.6 — STABLE

---

## 📌 UI — Diagnostics Éclairage

- Remplacement des blocs `markdown` “Simulation séjour” et “Simulation garage” par des grilles homogènes (3 colonnes) :
  - Ajout d’une tuile état ON/OFF via `template: status_72_on_off` :
    - Séjour : `input_boolean.light_simulation_state` (icon `mdi:lightbulb`)
    - Garage : `input_boolean.garage_light_state` (icon `mdi:lightbulb`)
  - Ajout de tuiles “info” via `template: socle_info_72` pour les horaires (matin/soir) :
    - Séjour : `input_text.horaires_simulation_presence_matin` / `input_text.horaires_simulation_presence_soir`
    - Garage : `input_text.horaires_simulation_presence_garage_matin` / `input_text.horaires_simulation_presence_garage_soir`
    - Icônes : `mdi:weather-sunset-up` / `mdi:weather-sunset-down`

---

## 📌 UI — Réglages — Modes

- Section “🧳 Vacances” :
  - Regroupement des tuiles dates en `grid` (2 colonnes) :
    - `input_datetime.debut_vacances`
    - `input_datetime.fin_vacances`

---

## 📌 UI — Diagnostics Ouvertures

- Réordonnancement du dashboard :
  - Mise en évidence en tête de page de `binary_sensor.fenetre_ouverte_maison_avec_delai`
    (“Fenêtre ouverte (Maison) - Mixte avec délais”) avant la grille RDC/Étage.

---

## 📌 UI — Diagnostics Chauffage

- Ajustement cosmétique de section :
  - `name: "Réglages poêle"` → `name: "Mode économique"`

---

## 📌 UI — Dashboard Arsenal

- Carte “Ouvertures” :
  - Retrait de l’icône (sobriété) sur la carte de navigation vers `/diagnostics-dashboard/ouvertures`.

---

## 📌 Chauffage — Templates button-card (refactor & robustesse)

- `button_card_templates/dashboards/chauffage/decision.yaml`
  - Introduction d’un objet `variables.diag` (JS) :
    - normalisation `reel / intention / raison`
    - mapping de libellés (raison)
    - catégorisation `warn/info/ok/ko`
    - background-color piloté par la catégorie.

- `button_card_templates/dashboards/chauffage/diagnostic_global_compact.yaml`
  - Ajout `variables.diagnostic` (type : `indisponible/override/blocage/standby/absence/attente/nominal`)
  - Simplification du rendu :
    - label “Cohérent contrat • Aucune action requise”
    - background-color piloté uniquement par `variables.diagnostic.type` (charte).

- `button_card_templates/dashboards/chauffage/diagnostic_reglage_courbe_diag_72.yaml`
  - Refactor complet “diag 72” :
    - `variables.diag` calcule l’étiquette selon `kind` (`tendance_24h` / `global` / `froid`)
    - sortie robuste “Indisponible” si une source est absente
    - icônes dédiées : `mdi:chart-line`, `mdi:tune-variant`, `mdi:snowflake-thermometer`
    - couleurs : gris indispo / orange warn / vert OK.

- `button_card_templates/dashboards/chauffage/diagnostic_auto_courbe_status_72.yaml`
  - Réintégration/clarification du template avec règles explicites :
    - `kind=auto` : Inactif / Actif / Simulation / Appliqué
    - `kind=last` : dernier ajustement (ou “Aucun”)
    - couleurs conservées (rouge inactif, bleu simulation, vert appliqué, gris indispo).

- `button_card_templates/dashboards/chauffage/diagnostic_blocage_aeration.yaml`
  - Passage à un diagnostic structuré `variables.diagnostic` :
    - `blocage_episode` (Suspendu), `blocage_post` (Stabilisation), `override`, `ok`, `indisponible`
    - label multi-lignes incluant fin blocage + disponibilité ΔT
    - couleurs : gris indispo / rouge épisode / orange post / bleu override / vert OK.

---

## 📌 Capteurs métier — Ouvertures (icônes dynamiques)

- Ajout d’icônes dynamiques (lecture pure) pour améliorer la lisibilité UI, en particulier sur les états temporisés (“grâce”) :

- `11_template_sensors/ouvertures/delai/global.yaml`
  - `binary_sensor.fenetre_ouverte_maison_avec_delai` :
    - `mdi:timer-sand` pendant la grâce parents (`timer.fenetre_chambre_parents_grace == active`)
    - `mdi:window-open-variant` si capteur immédiat entrée ouvert (`binary_sensor.capteur_fenetre_entree`)
    - `grace_sejour` explicitement non déduite (valeur `false`).

- `11_template_sensors/ouvertures/delai/etage.yaml`
  - `binary_sensor.fenetre_ouverte_etage_avec_delai` :
    - `mdi:timer-sand` si fenêtres parents ouvertes + timer grâce actif
    - open/closed variants sinon.

- `11_template_sensors/ouvertures/delai/sejour.yaml`
  - `binary_sensor.fenetre_sejour_ouverte_avec_delai` :
    - `mdi:timer-sand` si une fenêtre séjour est ouverte pendant `timer.fenetre_sejour_ouverte_grace`.

- `11_template_sensors/ouvertures/delai/chambre_parents.yaml`
  - `binary_sensor.fenetre_chambre_parents_ouverte_avec_delai` :
    - `mdi:timer-sand` si une fenêtre parents est ouverte pendant `timer.fenetre_chambre_parents_grace`.

- Icônes globales (variants ouverts/fermés) :
  - `11_template_sensors/ouvertures/global/fenetres.yaml` : `binary_sensor.fenetre_ouverte_maison`
  - `11_template_sensors/ouvertures/etages/rdc.yaml` : `binary_sensor.fenetre_ouverte_rdc`
  - `11_template_sensors/ouvertures/etages/etage.yaml` : `binary_sensor.fenetre_ouverte_etage`
  - `11_template_sensors/ouvertures/global/portes_et_fenetres.yaml` : `binary_sensor.ouvertures_maison` (`mdi:lock-open-variant` si ON, `mdi:lock` si OFF)

---

## 📌 Capteurs robustes — Météo (Pluie)

- `11_template_sensors/meteo/pluie/capteurs_locaux.yaml`
  - Factorisation du fallback via ancre Jinja `&pluie_mm_local_fallback` (mapping par `this.entity_id`)
  - Suppression des `default_entity_id` :
    - `sensor.pluie_aujourdhui_local`
    - `sensor.pluie_total_local`
  - Fallback uniformisé : si source indispo → `this.state`.

---

## 📌 Viessmann / ViCare — Electricité (fallback unifié)

- `11_template_sensors/vicare/electricite/conso.yaml`
  - Introduction d’une ancre Jinja `&vicare_kwh_local_fallback` :
    - mapping par `this.entity_id` → source ViCare
    - fallback uniformisé : si source indispo → `this.state`
  - Réutilisation directe sur :
    - `sensor.electricite_chaudiere_semaine_local`
    - `sensor.electricite_chaudiere_annee_local`

---

## 📌 Viessmann / ViCare — Gaz (refactor)

- Remplacement des anciens templates gaz :
  - Supprimés :
    - `11_template_sensors/vicare/gaz/chauffage.yaml`
    - `11_template_sensors/vicare/gaz/ecs.yaml`
    - `11_template_sensors/vicare/gaz/conso_chauffage.yaml`
    - `11_template_sensors/vicare/gaz/conso_ecs.yaml`
  - Ajoutés :
    - `11_template_sensors/vicare/gaz/conso_periodique.yaml`
    - `11_template_sensors/vicare/gaz/conso_totale.yaml`

---

## 📌 Home Assistant — Runtime

- Mises à jour `storage/` (~6 fichiers) :
  - artefacts internes HA (non fonctionnels / non métier).

---

## 📌 Documentation

- Ajout (historisation) :
  - `documentation_arsenal/changelog/changelogs/v09_2_5.md`

---

## 📌 Git

- Ajout de 3 objets dans `git/objects/` (artefacts internes dépôt).

---

## 📌 STATUT

- Nature : STABLE
- Portée : UI (diagnostics + réglages), templates chauffage, capteurs robustes (ouvertures/pluie/vicare)
- Invariants :
  - Aucune régression fonctionnelle connue
  - Dette technique ajoutée : 0 (aucune détectée dans les diffs)
  - Conforme aux contrats Arsenal applicables
- Date de consolidation : 2026-02-19