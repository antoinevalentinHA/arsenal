# ARSENAL HA — Changelog v9.1.0
*(proposition : bump mineur → **9.1** ; périmètre technique : diff `arsenal_v9_0_4` → `arsenal_v9_0_5`)*

## 📌 Synthèse
- **Chantier UI structurant** : création d’un **socle button-card** documenté + début de migration des cartes/dashboards vers ce socle.
- **Durcissement capteurs** : amélioration de la détection **“Poêle en fonction”** (hystérésis, délais, garde-fous unknown/unavailable).
- **Repo / sauvegardes** : ajout massif d’objets Git (impact direct sur la taille des backups).

---

## 🧱 UI — Socle button-card (SIGNAL)

### Nouvelle bibliothèque “socle”
- Ajout d’un socle réutilisable et typé (base / decision / status / kpi / toggle / diagnostic / action…).
- Objectif : remplacer progressivement les templates historiques génériques par des briques canoniques, cohérentes et maintenables.

### Documentation associée
- Ajout d’une documentation complète “Socle UI” :
  - `documentation_arsenal/ui/socle_ui/01_carte_base.md` → `09_diagnostic.md`
- Ajout de docs “évolutions futures” UI :
  - `documentation_arsenal/evolutions_futures/analyse_ui.md`
  - `documentation_arsenal/evolutions_futures/ui.md`

---

## 🧭 UI — Navigation, badges & actions (SIGNAL)

### Réorganisation des templates génériques
- Déplacement : `action_standard_warning.yaml` → `generiques/action/action_standard_warning.yaml`
- Ajout de nouveaux templates structurés par familles :
  - actions
  - badges
  - navigation

### Nettoyage / suppression d’anciens templates
- Suppression d’un ensemble de templates historiques (alarme état, audi, météo humidex/humidité absolue, génériques action/navigation/toggle, etc.)
- Effet attendu : réduction de la dette UI, et recentrage sur le socle canon.

---

## 🗺️ Lovelace — Migration dashboards (MIGRATION)

### Dashboard “System”
- Refactor de mise en page avec utilisation de `grid` (ex. colonnes = 4) pour densifier la supervision de manière propre.

### Migration de cartes vers le socle (exemples représentatifs)
- **Capteurs de contact** :
  - rebasés sur `socle_status_72` (state masqué, sémantique/couleurs conservées)
- **Capteurs texte (diagnostic)** :
  - `carte_capteur_etat_textuel` → `socle_status_72`
  - mapping texte→couleur enrichi (critique/erreur/échec, avertissement, ok, fallback charte)
- **Mouvements** :
  - `carte_mouvement` → `socle_status`
  - gestion explicite `unknown/unavailable`
- **Chauffage** :
  - `carte_chauffage_decision` → `socle_decision_72`
- **ECS** :
  - `carte_ecs_etat_ballon` → `socle_kpi` (profil UI + capteur “couleur” métier)

---

## 🧪 Capteurs — Durcissement (SIGNAL)

### Poêle — Détection “Poêle en fonction”
- Ajout `delay_on` / `delay_off` (anti-oscillation).
- Gestion “safe” des états `unknown/unavailable/none`.
- Ajout d’un critère de cohérence thermique au déclenchement :
  - `d30 >= (d10 * 0.60)`
- Objectif : réduire les faux positifs tout en restant réactif.

---

## 🧹 Repo / Exploitabilité (MIGRATION)
- **Git** : ajout massif de fichiers sous `git/objects` (hausse mécanique du poids des sauvegardes).
- **Zigbee2MQTT** : suppression d’un ancien log archivé (`zigbee2mqtt/log/.../log.log`).

---

## 🚫 Ce que cette version ne fait pas
- Pas de refonte métier Chauffage/Alarme/ECS : la version est principalement **UI (socle + migration)**,
  avec un durcissement ciblé sur le capteur “Poêle en fonction”.
