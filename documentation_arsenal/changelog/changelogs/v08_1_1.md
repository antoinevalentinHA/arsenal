==================================================
🧠 ARSENAL HA — v8.1.1 STABLE
==================================================

Date de consolidation : **2026-01-23**

## 📌 Signal — Ce qui change vraiment

### ✅ Chauffage — Décision centrale 100% événementielle + couverture complète des causes
- Suppression du trigger périodique `time_pattern ("/10")` dans `10240000000001` — **Chauffage – Trigger décision centrale**.
- Passage à un déclenchement strictement **événementiel** (fin du polling), tout en conservant l’idempotence.
- Extension des triggers décisionnels pour couvrir les causes contractuelles sans latence :
  - `sensor.chauffage_autorisation_cible`
  - `input_boolean.aeration_episode_en_cours`
  - `input_boolean.chauffage_inhibition_geofencing`
  - transition `input_boolean.chauffage_application_en_cours → off`
  - maintien `input_boolean.systeme_stable → on`

### ✅ Chauffage — Observabilité thermique (Phase 1) : fondations structurantes
- Création du domaine : `/homeassistant/11_template_sensors/chauffage/diagnostic_thermique/`.
- Introduction des familles A/B/C/D (diagnostic passif, sans seuils métier, sans écriture, reload-safe/runtime-safe) :
  - **Famille A — Inertie post-reprise**
    - `sensor.temperature_reprise_presence_chambres`
    - `sensor.amplitude_chute_reprise_presence_chambres`
    - `sensor.duree_chute_reprise_presence_chambres`
    - `sensor.vitesse_reprise_presence_chambres`
  - **Famille B — Inertie post-arrêt**
    - `sensor.temperature_arret_presence_chambres`
    - `sensor.amplitude_overshoot_arret_presence_chambres`
    - `sensor.duree_overshoot_arret_presence_chambres`
    - `sensor.vitesse_refroidissement_presence_chambres`
  - **Famille C — Dynamiques absence**
    - `sensor.temperature_plancher_absence_chambres`
    - `sensor.duree_stabilisation_absence_chambres`
  - **Famille D — Stabilité globale**
    - `sensor.amplitude_oscillation_cycle_presence_chambres`
    - `sensor.nombre_cycles_jour_presence_chambres`
    - `sensor.duree_cycle_moyenne_presence_chambres`
- Intégration stricte aération :
  - invalidation pendant `input_boolean.aeration_pipeline_arme`
  - exclusion complète des cycles impactés
- Recorder : extension contrôlée de l’allowlist (inerties/vitesses/oscillateur), avec exclusion conservée des repères `temperature_reprise_*` et `temperature_arret_*`.

### ✅ Architecture — Gouvernance des IDs + migration “vicare → domaines”
- Extension des préfixes d’automatisations :
  - `1024 - chauffage`
  - `1025 - ecs`
  - `1026 - bouclage`
  - maintien `1013 - vicare`
- Réorganisation des arborescences : création des domaines `chauffage/`, `ecs/`, `bouclage/` + déplacement massif depuis `vicare/` vers les domaines fonctionnels.
- Migrations détectées (moves/renames “same content”) : **104 mouvements** (principalement intra top-folders).
- Refonte des IDs d’automatisations :
  - alignement sur les nouveaux préfixes
  - redémarrage des suffixes à partir de `001`
  - resynchronisation documentaire complète des références d’IDs

### ✅ ECS / Bouclage — Gouvernance inter-contexte + intégrité paramètres
- Bouclage ECS : arbitrage explicite **AUTO > MANUEL** (inter-contexte) consolidé autour de `10260000000002` — **Arrêt automatique (fin timer)** :
  - ajout d’une condition globale
  - arrêt autorisé uniquement si `input_boolean.bouclage_plage_active == off`
  - idempotence inter-contexte garantie
- Intégrité des paramètres (ECS) :
  - création de `binary_sensor.parametres_invalides_ecs`
  - invariant : `heure_fin_bouclage_ecs` strictement > `heure_debut_bouclage_ecs`
  - agrégation étendue dans `group.parametres_invalides_domaines`

---

## 📌 Autres évolutions (signal secondaire)

### 🧹 Nettoyage télémétrie chaudière (bâtiment / machine)
- Suppression des capteurs :
  - `sensor.burner_modulation_diagnostic`
  - `sensor.burner_modulation_moyenne_24h`
  - `sensor.burner_modulation_max_24h`
  - statistiques associées
- Séparation bâtiment / machine renforcée, aucun impact fonctionnel attendu.

### 🖥️ UI Lovelace — Imprimerie (simplification)
- Suppression du dashboard “boutons/commandes” :
  - retrait de `imprimerie-boutons-dashboard` dans `lovelace/dashboards.yaml`
  - suppression des raccourcis de navigation associés dans les dashboards Imprimerie

### ❄️ Climatisation — Paramétrage seuils
- Assouplissement du seuil “OFF” en mode chauffage clim :
  - `clim_offset_off` : `min` étendu à `-3.0` (au lieu de `0.0`)

### 📚 Documentation — Chauffage / ECS / ChatGPT (pivot documentaire)
- Chauffage :
  - remplacement du contrat monolithique `documentation_arsenal/contrats/chauffage.md`
    par un corpus structuré `documentation_arsenal/contrats/chauffage/`
    (gouvernance, souveraineté, capteurs, triggers, décision centrale, blocages, aération, absence/geofencing, auto-ajustement, table canonique, sémantique, …)
- ECS :
  - clarification bouclage (règles inter-contextes AUTO > MANUEL)
  - mise à jour des références d’automatisations (ex : bouclage automatique programmé référencé en `10260000000001`)
- ChatGPT :
  - durcissement des “erreurs courantes” (capteurs numériques stricts, valeurs non numériques, symptômes/causes/corrections)
  - ajout d’un corpus “cas d’usage projet chauffage” (README, architecture, nomenclature, observabilité, prompts, …)

---

## 🧾 Bruit identifié (non structurant)
- `zigbee2mqtt/coordinator_backup.json` : mise à jour timestamp (backup)
- Ajustements cosmétiques/formatage : normalisations de mise en page dans plusieurs documents / dashboards

---

## 📌 Points d’attention (structure, pas du runtime)
- Migration “vicare → domaines” : risque principal = références résiduelles à d’anciens chemins / anciens IDs / anciens intitulés de sous-dossiers.
- Cas atypique à requalifier : mouvement détecté `10_automations -> documentation_arsenal` (déclaré “same content”).
- Suppression du polling décisionnel : l’architecture repose désormais entièrement sur l’exhaustivité des événements listés (modèle assumé).

---

## 📌 STATUT
- Nature : version incrémentale gouvernée
- Portée : multi-domaines Chauffage / ECS / Bouclage / Architecture (+ UI + Docs)
- Invariants :
  - Aucune régression fonctionnelle connue
  - Aucune dette technique ajoutée hors dette Vicare identifiée
  - Conforme aux contrats Arsenal applicables

==================================================
