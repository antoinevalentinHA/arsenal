# ==========================================================
# 🧠 ARSENAL — CARTE UI
#     Système / Connectivité — Synthèse Passerelles BLE
#
# 🔖 NATURE  : carte_pivot
# 🖼️ TYPE UI : synthese
# 🎚️ CONFIANCE : forte
# ==========================================================
# 🎯 RÔLE
#   Fournir une synthèse visuelle dédiée de la disponibilité
#   API ESPHome des 4 passerelles BLE actives.
#
# 🧩 PÉRIMÈTRE
#   - Affichage et interprétation visuelle uniquement
#   - Aucune logique métier exécutive
#   - Aucune autorité système
#
# 🔗 DÉPENDANCES
#   Lit      : sensor.passerelles_ble_synthese
#   Template : socle_status_label_72
#
# 🚫 INTERDITS
#   - Prendre une décision métier
#   - Remplacer une automatisation
#   - Produire un effet matériel
#   - Devenir une source de vérité système
#
# 🏷️ STATUT
#   Carte UI — Arsenal v14.x
# ==========================================================

passerelles_ble_synthese:
  template:
    - socle_status_label_72

  entity: sensor.passerelles_ble_synthese

  icon: mdi:bluetooth-connect

  state_display: >
    [[[
      const s = entity.state;

      if (s === 'ok')
        return 'Nominal';

      if (s === 'degraded')
        return 'Dégradé';

      if (s === 'critical')
        return 'Critique';

      if (s === 'unknown')
        return 'Indéterminé';

      return 'Inconnu';
    ]]]

  label: >
    [[[
      return entity.attributes.cause || 'nominal';
    ]]]

  styles:
    card:
      - background-color: >
          [[[
            const s = entity.state;

            if (s === 'ok')
              return 'rgba(76, 175, 80, 0.2)';

            if (s === 'degraded')
              return 'rgba(255, 152, 0, 0.2)';

            if (s === 'critical')
              return 'rgba(244, 67, 54, 0.2)';

            return 'rgba(158, 158, 158, 0.1)';
          ]]]
