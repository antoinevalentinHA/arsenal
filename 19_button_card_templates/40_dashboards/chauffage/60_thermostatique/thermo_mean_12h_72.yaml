# ==========================================================
# 🧠 ARSENAL — TEMPLATE UI METIER
#     Thermostatique — Moyenne température 12h (pièce sélectionnée)
# ==========================================================
#
# 📌 STATUT
#     TEMPLATE UI METIER
#
# 🎯 ROLE
#     Affiche la moyenne de température sur 12h pour la pièce
#     courante sélectionnée dans input_select.adjustment_piece.
#
# 🧱 NATURE
#     - Template button-card
#     - Spécialisation de socle_kpi_label_72_sans_icone
#     - UI uniquement (aucune décision)
#
# 🎨 SEMANTIQUE
#     - valeur disponible      -> 🔵 bleu fixe (info diagnostic)
#     - unavailable / inconnu  -> affiche "—"
#
# 🔗 DEPENDANCES
#     - input_select.adjustment_piece                : pièce active (clé de routage)
#     - sensor.temperature_stats_12h_mean_<piece>    : moyenne 12h par pièce
#
# ⚠️ CONTRAINTES
#     - Le sensor cible est construit dynamiquement — convention de nommage requise
#     - Toutes les actions désactivées
#
# 🧩 USAGE
#     type: custom:button-card
#     template: thermo_mean_12h_72
#     entity: input_select.adjustment_piece
#
# ==========================================================

thermo_mean_12h_72:
  template: socle_kpi_label_72_sans_icone
  entity: input_select.adjustment_piece
  tap_action: { action: none }
  hold_action: { action: none }
  double_tap_action: { action: none }
  name: "Moyenne 12h"
  state_display: |
    [[[
      const piece = states['input_select.adjustment_piece']?.state;
      if (!piece || ['unknown','unavailable'].includes(piece)) return '—';
      const id = `sensor.temperature_stats_12h_mean_${piece}`;
      const v = states[id]?.state;
      if (!v || ['unknown','unavailable'].includes(v)) return '—';
      return `${v} °C`;
    ]]]
  label: "Température"
  styles:
    card:
      - background-color: |
          [[[
            const piece = states['input_select.adjustment_piece']?.state;
            if (!piece || ['unknown','unavailable'].includes(piece))
              return 'rgba(158, 158, 158, 0.1)';

            const id = `sensor.temperature_stats_12h_mean_${piece}`;
            const v = states[id]?.state;
            if (!v || ['unknown','unavailable'].includes(v))
              return 'rgba(158, 158, 158, 0.1)';

            return 'rgba(33, 150, 243, 0.2)';
          ]]]
