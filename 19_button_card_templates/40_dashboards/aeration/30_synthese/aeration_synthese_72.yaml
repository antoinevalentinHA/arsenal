# ==========================================================
# 🧠 ARSENAL — CARTE UI
#     Aération — Synthèse
#
# 🗂️ COUCHE : synthese
# 🧱 TYPE UI : synthèse priorisée
# 🔍 NIVEAU DE CONFIANCE : eleve
# ==========================================================
#
# 🎯 RÔLE
#   Afficher une phrase de synthèse priorisée de l'état
#   d'aération : blocage, épisode en cours ou nominal.
#
# 📡 SOURCE
#   - input_boolean.chauffage_blocage_aeration
#   - input_boolean.aeration_episode_en_cours
#
# ⚙️ LOGIQUE UI
#   - Priorité stricte : blocage > episode > nominal
#   - blocage = on → "Chauffage bloqué (post-aération)" → fond rouge
#   - episode = on → "Aération en cours"                → fond bleu
#   - nominal      → "Situation nominale"               → fond vert
#   - indisponible → "Indisponible"                     → fond gris
#   - Aucune action utilisateur
#
# ----------------------------------------------------------
# 🔒 STRUCTURE
#   SATELLITE et PIVOT sont mutuellement exclusifs
# ----------------------------------------------------------
#
# ⚠️ CARTE PIVOT
#   Traverse :
#     - intention (aération souhaitée ou bloquée)
#     - état réel (épisode en cours)
#     - diagnostic (situation nominale ou indisponible)
#
# ==========================================================

aeration_synthese_72:
  template: socle_status_label_72

  show_state: false
  show_name: true
  show_label: true
  show_icon: true
  color_type: card

  icon: mdi:window-open-variant

  label: |
    [[[
      const st = (id) => states[id]?.state;
      const UNAV = ['unknown','unavailable',undefined,null,'none',''];

      const blocage = st('input_boolean.chauffage_blocage_aeration');
      const episode = st('input_boolean.aeration_episode_en_cours');

      if ([blocage, episode].some(v => UNAV.includes(v)))
        return "Indisponible";

      if (blocage === 'on')
        return "Chauffage bloqué (post-aération)";

      if (episode === 'on')
        return "Aération en cours";

      return "Situation nominale";
    ]]]

  styles:
    card:
      - background-color: |
          [[[
            const st = (id) => states[id]?.state;
            const UNAV = ['unknown','unavailable',undefined,null,'none',''];

            const blocage = st('input_boolean.chauffage_blocage_aeration');
            const episode = st('input_boolean.aeration_episode_en_cours');

            if ([blocage, episode].some(v => UNAV.includes(v)))
              return "rgba(158, 158, 158, 0.1)";

            if (blocage === 'on')
              return "rgba(244, 67, 54, 0.2)";

            if (episode === 'on')
              return "rgba(33, 150, 243, 0.2)";

            return "rgba(76, 175, 80, 0.2)";
          ]]]