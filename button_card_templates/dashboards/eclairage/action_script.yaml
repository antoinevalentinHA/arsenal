#################################################################
# 💡 ACTION — ÉCLAIRAGE VIA SCRIPT (CONFIRMÉ)
#################################################################

carte_action_eclairage_script:
  template: socle_action_script_confirme

  tap_action:
    action: call-service
    service: script.garage_toggle
    confirmation:
      text: Allumer/éteindre l’éclairage ?