#################################################################
# 🔁 CARTE MODE BINAIRE INTERPRÉTÉ — UI
#################################################################

carte_mode_binaire_interprete:
  template: socle_status

  show_state: true
  show_label: false

  tap_action:
    action: none
  hold_action:
    action: none
  double_tap_action:
    action: none

  state_display: |
    [[[
      if (!entity || entity.state === 'unknown' || entity.state === 'unavailable') return '—';

      return entity.state === 'on'
        ? (variables?.label_on || 'Actif')
        : (variables?.label_off || 'Normal');
    ]]]

  styles:
    card:
      - background-color: |
          [[[
            if (!entity || entity.state === 'unknown' || entity.state === 'unavailable')
              return 'rgba(158, 158, 158, 0.1)';

            return entity.state === 'on'
              ? (variables?.color_on || 'rgba(244, 67, 54, 0.2)')
              : (variables?.color_off || 'rgba(76, 175, 80, 0.2)');
          ]]]

    name:
      - justify-self: center
      - text-align: center

    state:
      - justify-self: center
      - text-align: center