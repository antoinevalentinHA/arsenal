name: Arsenal Contracts — Sommeil

on:
  push:
  pull_request:

jobs:
  sommeil-contracts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Sommeil contract checks
        run: |
          python3 scripts/arsenal_contracts/check_sommeil_contracts.py
