name: Arsenal Contracts — Aération M2

on:
  push:
  pull_request:

jobs:
  contracts-aeration-m2:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run aération M2 contract validation
        run: python scripts/arsenal_contracts/check_aeration_m2_contracts.py