name: Arsenal Contracts — Initial Key

on:
  push:
  pull_request:

jobs:
  contracts-initial-key:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate contract — clé initial (HINIT)
        run: python scripts/arsenal_contracts/check_initial_key_contracts.py
