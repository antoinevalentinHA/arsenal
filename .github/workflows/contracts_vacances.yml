name: Arsenal Contracts — Vacances

on:
  push:
  pull_request:

jobs:
  vacances-contracts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Vacances contract checks
        run: |
          python3 scripts/arsenal_contracts/check_vacances_contracts.py