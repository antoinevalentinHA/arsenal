name: Arsenal Contracts — HR Stabilisation

on:
  push:
  pull_request:

jobs:
  validate-hr-stabilisation-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate HR stabilisation contracts
        run: python scripts/arsenal_contracts/check_hr_stabilisation_contracts.py
