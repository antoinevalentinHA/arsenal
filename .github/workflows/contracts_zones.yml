name: Arsenal Contracts — Zones

on:
  push:
  pull_request:

jobs:
  validate-zones-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate zones contracts
        run: python scripts/arsenal_contracts/check_zones_contracts.py