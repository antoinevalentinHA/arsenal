name: Arsenal Contracts — 01 Customize

on:
  push:
  pull_request:

jobs:
  validate-01-customize-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate 01_customize contracts
        run: python scripts/arsenal_contracts/check_01_customize_contracts.py