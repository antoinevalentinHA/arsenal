name: Arsenal Contracts — Humidité Relative Jardin

on:
  push:
  pull_request:

jobs:
  validate-humidite-relative-jardin-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate humidité relative jardin contracts
        run: python scripts/arsenal_contracts/check_humidite_relative_jardin_contracts.py
