name: Arsenal Contracts — BSSID Maison

on:
  push:
  pull_request:

jobs:
  contract-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Vérification contractuelle — Référentiel BSSID Maison
        run: python3 scripts/arsenal_contracts/check_bssid_contracts.py