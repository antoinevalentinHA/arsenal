name: Arsenal Contracts — SwitchBot Transactionnel

on:
  push:
  pull_request:

jobs:
  validate-switchbot-transactionnel-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate SwitchBot transactionnel contracts
        run: python scripts/arsenal_contracts/check_switchbot_transactionnel_contracts.py