name: Arsenal Contracts — Recorder

on:
  push:
  pull_request:

jobs:
  contract-recorder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate contract — Recorder
        run: python scripts/arsenal_contracts/check_recorder_contracts.py
