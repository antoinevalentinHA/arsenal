name: Arsenal Contracts — Batteries

on:
  push:
  pull_request:

jobs:
  contract-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Vérification contractuelle — Batteries des capteurs
        run: python3 scripts/arsenal_contracts/check_batteries_contracts.py