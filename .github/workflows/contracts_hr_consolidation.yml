name: Arsenal Contracts — HR Consolidation

on:
  push:
  pull_request:

jobs:
  validate-hr-consolidation-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate HR consolidation contracts
        run: python scripts/arsenal_contracts/check_hr_consolidation_contracts.py
