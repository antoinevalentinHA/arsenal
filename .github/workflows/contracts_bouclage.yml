name: Arsenal Contracts — ECS Bouclage

on:
  push:
  pull_request:

jobs:
  contract-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Vérification contractuelle — ECS Bouclage
        run: python3 scripts/arsenal_contracts/check_bouclage_contracts.py