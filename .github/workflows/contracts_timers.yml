name: Arsenal Contracts — Timers

on:
  push:
  pull_request:

jobs:
  contracts-timers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run timers contract validation
        run: python scripts/arsenal_contracts/check_timers_contracts.py