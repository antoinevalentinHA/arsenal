name: Arsenal Contracts — Deshum Guard

on:
  push:
  pull_request:

jobs:
  contracts:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run deshum guard contract checks
        run: python scripts/arsenal_contracts/check_deshum_guard_contracts.py