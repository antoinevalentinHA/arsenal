name: Arsenal Contracts — ECS Cycle

on:
  push:
  pull_request:

jobs:
  contract-ecs-cycle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate contract — ECS Cycle & Inertie
        run: python scripts/arsenal_contracts/check_ecs_cycle.py
