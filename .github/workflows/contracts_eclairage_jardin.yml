name: Arsenal Contracts — Eclairage Jardin

on:
  push:
  pull_request:

jobs:
  contract-eclairage-jardin:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate contract — Éclairage / Jardin
        run: python scripts/arsenal_contracts/check_eclairage_jardin_contracts.py
