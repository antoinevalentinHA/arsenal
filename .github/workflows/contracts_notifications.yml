name: Arsenal Contracts — Notifications

on:
  push:
  pull_request:

jobs:
  validate-notifications-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate notifications contracts
        run: python scripts/arsenal_contracts/check_notifications_contracts.py
