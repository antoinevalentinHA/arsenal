name: Arsenal Contracts — Visite

on:
  push:
  pull_request:

jobs:
  visite-contracts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Visite contract checks
        run: |
          python3 scripts/arsenal_contracts/check_visite_contracts.py