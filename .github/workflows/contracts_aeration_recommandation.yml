name: Arsenal Contracts — Aération Recommandation

on:
  push:
  pull_request:

jobs:
  contract-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Vérification contractuelle — Aération Recommandation
        run: python3 scripts/arsenal_contracts/check_aeration_recommandation_contracts.py