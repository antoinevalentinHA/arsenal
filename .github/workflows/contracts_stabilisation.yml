name: Arsenal Contracts — Stabilisation

on:
  push:
  pull_request:

jobs:
  validate-stabilisation-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate stabilisation contracts
        run: python scripts/arsenal_contracts/check_stabilisation_contracts.py
