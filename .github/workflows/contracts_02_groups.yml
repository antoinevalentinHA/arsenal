name: Arsenal Contracts — 02 Groups

on:
  push:
  pull_request:

jobs:
  validate-02-groups-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate 02_groups contracts
        run: python scripts/arsenal_contracts/check_02_groups_contracts.py