name: Arsenal Contracts — Alarme

on:
  push:
  pull_request:

jobs:
  validate-alarme-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate alarme contracts
        run: python scripts/arsenal_contracts/check_alarme_contracts.py