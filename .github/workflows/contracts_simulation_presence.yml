name: Arsenal Contracts — Simulation de présence

on:
  push:
  pull_request:

jobs:
  contract-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Vérification contractuelle — Simulation de présence
        run: python3 scripts/arsenal_contracts/check_simulation_presence_contracts.py