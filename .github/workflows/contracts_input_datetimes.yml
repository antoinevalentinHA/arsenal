name: Arsenal Contracts — Input Datetimes

on:
  push:
  pull_request:

jobs:
  contracts-input-datetimes:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run input_datetimes contract validation
        run: python scripts/arsenal_contracts/check_input_datetimes_contracts.py