name: Arsenal Contracts — 03 Input Numbers

on:
  push:
  pull_request:

jobs:
  validate-03-input-numbers-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate 03_input_numbers contracts
        run: python scripts/arsenal_contracts/check_03_input_numbers_contracts.py