name: Arsenal Contracts — Input Booleans

on:
  push:
  pull_request:

jobs:
  contracts-input-booleans:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run input_booleans contract validation
        run: python scripts/arsenal_contracts/check_input_booleans_contracts.py