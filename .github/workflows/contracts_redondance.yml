name: Arsenal Contracts — Redondance

on:
  push:
  pull_request:

jobs:
  check-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Vérification contrat réconciliation capteurs de contact
        run: python scripts/arsenal_contracts/check_redondance_contracts.py