name: Arsenal Contracts — Redondance

on:
  push:
  pull_request:

jobs:
  validate-redondance-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate redondance contracts
        run: python scripts/arsenal_contracts/check_redondance_contracts.py
