# ==========================================================
# 🧠 ARSENAL — STATISTICS SENSOR
#     Thermique — Moyenne 30j Température filtrée à l'aube
# ----------------------------------------------------------
# 🎯 RÔLE
#   Exposer la moyenne glissante sur 30 jours de la
#   température filtrée à l'aube par zone, à destination
#   des couches de diagnostic et d'analyse de tendance
#   thermique saisonnière.
#
# 🧩 PÉRIMÈTRE
#   - Statistique dérivée sur série temporelle uniquement
#   - Aucune logique métier
#   - Aucun pilotage d'équipement
#
# 🔖 NATURE
#   moyenne_glissante | reference_statistique | agregat_quantitatif
#
# 📋 PARAMÈTRES
#   state_characteristic : mean (toutes zones)
#   sampling_size        : 5000
#                          justification : borne haute couvrant
#                          la fenêtre de 30 j à fréquence
#                          d'échantillonnage usuelle, avec
#                          marge pour pics de publication
#   max_age              : 30 j
#                          justification : fenêtre mensuelle
#                          permettant l'établissement d'une
#                          référence saisonnière stable, lissant
#                          les épisodes ponctuels et les cycles
#                          hebdomadaires d'occupation
#   precision            : omis (par défaut HA)
#
# 🔗 DÉPENDANCES
#   Lit : sensor.temperature_filtre_aube_*
#
# 🚫 INTERDITS
#   - Introduire une logique métier
#   - Piloter directement un équipement
#   - Constituer à lui seul une autorité décisionnelle
#   - Laisser une fenêtre temporelle sans justification
#
# 🏷️ STATUT
#   Référence — Arsenal v14.x
# ==========================================================

# ----------------------------------------------------------
# 🏠 Chambres
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Chambre Enfants"
  entity_id: sensor.temperature_filtre_aube_chambre_enfants
  state_characteristic: mean
  max_age: &max_age_30d
    days: 30
  sampling_size: &sampling_size_5000 5000

- platform: statistics
  name: "Température filtre aube Moyenne Chambre Matthieu"
  entity_id: sensor.temperature_filtre_aube_chambre_matthieu
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Chambre Parents"
  entity_id: sensor.temperature_filtre_aube_chambre_parents
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# 🏡 Espaces de vie et communs
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Entrée"
  entity_id: sensor.temperature_filtre_aube_entree
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Séjour"
  entity_id: sensor.temperature_filtre_aube_sejour
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Petite Maison"
  entity_id: sensor.temperature_filtre_aube_petite_maison
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Palier"
  entity_id: sensor.temperature_filtre_aube_palier
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# 🚿 Salles de bains
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne SDB Enfants"
  entity_id: sensor.temperature_filtre_aube_sdb_enfants
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne SDB Parents"
  entity_id: sensor.temperature_filtre_aube_sdb_parents
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# 🌳 Extérieur et jardin
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Jardin"
  entity_id: sensor.temperature_filtre_aube_jardin
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Extérieur"
  entity_id: sensor.temperature_filtre_aube_exterieur
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# 🏢 Zones administratives
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Direction"
  entity_id: sensor.temperature_filtre_aube_direction
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Compta"
  entity_id: sensor.temperature_filtre_aube_compta
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Qualité"
  entity_id: sensor.temperature_filtre_aube_qualite
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Devis"
  entity_id: sensor.temperature_filtre_aube_devis
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Commercial"
  entity_id: sensor.temperature_filtre_aube_commercial
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# 🏭 Zones industrielles / ateliers
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Komori"
  entity_id: sensor.temperature_filtre_aube_komori
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Bobst"
  entity_id: sensor.temperature_filtre_aube_bobst
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Media"
  entity_id: sensor.temperature_filtre_aube_media
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# 📦 Zones de stockage
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Stock Carton"
  entity_id: sensor.temperature_filtre_aube_stock_carton
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Stock PF"
  entity_id: sensor.temperature_filtre_aube_stock_pf
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

# ----------------------------------------------------------
# ⚙️ Locaux techniques
# ----------------------------------------------------------
- platform: statistics
  name: "Température filtre aube Moyenne Cave"
  entity_id: sensor.temperature_filtre_aube_cave
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000

- platform: statistics
  name: "Température filtre aube Moyenne Garage"
  entity_id: sensor.temperature_filtre_aube_garage
  state_characteristic: mean
  max_age: *max_age_30d
  sampling_size: *sampling_size_5000
