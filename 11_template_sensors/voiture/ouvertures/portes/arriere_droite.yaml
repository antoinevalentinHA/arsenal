# ==========================================================
# 🚪 TEMPLATE TRIGGERED BINARY SENSOR — Audi e-tron (Porte arrière droite)
# ----------------------------------------------------------
# 🎯 Objectif :
#   Stabiliser l’état du capteur `binary_sensor.audi_a3_sportback_e_tron_right_rear_door`
#   afin de garantir un affichage fiable et continu, même en cas
#   d’interruption ou de latence de l’intégration Audi.
#
# ⚙️ Fonctionnement :
#   - Déclenchement à chaque changement d’état du capteur source.
#   - Réévaluation automatique au démarrage de Home Assistant.
#   - Si état valide → reprise directe.
#   - Sinon → conservation du dernier état connu.
#
# 🧮 Source : binary_sensor.audi_a3_sportback_e_tron_right_rear_door
# 📊 Type : Trigger Template Binary Sensor
# ==========================================================

- trigger:
    - platform: state
      entity_id: binary_sensor.audi_a3_sportback_e_tron_right_rear_door

  binary_sensor:
    - name: "Audi e-tron – Porte arrière droite (Local)"
      unique_id: audi_a3_sportback_e_tron_right_rear_door_local
      device_class: door
      state: >
        {% set s = states('binary_sensor.audi_a3_sportback_e_tron_right_rear_door') %}
        {% if s in ['on','off'] %}
          {{ s }}
        {% else %}
          {{ this.state | default('off') }}
        {% endif %}