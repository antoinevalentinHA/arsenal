esphome:
  name: esp32-ble-proxy-4
  friendly_name: ESP32 BLE Proxy 4

esp32:
  board: esp32dev

logger:

api:

ota:
  - platform: esphome

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "esp32-ble-proxy-2-fallback"
    password: !secret esp_fallback_ap_password

captive_portal:

esp32_ble_tracker:
    scan_parameters:
        interval: 1100ms
        window: 1100ms
        active: true

bluetooth_proxy: