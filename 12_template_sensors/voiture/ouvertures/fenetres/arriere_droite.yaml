# ==========================================================
# 🪟 TEMPLATE TRIGGERED BINARY SENSOR — Audi e-tron (Fenêtre arrière droite)
# ----------------------------------------------------------
# 🎯 Objectif :
#   Stabiliser l’état du capteur `binary_sensor.audi_a3_sportback_e_tron_right_rear_window`
#   pour assurer un suivi fiable et cohérent sur le dashboard.
#
# 🧮 Source : binary_sensor.audi_a3_sportback_e_tron_right_rear_window
# 📊 Type : Trigger Template Binary Sensor
# ==========================================================

- trigger:
    - platform: state
      entity_id: binary_sensor.audi_a3_sportback_e_tron_right_rear_window

  binary_sensor:
    - name: "Audi e-tron – Fenêtre arrière droite (Local)"
      unique_id: audi_a3_sportback_e_tron_right_rear_window_local
      device_class: window
      state: >
        {% set s = states('binary_sensor.audi_a3_sportback_e_tron_right_rear_window') %}
        {% if s in ['on','off'] %}
          {{ s }}
        {% else %}
          {{ this.state | default('off') }}
        {% endif %}
