button_card_templates: !include_dir_merge_named /config/19_button_card_templates

# Dashboard Boiler — entrée SYSTÈME (retour -> /system-dashboard).
# Corps partagé avec les frères boiler_chauffage.yaml / boiler_ecs.yaml ;
# seule la destination du bouton Retour (badges) diffère.
views:
  - badges: !include ../../includes/badges/diagnostics_systeme.yaml

    cards:
      - !include ../../includes/cartes/systeme/boiler_corps.yaml
