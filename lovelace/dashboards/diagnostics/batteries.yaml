button_card_templates: !include_dir_merge_named /config/button_card_templates

title: Diagnostics Batteries
icon: mdi:battery

views:
  ####################################
  # 🔋 DIAGNOSTICS BATTERIES
  ####################################
  - title: Batteries
    path: batteries
    icon: mdi:battery

    # ==========================================================
    # 🧭 BADGES — Navigation
    # ==========================================================
    badges: !include ../../includes/badges/diagnostics_systeme.yaml

    # ==========================================================
    # 🧩 CONTENU PRINCIPAL
    # ==========================================================
    cards:
      - type: vertical-stack
        cards:

          ########################################################
          # 🔋 SECTION : Batteries (vue unifiée & tri auto)
          ########################################################
          - !include ../../includes/section_headers/details.yaml

          - !include ../../includes/cartes/auto_entities_batteries.yaml
