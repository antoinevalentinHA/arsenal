# Registre de couverture de vérification normative — Cartographie de gouvernance vivante

> **État dérivé, non opposable.** Ce document ne fait **pas foi**. Les **sources font foi** : les contrats (`../contrats/`), les checkers (`../../scripts/arsenal_contracts/`) et les workflows (`../../.github/workflows/`). En cas de divergence registre ↔ source, **la source prime** et ce registre est corrigé.
> **Co-commit requis.** Tout **ajout ou retrait d'un checker ou d'un workflow** met à jour ce registre **dans le même commit** — même discipline que [`REGISTRE_CHANTIERS.md`](REGISTRE_CHANTIERS.md).
> **Ni doctrine, ni contrat.** Ce registre **ne crée aucune règle métier ou technique opposable** et **n'établit aucune doctrine** : il **mesure** un état (quelle surface normative est mécaniquement vérifiée), il ne le **prescrit** pas. Il **définit en revanche ses propres règles de maintenance documentaire** (cf. §6) — d'où l'exigence de co-commit ci-dessus, qui régit la tenue du document, **non** le comportement du système.

---

## 1. Objet et axe mesuré

Ce registre cartographie la **couverture de vérification mécanique / CI** de la surface normative d'Arsenal : pour chaque famille de contrats, **existe-t-il un checker**, et **est-il exécuté en CI** ?

**Axe explicitement exclu — la présence d'un contrat par domaine runtime.** « Chaque domaine actif (`10_scripts/`, `11_automations/`, `12_template_sensors/`…) dispose-t-il d'un contrat normatif ? » est une **mesure différente**, déjà traitée par :

- [`04_chantiers/transverses/etat_couverture_normative_domaines.md`](04_chantiers/transverses/etat_couverture_normative_domaines.md) — photographie d'état datée/épinglée (axe **contrat × domaine**) ;
- [`04_chantiers/transverses/etat_avancement_couverture_normative_domaines.md`](04_chantiers/transverses/etat_avancement_couverture_normative_domaines.md) — point d'étape vivant rattaché.

| | Couverture **normative par domaine** (docs existants) | Couverture **de vérification** (ce registre) |
|---|---|---|
| Question | Le domaine a-t-il un contrat ? | La surface normative est-elle vérifiée mécaniquement ? |
| Axe | contrat × domaine runtime | contrat × checker × workflow CI |
| Nature | photographie datée + chantier | cartographie vivante, pérenne |

Les deux sont complémentaires : un domaine peut être **contractualisé** (couvert sur l'axe domaine) sans être **vérifié mécaniquement** (non couvert sur l'axe de ce registre), et inversement.

---

## 2. Les trois couches

| Couche | Définition | Source canonique | Volume (cf. §3) |
|---|---|---|---|
| **Vérité normative** | Ce que le système DOIT faire : contrats opposables + doctrines transversales. | [`../contrats/`](../contrats/), [`../architecture/03_doctrines/`](../architecture/03_doctrines/) | 320 `.md` de contrats · 14 doctrines |
| **Couverture mécanique** | Les contrôles qui vérifient une partie de la vérité normative. | `../../scripts/arsenal_contracts/` | 88 checkers |
| **CI exécutée** | Le sous-ensemble des contrôles effectivement lancés en intégration continue. | `../../.github/workflows/` | 7 workflows — **ventilation détaillée au §3**, non recopiée ici |

Relation de couverture attendue : **surface normative à vérifier ≥ surface vérifiée mécaniquement ≥ surface effectivement exécutée en CI**. L'écart entre ces surfaces est l'objet même de la mesure, **pas** un jugement de conformité.

> **Volumes confrontés mécaniquement (COUNT-2 / COUNT-3).** La colonne « Volume (cf. §3) » ci-dessus **redit** des compteurs dont le §3 est la source déclarée. Cette duplication **a dérivé en silence** (§2 affichait 291/80/84 contre 293/82/87 réels — corrigé le 2026-07-15) : la garde du **Lot 1D** ne couvrait que le §3. Depuis, [`check_ci_coverage_registry.py`](../../scripts/arsenal_contracts/check_ci_coverage_registry.py) confronte **aussi ces trois lignes** au comptage réel — toute dérive est une **ERROR CI**. Modifier un volume ici sans le §3 (ou l'inverse) fait désormais échouer la CI.

---

## 3. Vue d'ensemble — compteurs constatés

> Compteurs **rafraîchis** au **C14 Lot 1D** (anti-dérive) — désormais **confrontés mécaniquement** par [`check_ci_coverage_registry.py`](../../scripts/arsenal_contracts/check_ci_coverage_registry.py) (exécuté dans le job unique `contracts_all.yml` depuis la consolidation ; cf. §8) : toute dérive de ces valeurs fait **échouer la CI**. Comptages bruts (commandes ci-dessous), non interprétés.

| Indicateur | Valeur | Commande de re-vérification |
|---|---|---|
| Contrats `.md` (récursif) | **320** | `find 00_documentation_arsenal/contrats -name '*.md' \| wc -l` |
| Doctrines transversales | **14** | `ls 00_documentation_arsenal/architecture/03_doctrines/*.md \| grep -v README \| wc -l` |
| Checkers | **88** | `ls scripts/arsenal_contracts/check_*.py \| wc -l` |
| Workflows (total) | **7** | `ls .github/workflows/*.yml \| wc -l` |
| Workflows `contracts_*` (préfixe) | **1** | `ls .github/workflows/contracts_*.yml \| wc -l` — l'unique `contracts_all.yml` (**job unique**, checkers regroupés par domaine en commentaires) |
| Workflows à checker hors préfixe `contracts_*` | **1** | `validation.yml` → `check_configuration_includes.py` (+ lint de style informatif) |
| Workflows portant des checkers (total) | **2** | `contracts_all.yml` (**87** `check_*.py` dans **un job unique**, regroupés par domaine en commentaires) + `validation.yml` (**1** : `check_configuration_includes.py`) = **88** checkers couverts. *(Modèle « 1 workflow = 1 checker » **abandonné** le 2026-08-06 — consolidation, cf. §8 : ~86 VM/événement → 7 jobs (2026-08-06) puis **1 job** (2026-08-09). Complétude toujours prouvée par INTEG-1 de `check_ci_coverage_registry.py`.)* |
| Orchestrateurs (sans `check_*.py`) | **4** | `doctrine` (inline) · `docs` (6 scripts `docs_lint/`) · `arsenal-ci-chauffage` (`tools/arsenal_ci`) · `arsenal-ci-climatisation` (`tools/arsenal_ci` — gate live politique d'absence COOL, C20) |
| Scanner sécurité publication (**bloquant**, sans `check_*.py`) | **1** | `security_publication_audit.yml` → `scripts/security/audit_publication_git.py` — **bloquant sur `CRITICAL`** (`--fail-on critical`, aucun `continue-on-error` ; WARNING informatifs ; `--selftest` + contrats Zigbee2MQTT en préambule). *(Corrigé le 2026-07-15 : le registre le décrivait encore « `continue-on-error`, **non bloquant** » — état du Lot 1E-b, c'est-à-dire **exactement celui qui a laissé publier les secrets Zigbee2MQTT (incident P0 du 2026-07-05, #282)**. Le job bloque depuis. **Ligne non gardée** — cf. §6.)* |
| Couplage checker ↔ CI | **N:1** (87 checkers → 1 workflow, **1 job**) | aucun checker orphelin, aucune référence morte (INTEG-1/INTEG-2 de `check_ci_coverage_registry.py`, inchangé). Le couplage historique 1:1 est **abandonné** (§8, 2026-08-06). |

**Lecture de couche CI (modèle consolidé 2026-08-06, affiné 2026-08-09).** Les 87 checkers de contrat sont exécutés par **un seul** workflow — `contracts_all.yml` — dans **un job unique** (les 87 noms regroupés par domaine en commentaires — `ecs`, `clim_chauffage`, `air_humidite`, `meteo_jardin`, `ui_lovelace`, `aspirateur`, `presence_maison`, `helpers_infra`) qui lance ses checkers en **sous-processus isolés** par `scripts/ci/run_checkers.py` (selftest auto-détecté, tous lancés, **exit 1 si un seul échoue**), sur déclencheurs `push` (limité à `main`) + `pull_request`, avec un bloc `concurrency` annulant les runs obsolètes d'une branche (audit CI 2026-08-09, cf. §8). Le 88ᵉ checker, `check_configuration_includes.py`, reste porté par `validation.yml` (bloquant + yamllint de style informatif, cf. §5.4). Les 4 orchestrateurs sans `check_*.py` : `doctrine.yml` (python inline + grep), `docs.yml` (6 scripts `scripts/docs_lint/`), `arsenal-ci-chauffage.yml` et `arsenal-ci-climatisation.yml` (package `tools/arsenal_ci`). Le scanner `security_publication_audit.yml` exécute `scripts/security/audit_publication_git.py` en **mode bloquant sur `CRITICAL`** (incident P0 du 2026-07-05). Réconciliation du total : **7 = 1 `contracts_all` + 1 `validation` + 4 orchestrateurs + 1 scanner sécurité**. **Motivation de la consolidation** : ~86 VM jetables par événement (≈25-30 s d'overhead chacune pour ~1 s de calcul utile) déclenchaient le throttling Actions de GitHub ; le regroupement (puis le job unique du 2026-08-09) descend à ≤ ~10 jobs/événement — souvent moins grâce aux filtres `paths:` ciblés (§5.5) et à la suppression de la double exécution `push`+`pull_request` — **sans toucher aux checkers ni à leur couverture** (prouvée par INTEG-1). Détail : §8, 2026-08-06 et 2026-08-09.

---

## 4. Matrice initiale — par famille

> Granularité **famille** (regroupement thématique des 88 checkers), choisie comme maintenable **sans audit clause-à-clause**. La colonne « Profondeur » reste **qualitative** : elle décrit l'objet du contrôle, **jamais** un taux de clauses couvertes (non audité). *(Le couplage 1:1 checker ↔ workflow `contracts_*` décrit ci-après est **historique** : depuis le 2026-08-06 les checkers sont regroupés dans `contracts_all.yml` (un job unique depuis le 2026-08-09) — §3, §8. Les familles ci-dessous restent la carte thématique des checkers ; seule leur exécution CI a été consolidée.)*

| Famille | Checkers | Contrats source | Profondeur (qualitative, non auditée) |
|---|---:|---|---|
| Aération (blocage chauffage) | 8 | `../contrats/aeration_blocage_chauffage/`, `aeration_recommandation.md` | Machine d'état `m0→m6` + recommandation ; un checker par module. |
| Climatisation | 3 | `../contrats/climatisation/` | Admissibilité + seuils COOL + ventilation. |
| ECS & Bouclage | 6 | `../contrats/ecs/`, `bouclage.md` | Cycle, fondations, sécurité ECS + bouclage ; **+ offsets (§10) et désinfection-retour** (`ecs_offsets_params`, `ecs_desinfection_retour`). |
| Boiler | 1 | `../contrats/boiler/` | Transactionnel. |
| Humidité & déshumidification | 6 | `../contrats/deshumidificateur/`, `meteo/` (HR) | Guard, transaction, métier déshum ; consolidation/stabilisation HR ; HR jardin. |
| Météo & température jardin | 5 | `../contrats/meteo/` | Température jardin, palmarès chaud/froid, palmarès min journalière haute, diagnostic Netatmo. |
| Éclairage & volets | 4 | `../contrats/eclairage/`, `volets_pluie.md` | Entrée, jardin, séjour ; volets pluie. |
| Présence, sécurité & absence | 8 | `../contrats/alarme/`, `simulation_presence.md`, `vacances.md`, `visite.md`, `sante/`, `babysitting.md`, `mobile.high_accuracy.*`, `bssid.md` | Alarme, simulation présence, vacances, visite, sommeil, babysitting, haute précision mobile, BSSID. |
| UI / Lovelace | 9 | `../contrats/ressources_lovelace.md`, `ui/`, `ui/couleurs/02_palette.md`, `ui/couleurs/05_regles.md`, `ui/pattern_dashboard.md` | Includes, navigation, en-têtes de section ; couleurs UI (charte) + couleurs runtime (lexical) + **couleurs sémantiques** (`ui_semantic_colors` : garde indisponibilité R6 — tout `background-color` résolvant un état littéral `'unavailable'`/`'unknown'` doit peindre le gris indisponibilité `rgba(158,158,158,0.1)`, distinct du gris neutre `0.2` ; opt-out tracé `# arsenal:semantic-colors:ack`) + **hiérarchie sémantique** (`ui_semantic_colors_hierarchy` : I2, R1–R3 — dans une cascade de MASQUAGE à seuils numériques avec vert en fallback, interdit de retourner une couleur moins sévère avant une plus sévère ; bandes exclusives / mappings d'état / thermique explicitement hors périmètre) ; **squelette pur** (`lovelace_no_inline_templating` : aucun template Jinja inline dans `dashboards/**`, garde-fou anti-régression) ; **géométrie des grilles** (`lovelace_grid` : R-LL-GRID-1 — complétude structurelle statique des cartes `type: grid` — `columns` entier > 0, `cards` liste non vide, cellules directes divisibles ; **R-LL-GRID-2** — grilles dynamiques (`conditional`/`visibility:` enfant direct) évaluées par motifs statiquement démontrables (A : `columns==1` ; B : fixes + paires complémentaires sur une même entité), sinon non conforme ; un succès G1 ne prouve pas G2 ; CH-LL-CI-2). |
| Helpers & socle de configuration | 11 | `01_customize`, `02_groups`, `03_input_numbers`, `06_input_selects`, `19_button_card_templates`, inputs, counters, timers, zones | Briques structurelles numérotées + helpers + zones. |
| Notifications | 1 | `../contrats/notifications.md` | Contrat notifications. |
| Système, infra & gouvernance | 14 | `arsenal_self`, `recorder`, `redondance`, `resilience_integrations`, `ups_arret_ha`, `batteries`, `parametres_invalides`, `registre_chantiers`, etc. | Self-checks Arsenal, recorder, résilience, UPS, batteries, paramètres invalides, lien registre chantiers, garage, voiture, switchbot, consolidation, stabilisation. |
| Présence (séparation/confinement) | 1 | `../contrats/alarme/30_decision_centrale.md`, `presence.md`, `architecture/presence/` | `presence` — invariants **déjà vrais** (anti-régression) : séparation confort↔sûreté (R1), confinement de `presence_famille_securite_confirmee_alarme` (R2), voies armement/désarmement alarme (R3). Workflow **non filtré**. |
| Doctrine `initial` (restauration d'état) | 1 | [`../architecture/03_doctrines/restauration_etat_helpers.md`](../architecture/03_doctrines/restauration_etat_helpers.md) | `check_initial_key_contracts` — garde-fou de la clé `initial` (source de vérité : marqueur `initial VOULU`) ; **bloquant** ; workflow `contracts_initial_key.yml` (non filtré). État post-#207 : 15 occurrences, **0 ERROR / 0 WARN / 15 INFO**. Doctrine transversale **réellement instrumentée** (cf. §5.2). |
| IDs & préfixes d'automatisations (AID/APD/PID) | 3 | [`../architecture/03_doctrines/id_automatisations.md`](../architecture/03_doctrines/id_automatisations.md), [`prefixe_domaine_automatisations.md`](../architecture/03_doctrines/prefixe_domaine_automatisations.md) | `automation_ids` (AID : format 14 chiffres strict, préfixe déclaré, unicité) + `automation_prefix_domain` (APD : cohérence préfixe↔domaine, registre d'exceptions) + `prefix_id` (PID : intégrité de la **source de vérité** `06_input_selects/system/prefix_id.yaml` — format canonique « NNNN - domaine », unicité préfixe **et** domaine ; C14, ferme la dette « source de vérité jamais contrôlée pour elle-même », auparavant abandonnée en silence par AID/APD). Checkers **transversaux** d'IDs (cf. §5.2 — l'ancienne mention « pas de checker transversal d'IDs » est **caduque**). |
| Chauffage — étanchéité observabilité courbe | 1 | `../contrats/chauffage/76_observabilite_auto_ajustement_courbe.md` | `chauffage_courbe_etancheite` — INV-2 rendu bloquant (indépendant du moteur warn-only). |
| Chargement HA — includes `configuration.yaml` | 1 | `../../configuration.yaml` | `configuration_includes` — résolution des includes (C14 Lot 1C) ; hébergé par `validation.yml` (step bloquant). |
| Gouvernance CI — anti-dérive registre couverture | 1 | [`REGISTRE_COUVERTURE_VERIFICATION.md`](REGISTRE_COUVERTURE_VERIFICATION.md) | `ci_coverage_registry` — confronte compteurs §3 + intégrité checkers↔workflows (C14 Lot 1D) ; `check_ci_coverage_registry.py`, exécuté dans le job unique `contracts_all.yml` (cf. §8). |
| Frontière de périmètre — sources hors domicile | 1 | [`../architecture/03_doctrines/principes_generaux.md`](../architecture/03_doctrines/principes_generaux.md) §10, [`01_rapports/architecture/audit_frontiere_maison_imprimerie_sources_exterieures.md`](01_rapports/architecture/audit_frontiere_maison_imprimerie_sources_exterieures.md) | `source_isolation` — deny-list **exacte** des sources extérieures hors domicile (périmètre du site Imprimerie) interdites dans une décision Maison ; matching par frontière de token (exempte `sensor.temperature_exterieure_moyenne_jour`, source jardin) ; workflow `contracts_source_isolation.yml`. |
| Précipitations — production / qualification | 1 | [`../contrats/meteo/pluie_production.md`](../contrats/meteo/pluie_production.md) | `pluie_production` — acquisition sécurisée + qualifications **plurielles par design** (any-rain / forte / récente / prévue) ; source quantitative exclusive de « pluie forte » (INV-PROD-3) ; **verrou anti-régression C16** (sortie consommant l'évidence, INV-PROD-5) ; frontière de `volets_pluie.md`. Workflow `contracts_pluie_production.yml` (non filtré). |
| VMC — initialisation des paramètres | 1 | [`04_chantiers/vmc/arbitrage_propriete_parametres_vmc.md`](04_chantiers/vmc/arbitrage_propriete_parametres_vmc.md), [`realisation_l70_parametres_vmc.md`](04_chantiers/vmc/realisation_l70_parametres_vmc.md) | `vmc_initialisation` — **contrôle de comportement**, non lexical : le prédicat Jinja est **extrait du YAML contrôlé** et **évalué** contre les scénarios de la sémantique retenue : `unavailable` **jamais retenu** · `unknown` persistant **retenu**, cas normal d'un helper neuf sans état à restaurer · numérique égal, différent ou hors plage d'amorce **conservé** · **épisode `unavailable` puis restauration numérique joué comme une séquence aux trois instants** · contrôle croisé · **contrôle d'amorçabilité symétrique** — un déploiement neuf, dix helpers à `unknown`, doit tous les amorcer, la garde ne devant pas aller trop loin. Aucune logique n'est reproduite dans le test — une copie du prédicat le rendrait aveugle à toute dérive. Garde également l'**attente bornée** (`timeout` + `continue_on_timeout`, `for:` sur le déclencheur d'état) et l'**absence de repli numérique silencieux**. Workflow `contracts_vmc_initialisation.yml`, `--selftest` exécuté après le contrôle réel. |
| Aspirateur — intégrité normative et conduite runtime | 1 | [`../contrats/aspirateur/`](../contrats/aspirateur/README.md), `10_scripts/aspirateur/`, `04_input_texts/aspirateur/`, `12_template_sensors/aspirateur/` | `aspirateur` — **deux couches depuis le lot runtime L1**. (1) Intégrité du système normatif : numérotation `ASP-INV-*` et unicité de `ASP-IMC-1` ; clôture du référentiel de segments (périmètres et raccourcis mono-carte, dans la table canonique) ; clôture du catalogue de codes (aucun motif orphelin, aucun refus qu'aucune clause ne produit) ; clôture de la partition d'états (classes disjointes, tout état cité classé) ; table des cinq profils (`gentle` exclu, valeurs natives bornées) ; attestation des entités natives par l'audit du domaine ; absence d'entité native d'action Roborock dans les arbres Lovelace ; deux constantes temporelles et deux seulement. (2) Conduite runtime : écrivain unique et absence d'écrivain Roborock concurrent dans TOUT le YAML de configuration ; charge utile **rendue** et vérifiée enveloppée ; convention ×1 / ×2 / ×3 (`repeat` absent pour ×1, jamais `0` ni `1`) ; voie zonée et démarreurs interdits ; mode de nettoyage jamais écrit ; ordre carte → eau → aspiration → revérification intégrale → commande unique ; vocabulaire de verdict fermé et ordonnancement des issues (`ISSUE_NON_ETABLIE` avant l'appel, `COMMANDE_ACCEPTEE` au seul retour réussi, aucun `continue_on_error`) ; motif lisible total sur les 18 codes ; concordance des identifiants attribués et du référentiel embarqué, espace finale de `Étage ` comprise, capacité des helpers confrontée à la sérialisation maximale d'une intention V1. (3) Contrôles PAR RENDU, qui exécutent les gabarits au lieu de les lire : le `data:` de la sélection de carte doit se rendre en mapping et restituer l'option exacte, espace finale comprise ; aucune variable ne doit se rendre en valeur retypée par `_parse_result` ; la garde de type de `segments` refuse un mapping ; l'état canonique est rendu sur les 43 valeurs réellement exposées par l'appareil plus `unavailable` et confronté à l'image contractuelle ; la garde de lancement est rendue sur le produit cartésien des quatre témoins et confrontée à la règle du moteur ; chaque témoin relu tardivement produit son refus. **(4) Acte contractuel Maintenance (lot M0) : le chapitre `14_entretien.md` est couvert.** `ASP-CI-29` à `ASP-CI-33` couvrent le périmètre fermé à quatre postes et ses huit entités natives attestées, le seuil unique d'échéance à 10 % et ses trois situations, les séquences différées de remise à zéro — pression unique, relecture bornée, issue terminale —, l'interdiction des pressions sur la primitive irréversible hors allowlist nominative (allowlist **vide** en M0, indesserrable par sa seule modification : toute allowlist non vide échoue tant que le verrou transitoire reste actif, et son ouverture exige une modification explicite du checker portant simultanément sur le verrou et sur le visiteur YAML — changement de frontière CI soumis à revue), et le caractère **minimal** de l'amendement du chapitre `08` §6. **(5) Projection d'entretien (lot M1) :** `ASP-CI-34` à `ASP-CI-36` couvrent le périmètre fermé, le seuil et l'autorité de la projection ; la projection **rendue** sur les trois situations — dû, non dû, non évaluable —, en exécutant les gabarits du capteur de synthèse et du témoin binaire plutôt qu'en les lisant ; et les interdits de la projection. Ils portent sur les deux entités dérivées, non sur la notification. **(6) Projection persistante d'entretien (lot N1) :** `ASP-CI-37` à `ASP-CI-39` couvrent l'unicité du writer et des identifiants d'automation et de notification, la présence conjointe de la création et de la suppression dans le même writer, le mode et les trois déclencheurs — dont la borne `attribute: postes_dus`, qui empêche la projection de suivre le rythme du coordinateur, et le déclencheur de réconciliation au readiness ; la chaîne **rendue** M1 → N1 sur les huit scénarios exigés plus les pièges adverses, avec la fermeture de la suppression aux deux faces de l'autorité — témoin `off`, état scalaire `aucun`, attribut `postes_dus` présent et vide ; et les interdits du lot, dont le verrou qui refusait la projection de cycle `10280000000002` tant que son autorité d'extinction n'existait pas — **verrou levé par le lot `L2`, qui livre les huit valeurs de classe terminale**. Ils ne prouvent rien du rendu Markdown du message. **(7) Acte contractuel et runtime de conduite et de supervision (lot L2) : le chapitre `15_conduite_et_supervision.md` est couvert.** Les contrôles `ASP-CI-11`, `14`, `18`, `19`, `20`, `37` et `39` sont **étendus au runtime L2**, sans qu'aucun contrôle nouveau soit créé : liste d'autorisation des **trois** écrivains du verdict sous forme de table `{fichier → ensemble littéral}`, avec vérification de la **disjonction deux à deux**, de la **couverture totale** du vocabulaire et de l'absence de tout écrivain concurrent dans le dépôt ; appels d'appareil admis dans le **seul** fichier de conduite et lecture du verdict admise pour la **seule** projection de mission ; unicité de la primitive de démarrage dans les fichiers L2, adossée aux quatre conditions de sa garde fermée ; vocabulaire porté à **34** valeurs, décompte du helper **recalculé** et atteignabilité confrontée aux **trois** écrivains ; obligation, pour chaque geste, d'écrire son engagement **avant** son unique émission et de conclure dans une branche de relecture ; motif lisible total sur les 18 codes du catalogue **et les 18 valeurs de cycle de vie** ; fenêtres L2 bornées à la seule constante de confirmation, toute autre temporisation étant refusée ; et, côté projections, l'unicité de writer des deux notifications persistantes ainsi que l'abstention de la projection de mission, lectrice pure du verdict. `ASP-CI-10` n'est **pas** amendé : la mutualisation des quatre fenêtres à une valeur déjà admise ne produit aucune durée concurrente. Ils ne prouvent **ni** la réponse réelle de l'appareil aux quatre commandes, **ni** l'aboutissement d'une chaîne de retour, jamais observée directement. **(8) Couche d'intention (lot `U0`) :** `ASP-CI-28`, jusque-là **réservé**, est **livré**. Il confronte la **seconde matérialisation** du référentiel — les quatorze `input_boolean.aspirateur_segment_‹paire canonique›`, leurs paires, leur appartenance aux cartes et leurs libellés Arsenal — au chapitre `02` §2 **et** au référentiel embarqué du moteur `L1`, ainsi que le **mapping** qui les relie : tables de traduction carte / profil / passages, ordre canonique de la table de composition, options exactes des trois sélecteurs, absence d'`initial:` sur les dix-sept helpers, et les **cinq raccourcis** du chapitre `10` §3 — périmètre exact, caractère mono-carte, et interdiction de présumer profil ou passages. La seconde copie n'est pas niée : elle est **assumée et gardée**, comme `ASP-CI-21` garde celle du moteur (arbitrage `A-13`). `ASP-CI-11`, `37` et `39` voient leur périmètre étendu à l'automation `10280000000004` — quatrième nature du dossier, lectrice du verdict par **exception nominative**, qui ne l'écrit jamais et ne commande jamais l'appareil. **(9) Déclaration d'entretien (lot `M2`) :** `ASP-CI-40` à `ASP-CI-42` sont **livrés**, et le **verrou transitoire d'`ASP-CI-31` est levé par ce qui le remplace**. Le balayage de la primitive irréversible cesse d'être textuel : le visiteur **charge** chaque document et le **visite récursivement**, mappings, listes et clés compris — il rattrape donc le flow mapping, `tap_action: call-service`, le scalaire replié, l'alias YAML, le ciblage par `device_id` et l'`entity_id` templatisé, six formes que l'expression régulière de `M0` manquait. L'état du visiteur **dérive d'un essai réel** sur ces six formes adverses, et non d'un booléen déclaratif : une régression du parseur rabaisse le drapeau et **referme le verrou** sur une allowlist non vide, ce que l'auto-test joue **dans les deux sens** ; une allowlist nommant un fichier inexistant est refusée comme **autorisation dormante**. `ASP-CI-40` confronte le fichier `M2` aux sept obligations du chapitre `14` §6.1 — script unique, `mode: single`, champ fermé à quatre valeurs, quatre branches littérales pressant les quatre boutons exacts et eux seuls, aucun ciblage par `device_id`/`area_id`/`label_id`/`floor_id`, aucun `entity_id` ni service templatisé, aucune répétition — et **confronte au périmètre de `M1` la seconde matérialisation** du référentiel que porte sa table de traduction, comme `ASP-CI-28` le fait pour la couche d'intention (`A-13`). `ASP-CI-41` rend la **séquence** : la mesure est capturée **avant** la pression, la pression est refusée sur une mesure non numérique comme sur un compteur déjà au plafond, la postcondition est une **transition** comparée à la valeur capturée — jamais l'état statique `== plafond`, qui serait vrai d'avance et produirait une confirmation fausse —, la fenêtre vaut la seule constante de confirmation et poursuit à son expiration, les **deux** issues terminales sont écrites, et ni l'horodatage du bouton, ni une notification, ni une conclusion de panne n'y figurent. `ASP-CI-42` prouve que l'**écran d'entretien** n'appelle que le script backend : aucun `button.press`, aucune entité native de pression même citée, une confirmation nommant le poste par geste, les quatre valeurs du vocabulaire fermé couvertes, et un bandeau à trois destinations dont la clé de dashboard existe réellement. Ils ne prouvent **ni** l'effet réel de la pression sur le micrologiciel, **ni** la valeur remontée ensuite, **ni** le délai de propagation. |
| **Total** | **88** | | |

> Le détail checker-par-checker (clé stable = nom du checker) pourra être déplié ultérieurement sous chaque famille, sans audit de clauses, si le besoin de traçabilité fine apparaît (cf. §6).

---

## 5. Angles morts connus

Écarts **constatés** entre couches, sans jugement de conformité globale :

1. **Densité normative ≫ densité mécanique.** 320 fichiers de contrats Markdown (+ 14 doctrines) pour 88 checkers : plusieurs pages normatives (p. ex. sous-modules d'un même domaine) se projettent sur un nombre réduit de contrôles. La couverture mécanique est **partielle par construction**.
2. **Doctrines partiellement instrumentées.** Sur les 14 doctrines de `../architecture/03_doctrines/`, plusieurs règles transversales font désormais l'objet d'un contrôle CI : `doctrine.yml` (interdiction `platform: template` ancrée sur `12_template_sensors.md`, présence de `mode:` **par automation** ancrée sur `11_automations.md`, cf. C14 Lot 1B) ; **les IDs d'automatisations sont instrumentés transversalement** par `check_automation_ids_contracts.py` (doctrine `id_automatisations.md`) et la cohérence préfixe↔domaine par `check_automation_prefix_domain_contracts.py` (doctrine `prefixe_domaine_automatisations.md`) — l'ancienne affirmation « il n'existe pas de checker transversal pour les IDs d'automatisations » est **caduque** (corrigée au C14 Lot 1D). La **source de vérité** de ces préfixes (`06_input_selects/system/prefix_id.yaml`), jusque-là consommée par AID/APD mais **jamais contrôlée pour elle-même** (une option malformée était abandonnée en silence), est désormais gardée par `check_prefix_id_contracts.py` (PID : format canonique « NNNN - domaine », unicité préfixe et domaine ; C14). La doctrine `restauration_etat_helpers.md` (clé `initial`) est instrumentée par `check_initial_key_contracts.py` (bloquant). En revanche, **le nommage des entités** (`nommage_entites.md`) n'a toujours pas de checker transversal, et plusieurs doctrines (causalité métier, gestion du temps, commandabilité, séparation décision/action hors chauffage) restent non instrumentées.
3. **Moteur de raisonnement limité à un domaine.** Le moteur 3 étages (`lint` / `decision` / `execution`) de [`../../tools/arsenal_ci/`](../../tools/arsenal_ci/) n'est câblé en CI **que pour le chauffage** (`arsenal-ci-chauffage.yml`, filtré par `paths:`). Les autres domaines n'ont en CI que leur checker contractuel, pas les étages décision/exécution.
4. **Contrôles non bloquants / assumés.** `validation.yml` exécute désormais (C14 Lot 1C) un **step bloquant** de résolution des includes (`check_configuration_includes.py`) **et** un lint de style yamllint **explicitement informatif** (`continue-on-error: true`, non bloquant — l'ancien `|| true` trompeur a été retiré). `doctrine.yml` (C14 Lot 1B) est ancré et durci : le placeholder mort `default_entity_id` a été supprimé ; les deux règles actives (`platform: template`, `mode:` par automation) bloquent. `arsenal-ci-chauffage.yml` reste en **warn-only** (`ARSENAL_CI_ENFORCE=false`). `contracts_resilience_integrations` est en mode `report` + `STRICT_ON_NEW=1` (dette gelée, nouvel écart bloquant).
5. **Déclencheurs — consolidation (2026-08-06) puis affinage ciblé (audit CI 2026-08-09).** La consolidation du 2026-08-06 avait supprimé les filtres `paths:` hétérogènes des ~86 anciens `contracts_*.yml`. L'audit CI du 2026-08-09 a ensuite : (a) limité les `push` à `branches: [main]` et ajouté `concurrency`/`cancel-in-progress` — fin de la **double exécution** `push`+`pull_request` sur les branches de PR et annulation des runs obsolètes ; (b) fusionné la matrice `contracts_all` en **un seul job** ; (c) **réintroduit des filtres `paths:` ciblés**, mais uniquement sur trois workflows à périmètre de lecture **borné et vérifié dans la source des checkers** : `validation.yml` (`**.yaml`/`**.yml` — ne lit que du YAML), `doctrine.yml` (`12_template_sensors/**` + `11_automations/**`), `docs.yml` (`00_documentation_arsenal/**` + `scripts/docs_lint/**`). **`contracts_all.yml` reste volontairement sans filtre `paths:`** (toujours actif sur tout `push`-`main` + `pull_request`) : son checker de couverture compte les `.md` de contrats et doctrines, un filtre y risquerait une dérive de registre **non détectée**. `main` n'étant **pas protégée** (aucun *required status check*), un run skippé par `paths:` ne laisse aucun check `pending` et **ne bloque aucun merge** — pas de sentinelle nécessaire. *(Analyse historique « 67/80 non filtrés / 13/80 filtrés » caduque — modèle 1 workflow/checker abandonné, cf. §8.)*
6. **Écarts d'étiquette — tranché (2026-09-02).** `REGISTRE_CHANTIERS.md` qualifiait son contrôle de « non bloquant » alors que `check_registre_chantiers.py` sort en code 1 (bloquant en CI). La divergence est arbitrée **à la source** en faveur du réel : la règle de gouvernance n°3 du registre déclare désormais le contrôle **bloquant** et énumère ses deux invariants — `REG-1` (aucun lien mort) et `REG-2` (aucun identifiant non attribué en colonne `ID`). `REG-2` est né du défaut inverse : deux chantiers ouverts, runtime livré, ont vécu au registre sous `*(Cxx à attribuer)*` sans qu'aucune porte ne s'y oppose.
7. **Profondeur intra-checker non mesurée.** Un checker peut vérifier une clause unique ou l'intégralité d'un contrat. Ce registre **ne mesure pas** le taux de clauses couvertes (aucun audit clause-à-clause réalisé).

---

## 6. Règles de maintenance

1. **Co-commit — désormais confronté (C14 Lot 1D, étendu le 2026-07-15).** Tout ajout/retrait d'un `check_*.py` ou d'un workflow met à jour ce registre dans le même commit (compteurs §3 + famille §4). Cette discipline n'est plus seulement documentaire : [`check_ci_coverage_registry.py`](../../scripts/arsenal_contracts/check_ci_coverage_registry.py) **échoue la CI** si les compteurs §3 dérivent (COUNT-0/1), **si les volumes §2 dérivent** (COUNT-2/3), si un `check_*.py` est orphelin (aucun workflow), ou si un workflow référence un script inexistant. Un checker sans workflow doit soit recevoir son workflow, soit être classé `CHECKER_HELPERS` dans le checker anti-dérive.
   - **Portée volontairement bornée.** La garde des volumes est **ancrée sur les 3 lignes du §2** et sur les lignes d'indicateurs du §3 — **jamais sur tout le document**. Le **§8 (journal)** est une **trace historique datée**, truffée de nombres périmés par nature (« checkers 68→71 », « contrats 267→290 ») : les confronter au réel rendrait le journal inécrivable. **Le journal est de l'histoire, pas une déclaration d'état.**
   - **Toute source d'un compteur gardé déclenche la garde.** Le `paths:` du workflow couvre donc aussi `00_documentation_arsenal/contrats/**` et `architecture/03_doctrines/**` : sans eux, ajouter un contrat faisait dériver `contrats_md` **sans déclencher le contrôle**, et la dérive tombait plus tard sur une PR sans rapport.
   - **Le juge se vérifie avant de juger.** Le workflow exécute `--selftest` **avant** le contrôle réel (contrainte de méthode C14 §6 : *« tout checker embarque son auto-contrôle »*).
   - **⚠️ Ce qui reste HORS garde (dette assumée, 2026-07-15).** COUNT-0/1 ne confronte que **5 lignes d'indicateurs** du §3 (contrats, doctrines, checkers, workflows total, `contracts_*`). Restent **non gardées** : les lignes **dérivées** du §3 (« workflows à checker », « orchestrateurs », « scanner », « couplage »), la **prose** du §3, et les **matrices §4/§5**. Ce n'est pas théorique : au 2026-07-15, trois de ces énoncés étaient **faux** (« 81 à checker » contre 82 réels et contre le couplage 1:1 déclaré deux lignes plus bas ; scanner dit « non bloquant » alors qu'il **bloque** ; prose « 85 = … » contre 87). Corrigés à la main. **Les étendre exige de définir en code « orchestrateur » et « workflow à checker » — arbitrage propriétaire, non tranché ici.** Tant qu'ils sont hors garde, **ils dériveront à nouveau** : ne pas les lire comme une mesure fiable.
2. **La source prime.** Aucune valeur n'est recopiée comme vérité : les compteurs sont **re-vérifiables** par les commandes de §3 ; en cas d'écart, on corrige le registre, jamais la source pour coller au registre.
3. **Granularité famille par défaut.** On reste à la maille **famille** tant qu'aucun audit clause-à-clause n'est mené. Tout dépliage checker-par-checker doit conserver une **clé stable** (nom du checker), sans introduire de taux de couverture non audité.
4. **Pérenne, non clôturable.** Ce document **ne se clôt pas** (contrairement à un chantier de `04_chantiers/`). Il vit avec le harnais.
5. **Pas d'opposabilité.** Aucune ligne ne doit être citée comme faisant foi contre un contrat, un checker ou un workflow.
6. **Aucune conclusion de conformité globale.** Le registre décrit des écarts ; il ne déclare jamais le système « conforme » ou « non conforme » dans son ensemble.

---

## 7. Liens croisés

- [`REGISTRE_CHANTIERS.md`](REGISTRE_CHANTIERS.md) — cockpit des chantiers (instrument de gouvernance vivant frère).
- [`index.md`](index.md) — index de navigation des audits.
- [`04_chantiers/transverses/etat_couverture_normative_domaines.md`](04_chantiers/transverses/etat_couverture_normative_domaines.md) — couverture normative **par domaine** (axe distinct, cf. §1).
- [`04_chantiers/transverses/etat_avancement_couverture_normative_domaines.md`](04_chantiers/transverses/etat_avancement_couverture_normative_domaines.md) — point d'étape vivant rattaché.
- [`01_rapports/architecture/audit_couverture_maturite_gouvernance_consolide.md`](01_rapports/architecture/audit_couverture_maturite_gouvernance_consolide.md) — audit ponctuel consolidé couverture/maturité/gouvernance.
- [`../architecture/03_doctrines/`](../architecture/03_doctrines/) — doctrines transversales (couche « vérité normative »).

---

## 8. Journal de révision

> Léger : une ligne par mise à jour de fond (périmètre touché). Pas un changelog runtime.

| Date | Base observée | Périmètre |
|---|---|---|
| 2026-06-20 | `main` @ `fe3c5b7` | Création initiale. Compteurs : 267 contrats · 10 doctrines · 67 checkers · 71 workflows (67 contrats, 1:1) · 4 orchestrateurs. Matrice par famille (12 familles, 67 checkers). |
| 2026-06-20 | working tree | Ajout domaine **présence** : `check_presence_contracts.py` + `contracts_presence.yml` (non filtré). Compteurs → 68 checkers · 72 workflows (68 contrats, 1:1). Couvre des invariants déjà vrais (séparation/confinement/voies armement-désarmement). |
| 2026-07-01 | `main` @ `a5a7566` | Ajout de l'axe **HINIT** (doctrine `initial`) : `check_initial_key_contracts.py` + `contracts_initial_key.yml` (bloquant), doctrine `restauration_etat_helpers.md` v1.1. État : 15 occurrences · 0 ERROR / 0 WARN / 15 INFO. Matrice §4 : +1 famille (Total 68→69). **NB** : les compteurs §3 (photographie 2026-06-20) ont dérivé au-delà de HINIT (checkers 68→**71**, contrats 267→**290**, doctrines 10→**12**, workflows 72→**75**) — un **rafraîchissement global** de §3 reste à mener séparément (non réalisé ici pour éviter un re-binnage à l'aveugle). |
| 2026-07-01 | `main` @ `374b2bc` | **Rafraîchissement global des compteurs §3** (chantier C12 du registre des chantiers, clôturé au même commit). Recompte fiable post-HINIT remplaçant la photographie 2026-06-20 : contrats 267→**290**, doctrines 10→**12**, checkers 68→**71**, workflows total 72→**75**. **Réconciliation** : **71 checkers ↔ 71 workflows de contrat** (70 `contracts_*` + 1 exception de nommage `clim_ventilation_contracts.yml` → `check_climatisation_ventilation_contracts.py`) + 4 orchestrateurs = 75 ; couplage **1:1 rétabli**, 0 orphelin des deux côtés. Matrice §4 alignée (Total 69→**71** : Climatisation 2→3 « +ventilation », Météo 4→5 « +palmarès min journalière haute » ; `vmc`/`redondance` déjà comptés en « Système, infra & gouvernance »). §5 re-mesuré : **60/71** non filtrés, **11/71** filtrés `paths:` (les 9 antérieurs + `climatisation_ventilation` + `palmares_temperature_min_journaliere_haute`). Cohérence §2/§3/§4/§5/§8 rétablie. Aucun runtime, checker, workflow ou contrat modifié — mesure documentaire seule. |
| 2026-07-04 | `main` @ `de55d23` (+ ce lot) | **C14 Lot 1D — anti-dérive.** Rafraîchissement post-Lots 1A→1C **et** mise sous garde CI : compteurs §3 checkers 71→**78**, workflows 75→**81**, `contracts_*` 70→**76**, workflows à checker 71→**78** (76 `contracts_*` + `clim_ventilation` + `validation`), orchestrateurs 4→**3** (`validation` requalifié workflow à checker). Commande §3 des doctrines corrigée (`grep -v README` → 12). Matrice §4 : Total 71→**78** (ECS 4→6 ; +4 familles : AID/APD, chauffage étanchéité, includes HA, anti-dérive registre). §5.2 corrigé (affirmation « pas de checker transversal d'IDs » caduque) ; §5.4 corrigé (`validation.yml` : `\|\| true` retiré, includes bloquant + yamllint informatif) ; §5.5 re-mesuré (**66/78** non filtrés, **12/78** filtrés). **Nouveauté :** ces compteurs et l'intégrité checkers↔workflows sont désormais **confrontés mécaniquement** par `check_ci_coverage_registry.py` (workflow `contracts_ci_coverage_registry.yml`) — la dérive redevient une **ERROR CI**, plus un oubli silencieux. |
| 2026-07-07 | `main` @ `6d3eac3` (+ ce lot) | **Séparation des périmètres — sources extérieures hors domicile** (suite audit frontière Maison / site Imprimerie, PR #299). Ajout de `check_source_isolation_contracts.py` + `contracts_source_isolation.yml` (bloquant, non filtré) : deny-list **exacte** (9 sources cœur `_exterieur` + 15 filtrés + 6 seuils = 30 entités), matching par frontière de token (exempte `sensor.temperature_exterieure_moyenne_jour`, source jardin) ; périmètre de scan = 8 domaines de décision Maison × `12_template_sensors`/`11_automations`/`10_scripts`. Compteurs §3 : checkers 78→**79**, workflows 82→**83**, `contracts_*` 76→**77**, workflows à checker 78→**79** ; couplage 1:1 (79↔79). Matrice §4 : +1 famille « Frontière de périmètre — sources hors domicile », Total 78→**79**. §5.5 re-mesuré (**67/79** non filtrés, **12/79** filtrés). Correction incidente d'un compteur latent (workflow `ci_coverage_registry` filtré `paths:`, non déclenché depuis) : Contrats `.md` 290→**291**. Aucun runtime, aucune entité, aucun `entity_id` modifié. |
| 2026-07-04 | `main` @ `62d7a2b` (+ ce lot) | **C14 Lot 1E-b — branchement CI informatif du scanner sécurité publication.** Ajout du workflow `security_publication_audit.yml` (exécute `scripts/security/audit_publication_git.py --fail-on critical` en `continue-on-error`, **non bloquant**). Compteur §3 workflows total 81→**82** ; nouvelle catégorie « scanner sécurité publication (informatif, sans `check_*.py`) » = **1** ; réconciliation 82 = 76 `contracts_*` + 2 hors préfixe + 3 orchestrateurs + 1 scanner informatif. Couplage checker↔workflow-à-checker **inchangé** (1:1, 78↔78 — le scanner n'invoque pas de `check_*.py`). Aucune modification de la logique du scanner ni d'un contrat métier ; `CRITICAL=0` local confirmé avant branchement. |
| 2026-07-09 | `main` @ `1e95965` (+ ce lot) | **Garde-fou squelette Lovelace — aucun Jinja inline dans un dashboard** (suite extraction des cartes à logique inline vers includes, PR #319/#320). Ajout de `check_lovelace_no_inline_templating_contracts.py` + `contracts_lovelace_no_inline_templating.yml` (bloquant, filtré `paths:`) : contrôle textuel déterministe interdisant tout délimiteur Jinja (`{%` / `{{`) sous `18_lovelace/dashboards/**` ; l'arbre `includes/**` est **hors périmètre** (logique légitime). Base = 0 garantie par #319/#320. Compteurs §3 : checkers 79→**80**, workflows 83→**84**, `contracts_*` 77→**78**, workflows à checker 79→**80** ; couplage 1:1 (80↔80). Matrice §4 : UI / Lovelace 5→**6**, Total 79→**80**. §5.5 re-mesuré (**67/80** non filtrés, **13/80** filtrés — +`lovelace_no_inline_templating`). Aucun runtime, aucune entité, aucun `entity_id` modifié. |
| 2026-07-13 | `main` @ `0679de5` (+ ce lot) | **C14 §8.2 — hygiène de nommage : renommage du workflow ventilation climatisation.** `clim_ventilation_contracts.yml` → `contracts_climatisation_ventilation.yml` (alignement sur la convention de préfixe `contracts_*`), self-références `paths:` mises à jour. **Aucun ajout/retrait de workflow** : total workflows inchangé (**86**). Le workflow bascule de « hors préfixe » vers « `contracts_*` » : `contracts_*` 80→**81**, workflows à checker hors préfixe 2→**1** (`validation.yml` seul subsiste), workflows à checker total inchangé (**81**), couplage 1:1 inchangé (82↔82). §5.5 : le libellé filtré `clim_ventilation_contracts` devient `climatisation_ventilation` (toujours filtré `paths:`, 13/80 inchangé). Confronté par `check_ci_coverage_registry.py` (vert). Aucun runtime, checker, contrat, entité ni `entity_id` modifié — renommage de fichier CI seul. |
| 2026-07-11 | `main` @ `338e294` (+ ce lot) | **Contrat de production pluie (C17 lot 1).** Ajout du contrat normatif `contrats/meteo/pluie_production.md` (production/qualification des signaux de précipitation ; frontière de `volets_pluie.md` §5.2, jusqu'ici orpheline) + `check_pluie_production_contracts.py` + `contracts_pluie_production.yml` (bloquant, non filtré) : 8 tests (qualifications canoniques, source quantitative exclusive « pluie forte » INV-PROD-3, disponibilité honnête de l'évidence INV-PROD-6, non-repli à 0 de la prévue INV-PROD-7, **verrou anti-régression C16** INV-PROD-5). Compteurs §3 : contrats 291→**292**, checkers 81→**82**, workflows 85→**86**, `contracts_*` 79→**80** ; couplage 1:1 (82↔82). Matrice §4 : +1 famille « Précipitations — production / qualification », Total 81→**82**. §5.5 non re-mesuré (base antérieure, non bloquant). Aucun runtime, aucune entité, aucun `entity_id` modifié. |
| 2026-07-15 | `main` @ `6dd88b7` (+ ce lot) | **Anti-dérive étendue au §2 — fermeture de la boucle (suite C14 Lot 1D).** Angle mort découvert en réconciliant C14 : le tableau **§2 « Les trois couches »**, dont la colonne s'intitule pourtant « Volume (cf. §3) », **contredisait le §3 qu'il déclare comme source** — 291 contrats / 80 checkers / 84 workflows contre **293 / 82 / 87** réels. `check_ci_coverage_registry.py` passait **avant comme après** la correction : la garde du Lot 1D ne couvrait que le §3. La mesure de couverture mentait dans le document même qui mesure la couverture. **Correctif** : volumes §2 confrontés au comptage réel (**COUNT-2** ligne/section absente · **COUNT-3** dérive), §2 ≡ §3 ≡ réel par construction. **Bornage assumé** : garde ancrée sur les 3 lignes du §2, **jamais** sur tout le document — le **§8 (journal) est une trace historique datée** dont les nombres sont périmés par nature ; un selftest dédié (4e) prouve que le journal n'est pas mordu. **Deux trous adjacents fermés** : (1) le `paths:` du workflow ne couvrait ni `contrats/**` ni `03_doctrines/**` — ajouter un contrat faisait dériver `contrats_md` **sans déclencher la garde**, la dérive tombant plus tard sur une PR sans rapport ; (2) le `--selftest` existait mais **n'était exécuté nulle part** — il tourne désormais **avant** le contrôle réel (« on ne juge pas avec un juge défectueux », contrainte de méthode C14 §6). **Preuve négative** : le checker étendu, lancé sur le §2 tel qu'il était sur `main`, lève les 3 dérives (exit 1). Compteurs inchangés (82 / 87 / 81) — aucun ajout/retrait de checker ou de workflow. Aucun runtime, contrat métier ni entité modifié. |
| 2026-07-16 | `main` (+ ce lot) | **Couleurs UI — 3e étage : contrôle SÉMANTIQUE (invariant I1, garde indisponibilité).** Ajout de `check_ui_semantic_colors_contracts.py` + `contracts_ui_semantic_colors.yml` (bloquant, filtré `paths:` sur `18_lovelace/**` + `19_button_card_templates/**`). Comble le trou laissé par le contrôle **lexical** (`ui_runtime_colors`) : une carte peignant un état `unavailable`/`unknown` avec le gris **neutre** `rgba(158,158,158,0.2)` — valeur de palette parfaitement valide — passe le lexical (bon vocabulaire) tout en violant **R6** de `ui/couleurs/05_regles.md` et la distinction gris indispo `0.1` ≠ gris neutre `0.2` de `02_palette.md` (bon vocabulaire, mauvais sens). L'invariant I1 exige que tout bloc `background-color` résolvant un état **littéral** `'unavailable'`/`'unknown'` retourne le gris indisponibilité `rgba(158,158,158,0.1)`. **Périmètre borné, inattaquable face à la charte, zéro faux positif** : `background-color` uniquement (le texte relève de la typographie 04_typographie.md) ; déclencheur sur le **littéral d'état nommé** — le bare `isNaN` est **délibérément hors périmètre**, la charte autorisant une valeur non numérique à virer au ROUGE (« donnée invalide critique », `05_regles.md`) ou au gris neutre `0.2` (exceptions 2 thermique / 8 humidex, hors scan dans `12_template_sensors`) ; opt-out tracé `# arsenal:semantic-colors:ack`. **Garde-fou d'un invariant déjà vrai** (idiome `presence`/`pluie`) : **65/65** blocs `background-color` résolvant `unavailable`/`unknown` sont déjà conformes — **aucune modification runtime**, aucune entité ni `entity_id` touché. `--selftest` (8 cas : conforme / gris neutre = violation / `unknown` masqué / normalisation `0.10` / hors périmètre / `isNaN` seul hors périmètre / opt-out / texte `color` ignoré) exécuté **avant** le contrôle réel ; preuve négative : littéral `unavailable`→gris `0.2` lève exit 1. Compteurs §3 : checkers 82→**83**, workflows 87→**88**, `contracts_*` 81→**82**, workflows à checker 82→**83** ; couplage 1:1 (83↔83). §2 aligné (83 checkers · 88 workflows). Matrice §4 : UI / Lovelace 6→**7**, Total 82→**83**. §5.5 **non re-mesuré** (base antérieure, non bloquant ; le workflow s'ajoute aux filtrés `paths:`). *(Note de conception : une 1re version déclenchait aussi sur `isNaN` et « corrigeait » `carte_compteur_alerte.yaml` 0.2→0.1 ; après lecture intégrale de la charte — `isNaN` non équivalent à `unavailable` per exceptions 2/8 + « invalide critique → rouge » — la gâchette a été resserrée sur le littéral et la carte laissée intacte.)* |
| 2026-07-16 | `main` (+ ce lot) | **Couleurs UI — hiérarchie sémantique (invariant I2, R1–R3 anti-masquage).** Ajout de `check_ui_semantic_colors_hierarchy_contracts.py` + `contracts_ui_semantic_colors_hierarchy.yml` (bloquant, filtré `paths:` sur `18_lovelace/**` + `19_button_card_templates/**`). Contrôle l'ORDRE des couleurs dans les cascades de MASQUAGE `background-color` : `05_regles.md` R1 (le rouge prime), R2 (l'orange ne masque jamais un rouge), R3 (le jaune ne masque ni orange ni rouge). **Périmètre volontairement ÉTROIT, zéro faux positif** (étude de corpus préalable : ~80 % des blocs multi-couleurs sont des paliers à bandes exclusives, des mappings d'état discrets — Exception R7 — ou du backend `p[key]`, où l'ordre ne masque rien ; une règle d'ordre naïve y produirait des faux positifs massifs). Déclencheur = cascade de masquage identifiée par un discriminant robuste : comparaison numérique présente, **non** catégoriel (`=== '…'`/`.includes`/`p[…]`), **non** thermique (bleu froid), et **vert en position de fallback** (R4 : le vert n'apparaît qu'en l'absence d'anomalie). Contrôle : les `return` colorés précédant le vert doivent être de sévérité **non croissante** (le plus sévère d'abord) ; une inversion (ex. orange avant rouge) lève exit 1. Machines `state:` de button-card **hors périmètre** (parsing éclaté ; faux négatif assumé, jamais FP) ; opt-out tracé `# arsenal:semantic-colors:ack`. **Garde-fou d'un invariant déjà vrai** (idiome `presence`/`pluie`) : **6/6** cascades de masquage réellement contrôlées (famille « seuils » : `carte_capteur_seuils`, `carte_compteur_seuils_variables`, `carte_co2_seuils_variables`, `carte_nas_temperature_disque`, `carte_batterie_seuils_variables`, `carte_autonomie_seuils_variables`) déjà conformes ; **124 blocs correctement exclus** ; **aucune modification runtime**. `--selftest` (9 cas : masquage conforme `>` et `<` / inversion orange<rouge = violation / bande vert-premier exclue / catégoriel exclu / thermique exclu / backend map exclu / opt-out / mono-sévère / texte `color` ignoré) exécuté **avant** le contrôle réel ; preuve négative : inversion injectée `orange puis rouge, vert fallback` lève exit 1. Compteurs §3 : checkers 83→**84**, workflows 88→**89**, `contracts_*` 82→**83**, workflows à checker 83→**84** ; couplage 1:1 (84↔84). §2 aligné (84 checkers · 89 workflows). Matrice §4 : UI / Lovelace 7→**8**, Total 83→**84**. §5.5 **non re-mesuré** (base antérieure, non bloquant ; le workflow s'ajoute aux filtrés `paths:`). Aucune entité ni `entity_id` modifié. |
| 2026-07-19 | `main` (+ ce lot) | **C14 §6 — CI structure `06_input_selects`.** Ajout de `check_06_input_selects_contracts.py` + `contracts_06_input_selects.yml` (bloquant, non filtré) : validation **STRUCTURELLE** du patron name/options de `00_structure_includes/06_input_selects.md` sur les 17 fichiers (18 helpers) — ISS-000 racine · ISS-001 helper non-mapping · ISS-002 `options` load-bearing · ISS-003 doublon · ISS-004 `initial` ∈ options · ISS-005 slug object_id · ISS-006 `name` · ISS-007 include. Ferme l'extension explicitement différée par PID (« validation générique des autres input_select »). Proportionnalité D-NE-8 : seuls des invariants **universels falsifiables** ; interdits sémantiques (prose) hors périmètre. **Garde-fou d'un invariant déjà vrai** : **18/18** helpers conformes — aucune modification runtime, aucune entité ni `entity_id`. `--selftest` (24 cas) exécuté **avant** le contrôle réel ; exit 0 sur le corpus. Compteurs §3 : checkers 84→**85**, workflows 89→**90**, `contracts_*` 83→**84**, workflows à checker 84→**85** ; couplage 1:1 (85↔85). §2 aligné (85 checkers · 90 workflows). Matrice §4 : Helpers & socle 10→**11**, Total 84→**85**. §5.5 **non re-mesuré** (base antérieure, non bloquant ; workflow non filtré). Aucune entité ni `entity_id` modifié. |
| 2026-07-22 | `main` (+ ce lot) | **C35 L7.0 — CI de comportement sur l'initialisation des paramètres VMC.** Ajout de `check_vmc_initialisation_contracts.py` + `contracts_vmc_initialisation.yml` (bloquant, non filtré). **Nature inhabituelle, à souligner** : le contrôle n'est ni lexical ni structurel mais **comportemental** — le prédicat Jinja est **extrait du YAML contrôlé** puis **évalué** avec un `states()` simulé contre les scénarios de la sémantique retenue : `unavailable` **jamais retenu** · `unknown` persistant **retenu**, cas normal d'un helper neuf sans état à restaurer · numérique égal, différent ou hors plage d'amorce **conservé** · **épisode `unavailable` puis restauration numérique joué comme une séquence aux trois instants** · contrôle croisé · **contrôle d'amorçabilité symétrique** — un déploiement neuf, dix helpers à `unknown`, doit tous les amorcer, la garde ne devant pas aller trop loin. **Aucune logique n'est reproduite dans le test** : une copie du prédicat le rendrait aveugle à toute dérive du fichier contrôlé — c'est précisément le mode de défaillance que ce checker doit exclure. **Motivation** : les helpers de paramètres VMC n'ont pas de clé `initial:` et leur sortie de l'état sans valeur est assurée par une automatisation, aucun repli numérique n'étant admis depuis la passe 5 de C35. Le risque n'est donc pas l'absence d'initialisation mais **l'initialisation de trop** : réinitialiser un helper après une simple indisponibilité **écraserait un réglage utilisateur sans que rien ne le signale**. Règles : VINIT-000 extraction · VINIT-001 les cinq scénarios · VINIT-002 attente bornée (`timeout` **et** `continue_on_timeout`, `for:` sur le déclencheur d'état) · VINIT-003 aucun repli numérique silencieux · VINIT-004 écriture conditionnée · VINIT-005 table d'amorce complète et helpers tous surveillés. `--selftest` à **trois mutants** : un prédicat réinitialisant sur `unavailable`, un prédicat à repli numérique, et le prédicat réel confronté aux deux pièges — il ne doit retenir ni l'un ni l'autre. Compteurs §3 : checkers 85→**86**, workflows 90→**91**, `contracts_*` 84→**85**, workflows à checker 85→**86** ; couplage 1:1 (86↔86). §2 aligné (86 checkers · 91 workflows). Matrice §4 : nouvelle famille **VMC — initialisation des paramètres** (1), Total 85→**86**. §5.5 **non re-mesuré** (base antérieure, non bloquant ; workflow non filtré). Aucune entité ni `entity_id` modifié par l'ajout du checker. |
| 2026-07-29 | `main` (+ ce lot) | **CH-LL-CI-2 lot G1 — CI bloquante de complétude structurelle des grilles Lovelace (`R-LL-GRID-1`).** Ajout de `check_lovelace_grid_contracts.py` + `contracts_lovelace_grid.yml` (bloquant, filtré `paths:` sur `18_lovelace/**`, le checker, le workflow **et** `00_documentation_arsenal/ui/pattern_dashboard.md` — le document propriétaire de la norme réexécute le contrôle). Norme mergée en amont (PR #629 ; chantier ouvert PR #630). Classification à **trois états explicites** : STATIQUE (4 invariants — `columns` entier strictement positif, `cards` liste non vide, cellules directes divisibles par `columns`) · DYNAMIQUE (`type: conditional` ou `visibility:` enfant direct — recensée, comptée à part, **hors G1**, jamais annoncée conforme à G2) · NON ANALYSABLE (échec explicite, **aucun fallback silencieux** ; interdit de classer statique par défaut). Un `!include` direct compte pour **une** cellule **seulement si** sa cible résout vers une carte-racine mapping (arité prouvée par résolution relative au fichier source, **sans expansion récursive** ; cible absente/illisible/liste/scalaire → échec explicite). **Garde-fou d'un invariant déjà vrai** : scan réel **273 grilles · 270 statiques · 3 dynamiques · 270/270 statiques conformes · 0 non analysable · 0 violation** → exit 0 ; les 3 grilles dynamiques (`dashboards/systeme/nas.yaml` ×2, `includes/cartes/chauffage/aeration_timers_chauffage.yaml`) **non modifiées**. `selftest()` (20 cas, fixtures jetables en `tempfile`) exécuté **avant** le scan réel ; preuve négative (2 col / 3 cellules → violation `grille statique incomplète`). Compteurs §3 : checkers 86→**87**, workflows 91→**92**, `contracts_*` 85→**86**, workflows à checker 86→**87** ; couplage `contracts_*` 1:1 (85↔85)→**(86↔86)**. §2 aligné (87 checkers · 92 workflows). Matrice §4 : UI / Lovelace 8→**9**, Total 86→**87**. §5.5 **non re-mesuré** (base antérieure, non bloquant ; le workflow s'ajoute aux filtrés `paths:`). **R-LL-GRID-2 (géométrie dynamique) non implémenté dans ce lot ; qualification du NAS maison non entamée.** Aucun dashboard, include ni fichier NAS modifié. |
| 2026-08-06 | `main` (+ ce lot) | **Consolidation CI — ~86 workflows `contracts_*.yml` → 1 matrice `contracts_all.yml` (7 domaines).** Motivation : chaque checker Arsenal est un scan de ~1 s, mais chaque workflow payait ~25-30 s d'overhead de VM ; ~86 VM jetables par événement (× haute fréquence, 40 000 runs cumulés) ont fini par **déclencher le throttling Actions de GitHub** (Actions désactivé côté dépôt le 2026-08-06). **Correctif d'infra** : les 86 checkers de contrat sont regroupés dans `contracts_all.yml` — matrice `ecs` / `clim_chauffage` / `air_humidite` / `meteo_jardin` / `ui_lovelace` / `presence_maison` / `helpers_infra` — chaque job lançant ses checkers en **sous-processus isolés** via `scripts/ci/run_checkers.py` (selftest auto-détecté d'après la source ; **tous** lancés ; **exit 1 si un seul échoue** ; `pip install pyyaml jinja2` = union des dépendances ; env `RESILIENCE_CHECK_MODE`/`STRICT_ON_NEW`). `check_configuration_includes.py` reste porté par `validation.yml`. **Aucun checker modifié** ; **aucune perte de couverture** — la complétude reste prouvée par INTEG-1 de `check_ci_coverage_registry.py` (inchangé) : les 86 noms figurent tous dans `contracts_all.yml`, 0 orphelin, 0 référence morte. Jobs/événement : ~152 → ~15 ; VM/événement : ~86 → 7 pour les contrats. Compteurs §3 : workflows total 92→**7**, `contracts_*` 86→**1** ; checkers **inchangés** (87). §2 aligné (7 workflows). §5.5 : filtres `paths:` hétérogènes supprimés (déclenchement homogène). **Compromis assumé** : granularité de l'UI PR (7 coches par domaine au lieu de 86 nommées) ; le « quel checker » d'un échec vient du log agrégé (résumé PASS/FAIL par checker). Aucun runtime, contrat métier ni entité modifié — changement d'outillage CI seul. |
| 2026-08-09 | `main` (+ ce lot) | **Audit CI — anti-double-run, concurrence, job unique, filtres `paths:` ciblés (P1/P2/P5/P3).** Suite de la consolidation du 2026-08-06. **P1 (anti-double-run)** : les 4 workflows globaux (`contracts_all`, `validation`, `doctrine`, `docs`) déclaraient `on: push:` sans filtre → chaque commit sur une branche de PR relançait toute la suite **deux fois** (événements `push` **et** `pull_request`), et le `push` nu se déclenchait aussi sur les **tags** ; `push` est désormais limité à `branches: [main]` (re-validation post-merge), les PR restant couvertes par `pull_request` — c'était déjà le modèle de `security_publication_audit.yml`. **P2 (concurrence)** : bloc `concurrency` `(workflow, ref)` avec `cancel-in-progress` conditionné à `github.ref != refs/heads/main` — les pushes obsolètes d'une branche sont annulés, une validation de `main` n'est **jamais** coupée. **P5 (job unique)** : la matrice `contracts_all` (7 jobs par domaine) est fusionnée en **un seul job** exécutant les 86 checkers via `run_checkers.py` (les 86 noms restent listés explicitement → **INTEG-1 vert** ; parité de comportement inchangée : sous-processus isolés, selftest auto-détecté, exit 1 si un seul échoue) — minimise le nombre de VM démarrées (arbitrage assumé **ressources > latence**). **P3 (filtres `paths:` ciblés)** : `paths:` **réintroduits** sur `validation` (`**.yaml`/`**.yml`), `doctrine` (`12_template_sensors/**` + `11_automations/**`) et `docs` (`00_documentation_arsenal/**` + `scripts/docs_lint/**`) — périmètres de lecture des checkers **vérifiés dans leur source**, aucune perte de couverture ; **`contracts_all` reste sans filtre** (son checker de couverture compte les `.md`, il doit rester toujours actif). `main` non protégée (aucun check requis) → un run skippé par `paths:` ne bloque aucun merge, pas de sentinelle. **Compteurs §3 inchangés** (87 checkers · 7 workflows · 1 `contracts_*` — aucun ajout/retrait de fichier) ; `check_ci_coverage_registry.py` (INTEG-1/2 + selftest) reste **vert** et les **86 checkers passent en un seul appel** `run_checkers.py`. VM/événement pour les contrats : 7 → **1** ; double exécution `push`+`pull_request` **supprimée**. PR #675 (P1/P2, mergée) et #676 (P3/P5, mergée). Aucun runtime, contrat métier ni entité modifié — changement d'outillage CI seul. |
| 2026-07-29 | `main` (+ ce lot) | **CH-LL-CI-2 lot G2 — géométrie dynamique bloquante (`R-LL-GRID-2`), par EXTENSION du checker.** Extension de `check_lovelace_grid_contracts.py` (arbitrage : extension, **pas de nouveau checker ni workflow**) : les grilles DYNAMIQUES, jusque-là recensées hors G1, sont désormais **évaluées et bloquantes**. Motifs statiquement démontrables reconnus : **A** — `columns == 1` (toute cardinalité divisible) ; **B** — cellules fixes + paires de conditionnels **complémentaires sur une même entité** (`state: V` / `state_not: V`, parser strict couvrant les formes courte et explicite) contribuant exactement 1, cardinalité visible **constante** divisible par `columns`. Tout autre cas (conditions indépendantes, `visibility:` directe, forme non reconnue, tag non résolu, paire non divisible) → **non conforme par défaut**, aucune dérogation ; un succès G1 ne prouve jamais G2. **Garde-fou d'un invariant déjà vrai** : scan réel **3/3 grilles dynamiques conformes** (`dashboards/systeme/nas.yaml` ×2 motif A après #633 ; `includes/cartes/chauffage/aeration_timers_chauffage.yaml` motif B) → exit 0. `selftest()` porté à **24 cas** (ajout motifs A/B, conditions indépendantes, paire non divisible, `visibility:`), exécuté **avant** le scan réel ; preuve négative (conditions indépendantes → violation). **Compteurs §3 inchangés** (même checker, même workflow `contracts_lovelace_grid.yml` : 87 checkers · 92 workflows) ; `check_ci_coverage_registry.py` reste vert. Matrice §4 : ligne `lovelace_grid` complétée (R-LL-GRID-2 désormais bloquant). Aucun dashboard, include ni fichier NAS modifié. |
