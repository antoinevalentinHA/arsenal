# Registre des chantiers Arsenal — Cockpit de pilotage

> **Source canonique de « qu'est-ce qui est réellement ouvert aujourd'hui ? ».**
> **Index d'état uniquement.** Ce registre ne contient **aucune analyse** : chaque ligne **pointe** vers le document source faisant foi, qui détient le détail. Il ne recopie ni les options, ni les registres de risques, ni les `DETTE-*` des contrats.
> **Séparation stricte :** [`index.md`](index.md) = **navigation** (où trouver un document) ; ce registre = **statut** (ce qui attend une action). Aucun marqueur d'état ne doit diverger entre les deux — en cas de conflit, **le document source prime**.

## Règles de gouvernance

1. **Co-commit obligatoire.** Tout changement d'état d'un chantier (ouverture, patch runtime, clôture, requalification) met à jour ce registre **dans le même commit** — même discipline que le changelog.
2. **La source prime.** En cas de divergence registre ↔ document source, la source fait foi et le registre est corrigé. Le registre **ne crée aucune doctrine**.
3. **Boucle CI fermée.** Le contrôle [`check_registre_chantiers.py`](../../scripts/arsenal_contracts/check_registre_chantiers.py) vérifie que chaque lien de ce registre pointe vers un fichier existant (non bloquant).

## Cycle de vie

`Actif` → (`Parqué` | `À arbitrer/dormant` | `Backlog`) ↔ `Actif` (promotion) → `Clos récent` (trace 90 j) → retiré.
Un **item de backlog** n'entre en *Actifs* qu'une fois **promu** (décision d'agir). Une **vérification** ou un **dossier d'arbitrage** n'entre jamais en *Actifs* tant qu'aucune action n'est définie.

---

## ① Actifs — *le cockpit (action ou décision requise)*

| ID | Chantier | Domaine | Prio | Statut | Action attendue | Source(s) faisant foi |
|----|----------|---------|------|--------|-----------------|------------------------|
| **C1** | Clôture définitive de l'alarme | alarme | **P0-cond → P1** | clôture conditionnelle acquise | Exécuter le **test positif `S3`** (établit la garantie `ALM-CRIT-2`), puis réaligner CH-5 (contrats 50/51) | [`05_clotures/alarme/cloture_ch1_alarme.md`](05_clotures/alarme/cloture_ch1_alarme.md) · [`04_chantiers/alarme/etat_post_CH6.md`](04_chantiers/alarme/etat_post_CH6.md) |
| **C3** | Clôture du domaine Vacances | vacances / ECS | **P1** | VAC-IMP-5 : validation runtime en attente | Rejouer les 4 scénarios runtime (`S-COMPLETION-NATURELLE`…), puis clôturer le domaine | [`04_chantiers/vacances/chantier_vac_imp_5_desinfection_retour.md`](04_chantiers/vacances/chantier_vac_imp_5_desinfection_retour.md) |
| **C10** | Arrosage — V1 automatique & découvrabilité du domaine | arrosage | P2 | V1 runtime **mergée** (#125) + **durcissements** (#126→#147 : UI, observabilité, fraîcheur, coexistence fail-safe, intention explicable) ; observation v0 du canal réservoir sol. Alignements sur contrats existants, **sans nouvelle doctrine**. **Publié** en releases **v16.2** (observation v0) et **v16.3** (V1 + durcissements). **Domaine fonctionnellement complet côté décision/action/UI/diagnostic, en quasi-clôture** (mise à jour 2026-07-07 — le plan d'action vivant §3 le constate) : **UI opérateur LIVRÉE** (verdict + raison groupés, `arrosage/principal.yaml`) ; **diagnostic pont RÉSOLU** (faux manque — couvert côté Système, `dashboards/systeme/rain_bird.yaml`, volontairement hors cockpit arrosage) ; **notifications COUVERTES** (pont indisponible, refus/non-confirmation de démarrage, stop impossible/non confirmé — seule une notif « nominale » non actionnable reste un micro-arbitrage non bloquant) ; **validations terrain** : chaîne manuelle supervisée validée (répétable), **décision automatique V1 validée end-to-end une première fois** (2026-07-05, 05:30→05:52, sans forçage ni simulation) — reste le **comportement sur plusieurs cycles/saisons**, suivi **opportuniste** non bloquant. **Observation terrain 2026-07-19 (lecture Historique recorder, ~5→19/07)** : ~10 sessions d'arrosage, **toutes verdict `close_nominale`** (0 `stop_incertain`/`watchdog`/`reprise`) ⇒ **orchestration de session Lot D confirmée nominale sur plusieurs cycles** ; déclenchements **mixtes manuel/auto** (source non historisée) — la **décision automatique** reste, elle, étayée par **trois occurrences** (05/07, 18/07, 20/07 — les deux dernières qualifiées **automatiques par arbitrage propriétaire du 2026-07-20** ; verdicts `close_nominale`, écart fin prévue/observée **2-3 s**) : la réserve « pas encore multi-cycle » **tombe**. **Aucune dette bloquante connue.** | Poursuivre le **suivi terrain opportuniste** (multi-cycles) via le plan d'action vivant jusqu'à confirmation sur la durée ; aucun lot d'implémentation restant identifié pour la complétude fonctionnelle. Chaque incrément **publié dans le changelog de sa release** (co-commit). | [`03_plans_action/arrosage/plan_action_arrosage.md`](03_plans_action/arrosage/plan_action_arrosage.md) · [`../changelog/changelogs/v16/v16_3_0.md`](../changelog/changelogs/v16/v16_3_0.md) · [`../contrats/arrosage/README.md`](../contrats/arrosage/README.md) |
| **C13** | Notification d'échec d'exécution persistant clim (D5) | climatisation | **P1** | **promu par arbitrage propriétaire (2026-07-03)** après occurrence réelle silencieuse ([`diagnostic_clim_execution_echec.md`](01_rapports/climatisation/diagnostic_clim_execution_echec.md)) ; **runtime livré (2026-07-03)** : automation `10030000000121` (**ID attribué par décision propriétaire 2026-07-03** — réattribution assumée de l'ex-ID retiré de `synchronisation_etat.yaml`, cf. `12_ventilation_intention.md` §13 ; dérogation décidée/documentée/localisée au sens de `id_automatisations.md`) (`notification_echec_execution.yaml` — création sur échec latché + budget épuisé `counter > 2`, maintien pendant réarmement, dismiss sur disparition de l'échec, reconstruction boot) ; contrat `08_execution.md` aligné (§ « Notification d'échec persistant ») ; garde CI D5 ajoutée au checker admissibilité, **détection prouvée par mutation-testing (8 mutations)** ; **validation terrain en attente** (prochain échec persistant réel ou test provoqué) | Valider en conditions réelles (notification visible sur échec persistant, retrait au réarmement réussi), puis clore C13 et solder D5 au backlog | [`03_plans_action/climatisation/plan_action_post_revalidation_climatisation.md`](03_plans_action/climatisation/plan_action_post_revalidation_climatisation.md) (§2) · [`04_chantiers/climatisation/backlog_climatisation_hysteresis.md`](04_chantiers/climatisation/backlog_climatisation_hysteresis.md) |
| **C14** | Couverture CI de la norme contractuelle (écart Markdown ↔ CI) | transverse | **P1** | Cadrage livré (lecture seule). **Lot 1 (A–E) soldé** : ancrages checkers (1A) ; `doctrine.yml` arbitré + durci (1B) ; validation des includes / chargement HA (1C, impl. A/B) ; anti-dérive du registre de couverture `check_ci_coverage_registry` (1D, bloquant) ; sécurité publication git — scanner `audit_publication_git.py` **bloquant** en CI depuis l'**incident P0 du 2026-07-05** (secrets Zigbee2MQTT publiés, #282), correctif encodage `--history` (1E-f, 2026-07-09). **Lot 2 : PID livré (2026-07-09)** — `check_prefix_id_contracts.py` + workflow bloquant : intégrité de la source de vérité des préfixes `prefix_id.yaml` (format « NNNN - domaine », unicité préfixe/domaine). Compteurs de couverture à jour (82 checkers / **87** workflows / 81 `contracts_*` — recomptés le 2026-07-15 ; « 86 » était une coquille), anti-dérive vert. **Détail lot-par-lot dans les dossiers sources liés — non recopié ici (charte §91, anti backlog déguisé).** **Réconciliation documentaire du 2026-07-15** : les tables §3.1/§5/§6/§8.1 du chantier décrivaient encore la base `f583efe` et présentaient comme ouverts des items soldés (biais agent §1.1) — chaque ligne concernée porte désormais un marqueur ✅/⚠️ daté, sans réécriture de l'analyse. **Angle mort découvert puis FERMÉ (2026-07-15)** : l'anti-dérive 1D ne gardait que le §3 du registre de couverture, pas son tableau §2 (qui affichait 291/80/84 contre 293/82/87 réels — le checker passait avant comme après la correction). Garde **étendue** : volumes §2 confrontés au réel (COUNT-2/3, ancrage sur les 3 libellés de lignes, **bornage à la section** — le §8 journal est une trace historique datée, jamais confrontée), **`--selftest` branché en CI** (il existait sans être exécuté), **`paths:` réparé** (`contrats/**` + `03_doctrines/**` manquaient : ajouter un contrat faisait dériver un compteur gardé **sans déclencher la garde**). 5 mutations tuées ; preuve négative sur le §2 réel de `main`. **Dette assumée consignée** : les lignes **dérivées** du §3 (« workflows à checker », « orchestrateurs », « scanner », « couplage »), sa **prose** et les matrices §4/§5 restent **hors garde** — 3 énoncés y étaient faux (81 vs 82 à checker ; scanner dit « non bloquant » alors qu'il **bloque** depuis l'incident P0 ; prose « 85 » vs 87), corrigés à la main. Les garder exige de **définir en code « orchestrateur » / « workflow à checker »** = arbitrage propriétaire non tranché. | 1C-c ; reste des lots 2–3 ; ~~CI structure `06_input_selects`~~ **✅ soldé (2026-07-19)** — `check_06_input_selects_contracts.py` + `contracts_06_input_selects.yml` bloquant | [`04_chantiers/transverses/chantier_couverture_ci_contrats_arsenal.md`](04_chantiers/transverses/chantier_couverture_ci_contrats_arsenal.md) · [`04_chantiers/transverses/c14_lot1a_ancrages_checkers.md`](04_chantiers/transverses/c14_lot1a_ancrages_checkers.md) · [`04_chantiers/transverses/c14_lot1b_qualification_doctrine_yml.md`](04_chantiers/transverses/c14_lot1b_qualification_doctrine_yml.md) · [`04_chantiers/transverses/c14_lot1b_implementation_doctrine_yml.md`](04_chantiers/transverses/c14_lot1b_implementation_doctrine_yml.md) · [`04_chantiers/transverses/c14_lot1c_validation_chargement_ha.md`](04_chantiers/transverses/c14_lot1c_validation_chargement_ha.md) · [`04_chantiers/transverses/c14_lot1c_implementation_validation_includes.md`](04_chantiers/transverses/c14_lot1c_implementation_validation_includes.md) · [`04_chantiers/transverses/c14_lot1d_antiderive_registre_couverture_ci.md`](04_chantiers/transverses/c14_lot1d_antiderive_registre_couverture_ci.md) · [`04_chantiers/transverses/c14_lot1e_frontiere_git_securite_publication.md`](04_chantiers/transverses/c14_lot1e_frontiere_git_securite_publication.md) · [`04_chantiers/transverses/c14_lot1e_c_preparation_scanner_publication.md`](04_chantiers/transverses/c14_lot1e_c_preparation_scanner_publication.md) · [`04_chantiers/transverses/c14_lot1e_d_desindexation_log_anonymisation_ip.md`](04_chantiers/transverses/c14_lot1e_d_desindexation_log_anonymisation_ip.md) · [`04_chantiers/transverses/c14_lot1e_b_branchement_ci_informatif_scanner.md`](04_chantiers/transverses/c14_lot1e_b_branchement_ci_informatif_scanner.md) · [`01_rapports/transverses/incident_p0_zigbee2mqtt_secrets_publies.md`](01_rapports/transverses/incident_p0_zigbee2mqtt_secrets_publies.md) |
| **C16** | Précipitations — état collant `pluie_en_cours` + sortie d'épisode asymétrique | météo / précipitations | P2 | **runtime livré, validation terrain en attente.** Racine : la sortie ne traitait pas le régime « Netatmo absent » (`principes_generaux.md` §6) ⇒ état collant + fermeture prématurée. Correctif (Option B, arbitrage propriétaire) : évidence consolidée déclarative `binary_sensor.pluie_evidence_active` (Netatmo `>0` ∪ SNZB borné 10 min ∪ injection ; `delay_on 1 s` / `delay_off 5 min` ; disponibilité = ≥ 1 source réelle) **consommée par les DEUX écrivains** (`maj_sensor/on` + `off`, symétrie stricte, IDs inchangés) ; sortie nominale **événementielle**, `time_pattern` = **backstop dégradé strict** (Netatmo absent + épisode > `T_max` 3 h) ; réconciliation boot `systeme_stable` ; sémantique Netatmo unifiée `>0` (bande 0/0,1 résolue). Vérifié : matrice de rendu Jinja 14/14, `yamllint` + `check_recorder_contracts` verts. **Les corrections ne valent pas validation terrain.** | Valider en terrain : (a) Netatmo coupé pendant un épisode SNZB ⇒ l'épisode se ferme (plus de collage) ; (b) premières gouttes SNZB / Netatmo 0,0 ⇒ épisode ouvert ; puis clore C16. | [`01_rapports/meteo/audit_precipitations_domaine.md`](01_rapports/meteo/audit_precipitations_domaine.md) · [`01_rapports/meteo/audit_precipitations_chaine_decisionnelle.md`](01_rapports/meteo/audit_precipitations_chaine_decisionnelle.md) |
| **C19** | Aération — clôture d'épisode bloquée sur front fugitif (`aeration_episode_en_cours` collant) | aération | **P1** | **runtime + CI livrés (#359), validation terrain (reboot) en attente.** Racine : la clôture M2 ne dépendait que du front `fermeture_stable` off→on, **consommé sans effet** si une garde était transitoirement fausse (reboot `unknown→on` du template sensor, interrupteur maître off, système instable, pipeline non armé) ⇒ épisode collant **bloquant tout futur M1**, sans filet (sécurité démarrage / mini-guard / M0 gardent sur `episode=off`). Correctif (arbitrages propriétaire) : réconciliation M2 **sur état** dans le pipeline maître (5 triggers `reconciliation_*`, OR additif, **littéral `trigger.id == 'fermeture_stable'` conservé**, preuve exclusive `fermees_stable == 'on'`), **script M2 inchangé** ; capteur diagnostic dédié `binary_sensor.chauffage_aeration_cloture_en_retard` (`delay_on 60s`) **strictement hors chemin recover**. Contrats socle 02 (**INV-CLÔTURE** + état transitoire illégal), m2/1, socle 03, socle 07 alignés ; checker M2 durci (+4 tests, région de branche bornée) ; **oracle comportemental** `tools/arsenal_ci/behavior/m2_gate.py` (B1 reboot / B2 front consommé / B3 fenêtres ouvertes) ; **10 mutations négatives vérifiées** ; `pytest arsenal_ci` 146 passed. **Les corrections ne valent pas validation terrain.** | Valider en terrain : (a) reboot HA avec `aeration_episode_en_cours=on` + fenêtres fermées ⇒ clôture M2 (blocage activé) après stabilisation ; (b) front consommé simulé (interrupteur maître off à la fermeture, puis on) ⇒ clôture ; puis clore C19. | [`01_rapports/aeration/audit_cloture_episode_bloquee_front_fugitif.md`](01_rapports/aeration/audit_cloture_episode_bloquee_front_fugitif.md) · [`../changelog/changelogs/v16/v16_3_8.md`](../changelog/changelogs/v16/v16_3_8.md) |
| **C22** | Éclairage séjour — extinction sur luminosité réelle (angle mort présence + plein jour) | éclairage | P2 | **runtime livré (#368, mergé 2026-07-15), validation terrain en attente.** Le séjour n'éteignait que sur **absence** (deadline `10070000000015`) ⇒ lampe allumée en période sombre **restant allumée en plein jour tant qu'il y a présence**. Ajout d'un **second axe d'extinction sur luminosité réelle** : `sensor.luminosite_garage_illuminance` (**unique capteur**, proxy d'ambiance) → autorité `binary_sensor.sejour_extinction_luminosite_autorisee` (`seuil > 0 ET lux > seuil`) → automation `10070000000035` déléguant à `script.sejour_off` (rattrapage boot/reload). **Option « garder période + extinction lux » (arbitrage propriétaire)** : allumage inchangé (`periode_meteo`), **seuil unique, pas d'hystérésis** (allumage période-gaté + transition mouvement requise ⇒ pas de battement ; bandeau d'autorisation binaire préservé). Helper `input_number.sejour_seuil_luminosite_extinction_auto` **défaut 0 = désactivé** (sûr, non intrusif). Contrat `sejour.md` **v1.0.0→v1.1.0** (§6bis, §7) + test **T13**. Checkers verts en local (eclairage_sejour, 03_input_numbers, automation_ids, prefix_domain, ci_coverage_registry). **Instrumentation probatoire posée (2026-07-19)** : `sensor.luminosite_garage_illuminance` **n'était pas historisé** (absent de l'allowlist recorder — angle mort du §5 non anticipé) ⇒ **microscope Population B** ajouté (`recorder.yaml`, dérogation fréquence présomptive, cible LTS horaire, **retrait dès seuil calibré ET C22 clôturé**) ; calibration en attente de quelques cycles jour/nuit. **Les corrections ne valent pas validation terrain.** | **Instrumentation posée (2026-07-19)** ; laisser accumuler quelques cycles jour/nuit, **(a) qualité du proxy : ✅ soldée (2026-07-20)** — le capteur **suit la lumière extérieure** (signature solaire sur 26 h de LTS : décollage 06:35 / lever 06:36, retour au plancher 21:40 / coucher 21:41, palier nocturne plat à 1,0 lx ; **arbitrage propriétaire** : garage fenêtré, non éclairé par le séjour). Reste **(b) calibrer le seuil** — valeur de départ **50 lx** relevée mais **non figée** (un seul cycle, une seule météo ; le creux nuageux de midi à **33 lx** chevauche l'aube de 9 h à **42-56 lx**, et un seuil calé en été sera trop haut en hiver) : laisser accumuler quelques cycles de météo variée — et **(c) non-régression allumage** ; puis clore C22. Helper à `0` : aucun changement de comportement. | [`04_chantiers/eclairage/chantier_extinction_luminosite_sejour.md`](04_chantiers/eclairage/chantier_extinction_luminosite_sejour.md) · [`../contrats/eclairage/sejour.md`](../contrats/eclairage/sejour.md) |
| **C20** | Politique d'absence COOL — veto immédiat Vacances + durée d'absence longue réglable | climatisation | P2 | **Lots 1–4 mergés (#363/#364/#365/#366) ; Lot 5 (validation terrain) : protocole ouvert, aucune preuve recueillie (2026-07-14).** **Instrumentation probatoire posée le 2026-07-19** : 5 entités décisives du protocole n'étaient pas historisées alors que la trace §4 exige l'Historique — 11 des 12 scénarios n'étaient **pas exerçables**. Microscope Population B propre à C20 (`recorder.yaml`), **réévaluation 2026-11-16 (T0+120 j)**, retrait dès trace §4 complète et C20 clôturé. **S12 qualifié preuve L5 opérateur** (non établissable par l'Historique, non bloquant à ce titre). Aucune règle métier, aucun runtime décisionnel modifié. Action attendue = **dérouler la trace terrain** ([`protocole_validation_terrain_absence_cool.md`](04_chantiers/climatisation/protocole_validation_terrain_absence_cool.md), 12 scénarios) puis clôturer. **C20 non clôturable tant que la trace est vide.** Runtime : helper `clim_duree_absence_longue` (sans `initial:`), horodatage `clim_debut_absence` + automation unique `10030000000122`, extinction recomputée (timer `absence_longue_clim` **retiré**), composite `clim_veto_absence_vacances` consommé par `autorisation_clim_cool`, diagnostics étendus. Gate live : `test_lot_5_2` + workflow `arsenal-ci-climatisation.yml` (registre couverture 87). CI locale verte (arsenal_ci 169). Note de décision D1–D15 actée propriétaire ([`cadrage_decision_politique_absence_vacances_cool.md`](02_conception/climatisation/cadrage_decision_politique_absence_vacances_cool.md)). Corrige le **veto prématuré** (départ Vacances non pris en compte avant écoulement du timer) via **veto immédiat** `vacances_actives` (D1), et la **durée figée 8 h** via **helper réglable** (ID à attribuer, sans `initial:`) + **continuité physique** boot-proof (D4). **Vérité composite** `(absence_longue OR vacances) AND NOT preparation` sur l'**autorité unique** `binary_sensor.autorisation_clim_cool` (D6/D7) — **aucune décision centrale**. Dette `_reel`/garde = lot séparé (D14). **Contrat avant runtime.** C21 en dépend. | Rédiger le contrat (C20-L1), puis dérouler le plan par lots | [`04_chantiers/climatisation/chantier_politique_absence_cool.md`](04_chantiers/climatisation/chantier_politique_absence_cool.md) · [`03_plans_action/climatisation/plan_action_politique_absence_vacances_cool.md`](03_plans_action/climatisation/plan_action_politique_absence_vacances_cool.md) · [`02_conception/climatisation/cadrage_decision_politique_absence_vacances_cool.md`](02_conception/climatisation/cadrage_decision_politique_absence_vacances_cool.md) |
| **C23** | Restitution UI alarme — état `triggered` explicite + priorité d'indisponibilité sur la divergence | alarme | P2 | **Patch UI mergé (#392) — validation terrain différée.** Échelle de restitution `Indispo > Triggered > NOOP > Cohérence > Divergence` appliquée aux **2 cartes sœurs** (`carte_alarme_decision`, `carte_alarme_intention`) : branches **mutuellement exclusives** (preuve statique 25/25), gris indispo `rgba(158,158,158,0.1)` **prioritaire** (R6), `triggered` → « Alarme déclenchée » + `mdi:shield-alert`, `NOOP` gris neutre. Issu des 2 verdicts `PARTIEL` de l'audit d'exposition diagnostique (#390) : **ALM-DIAG-001** (triggered) / **ALM-DIAG-003** (indispo R6). **Non-mutualisation** (ni template partagé, ni capteur runtime, ni entité). **Runtime/décision/notifications/sûreté inchangés.** Checkers UI verts (`check_19_button_card_templates`, `check_ui_couleurs`, `check_ui_runtime_colors`). **Changelog non créé** (artefact de release, doctrine `redaction_changelog.md` §1). | Obtenir la **preuve terrain** `triggered`/indisponibilité (différée, **sans forçage** d'état réel consommable), puis clôturer | [`04_chantiers/alarme/chantier_restitution_triggered_indisponibilite_ui.md`](04_chantiers/alarme/chantier_restitution_triggered_indisponibilite_ui.md) · [`01_rapports/alarme/audit_exposition_diagnostics_alarme.md`](01_rapports/alarme/audit_exposition_diagnostics_alarme.md) |
| **C25** | Climatisation distante Audi — commande manuelle | voiture | P3 | ✅ **CLÔTURE CONDITIONNELLE ACQUISE (2026-07-21) — nominal validé end-to-end, réserve E6.** Feuille de route déroulée dans l'ordre (doc → cadrage/contrat A1 → runtime → UI → clôture conditionnelle : [`05_clotures/voiture/cloture_c25_commande_climatisation_distante.md`](05_clotures/voiture/cloture_c25_commande_climatisation_distante.md)). Constat d'audit central maintenu : l'appel `audiconnect.start_climate_control` **n'échoue pas** côté HA sur refus/erreur (exceptions absorbées, retour ignoré) ⇒ l'absence d'erreur ne prouve rien ; **la preuve de succès vient de sources indépendantes** (HA `climatisation_state`, myAudi, physique). **Reprise E1 (2026-07-21)** : **succès terminal confirmé** — `climatisation_state = cooling` (HA) + Climatiseur « Actif » (myAudi) + corroboration physique (SoC 64→41 %, autonomie 21→12 km / ~50 min), en **config minimale**. **Intermittence backend confirmée** (nouvel échec `fail_vehicle_timeout` à 09:48 le même matin avant réussite par répétition). **Cadrage contractuel LIVRÉ (2026-07-21)** — arbitrage propriétaire : franchissement **assumé et borné** de la frontière observationnelle, périmètre **minimal prouvé** ; décisions D1–D8 ([`02_conception/voiture/cadrage_commande_climatisation_distante.md`](02_conception/voiture/cadrage_commande_climatisation_distante.md)) + **contrat `voiture.md` amendé (A1)** : couche action **manuelle** sans décision automatique, `temp_c` + `climatisation_mode` (`comfort` prouvé, `economy` sous réserve E2), honnêteté terminale (succès sur transition `climatisation_state` seule, sinon `non confirmé`/`timeout`), pas de gate de commandabilité, ancrage véhicule portable. **Runtime LIVRÉ (2026-07-21)** : `script.audi_climatisation_distante` (single-shot honnête — appel + nudge `refresh_vehicle_data` + observation terminale bornée ~3 min de `sensor.audi_a3_sportback_e_tron_climatisation_state`) + état de commande `input_select.audi_climatisation_commande_etat` ; ciblage portable `device_id(nom)` ; booléens forcés `false` ; entité d'observation **confirmée live par l'opérateur**. **UI LIVRÉE puis SIMPLIFIÉE (2026-07-21)** : section « ❄️ Commande climatisation » **compacte** sur `dashboards/voiture/audi.yaml` (bouton d'action confirmé → script avec **défauts 21 °C/comfort** + restitution lecture seule de l'état) ; temp/mode **non exposés**, helpers de réglage **retirés** (dashboard principal ≠ dashboard de réglages) ; **aucune logique UI** (INV-CMD-7). **Outil d'observation temporaire** `script.audi_climatisation_observation` (trace `climatisation_state`+`remaining_climatisation_time` toutes les 5 s pendant ~2 min, **à retirer à la clôture**). **Chaîne de détection vérifiée dans le code** (source brute véhicule endpoint `climater`/MBB ; refresh post-action dans un `finally` ; `update_sleep` 5 s — cf. protocole §6). **Validation end-to-end NOMINALE (2026-07-21, ~15:54)** : bouton Arsenal → `input_select.audi_climatisation_commande_etat = Confirmée` (transition réelle observée). **Clôture conditionnelle acquise** ; **réserve unique = E6** (comportement en échec). **Restitution durcie (post-clôture, INV-CMD-8)** : vérité **auto-expirante** (`binary_sensor.audi_climatisation_active` = brut cloud ET avant échéance `input_datetime.audi_climatisation_fin_prevue`, capturée par `10150000000007`) + restitution `10150000000006` (notification active/dismiss + réconciliation `Confirmée → Au repos`). Corrige « état figé » **et** « clim active après fin naturelle » ; **cloud en confirmation** ⇒ robuste au retard **et au gel du climater** (403/404 → `support_climater=False`). Backstop « poller plus » (#501) écarté. **Reste (a) diagnostic `last_changed` (latence vs verrouillage climater) ; (b) E6.** | **Dérouler E6** (véhicule non branché : `climatisation_state` inchangé = discriminant fiable) via `script.audi_climatisation_observation`, consigner au §6, puis **clôture définitive** (lever la réserve, **retirer l'outil d'observation**, sortir C25 des Actifs). Nominal déjà validé ; essais E1b/E2–E5 opportunistes (E5 *composé* ; `economy`/E2 à confirmer avant offre pleine) | [`04_chantiers/voiture/chantier_commande_climatisation_distante.md`](04_chantiers/voiture/chantier_commande_climatisation_distante.md) · [`04_chantiers/voiture/protocole_caracterisation_terrain_climatisation_distante.md`](04_chantiers/voiture/protocole_caracterisation_terrain_climatisation_distante.md) |
| **C30** | Fiabilité de la convergence décision–exécution Climatisation en état rapporté dégradé | climatisation | **P1** | **Ouvert (2026-07-19) — ouverture documentaire, diagnostic causal NON établi.** Déclencheur : occurrence *fail-open* réelle du 2026-07-19 — divergence entre la décision publiée (arrêt sur fenêtre ouverte) et l'état physique de l'équipement, resté actif. L'ouverture **ne vaut ni** validation d'un diagnostic causal, **ni** décision de contractualiser, **ni** décision d'implémenter. **TRAVAUX STATIQUES TERMINÉS (2026-07-19).** **A1 livré sur preuves statiques** (#443 contrat, #444 runtime) : disponibilité native, cinq états inchangés, régime nominal inchangé, 2 mutations tuées. **A2 et A5 : réserves différées non solvables en l'état, non bloquantes** — A5 démontré par **deux verrous** (aucun prédicat d'état ne porte le besoin de reprise ; la couche de commande s'abstient sur les mêmes états rapportés et conclut au succès) ; réassertion, triggers de boot/reload et cadence **écartés, inopérants et générateurs de fausse sécurité**. **A6 livré sur preuves statiques** (#445 contrat, #446 runtime/UI/checker) — abstention native conjointe de `clim_mode_local`, du verdict de cohérence et de la restitution dérivée (**changement de doctrine assumé**, l'ancien double repli était contractualisé) ; **A6b sans état durable** `infra`/`postcondition`. **A3 et A4 seuls restent ouverts, sous verrou terrain** — et sur une **observation commune** : un unique relevé P1 les sert tous les deux. **Aucun travail statique restant.** **C30 actif, P1, non clos.** **Résiduel architectural : A2, A4 et A5 restent limités par l'absence d'un signal indépendant de l'intégration.** **Aucun Recorder, helper, nouveau capteur ni changement de retry.** **A2 requalifié défaut L1 — non solvable en l'état, non bloquant** : no-op légitime et abstention silencieuse ont la même signature, aucune instrumentation ne les distinguerait. **Constats, axes, risques et exclusions au document source — non recopiés ici.** | **Attendre une occurrence naturelle et consigner en temps réel le relevé P1 commun à A3 et A4. Aucune panne fabriquée. Aucun travail statique restant en l'état. C30 reste ouvert jusqu'à qualification terrain ou nouvel apport de preuve.** | [`04_chantiers/climatisation/chantier_convergence_decision_execution_climatisation.md`](04_chantiers/climatisation/chantier_convergence_decision_execution_climatisation.md) · [`04_chantiers/climatisation/protocole_observation_divergence_decision_reel.md`](04_chantiers/climatisation/protocole_observation_divergence_decision_reel.md) |
| **C32** | Restructuration des chambres enfants (déménagement) — Chambre Enfants + Salle de Jeux | transverse | P2 | **Actif (2026-07-21) — L0 à L8 livrés ; L6 (migration instance) SOLDÉ ; L6c SOLDÉ (2026-07-21) ; L9 en première passe.** Périmètre thermique amendé 3→2 (L2/L2b) puis aligné en runtime (L4a/L4b : Salle de Jeux hors agrégats et hors verdict collectif des vannes, mais **conserve vanne et plateau individuels**) ; renommage des deux pièces (L3a/L3b, behavior-preserving) ; fusion présence (L5a) et sommeil (L5b, 6 automations → 3) ; verrou CI S7 (L8). **L6 soldé le 2026-07-20 à 17:10** — dernier résidu d'instance `switch.prise_chambre_enfants_2` renommé ; écart **latent, jamais réalisé** (l'automatisation de remédiation n'avait pas été déclenchée depuis le 2026-07-15). **L6b livré et vérifié** (les 6 capteurs dérivés sont alimentés ; erreurs `temperature_source` réduites à 221 ms au démarrage). **L6c SOLDÉ (2026-07-21)** — constat sans correctif, défaut transitoire à rayon fermé **entièrement résorbé seul** (32 filtres `unknown` le 20/07 à 09:42 → **20** à 17:10 → **0** le 21/07 à 14:29:50, `periode_meteo = jour` ; 0 KO aussi sur l'intégralité des capteurs des 2 pièces ; témoin `chambre_parents` à 0). Prévision vérifiée sur les deux volets : transitoire **et** auto-réparable, aucun correctif appliqué. **L9 première passe (audit fonctionnel 2026-07-20)** : 0 entité `unavailable` sur 3891, A2 prouvé par le calcul, compteur de réveils fusionné déclenché en conditions réelles, 9 vues Lovelace inspectées sans carte cassée. **Amendement A2-bis (2026-07-20)** — la clause « groupe volets » de A2, appliquée en L4a, avait retiré la Salle de Jeux de `ouvrir/fermer_volets_enfants`, **seule brique appelée par `ouvrir/fermer_tous_les_volets`** : « Fermer tous les volets » ne fermait plus son volet, scénarios de départ et de nuit compris. Régression **silencieuse** (aucune erreur, aucune entité indisponible), trouvée en remontant les appelants du script. Groupe rétabli sur les 2 pièces et **adapté plutôt que vidé** — `commandes_groupees/chambres.yaml` → `etage.yaml`, `ouvrir/fermer_volets_enfants` → `ouvrir/fermer_volets_etage` (l'installation compte 4 volets, tous côté jardin : Séjour gauche/droit au RDC, Chambre Enfants et Salle de Jeux à l'étage). **Migration d'instance requise** : les 2 anciens `script.*` deviendront orphelins au registre. **Le volet thermique de A2 est intégralement maintenu** (agrégats, besoin de chauffe, garde COOL, verdict des vannes). Circuit pluie jamais concerné. | ~~**L6c — confirmer la résorption**~~ **✅ soldé (2026-07-21)**. Solder **L9** (restent non observables sans action : reboot de remédiation Netatmo, babyphone, groupe volets étage) puis prononcer **L10** (clôture C32). | [`04_chantiers/transverses/chantier_restructuration_chambres_enfants.md`](04_chantiers/transverses/chantier_restructuration_chambres_enfants.md) · [`../contrats/meteo/temperature_interieure/bornes_thermiques_chambres_etage.md`](../contrats/meteo/temperature_interieure/bornes_thermiques_chambres_etage.md) |
| **C33** | Dé-identification de la surface exposée (adultes) + durcissement du verrou de publication | transverse | P2 | **Actif (2026-07-20) — L0 à L5 livrés ; L6 (validation terrain) PARTIEL.** Canon `parent_1`/`parent_2` inscrit à la doctrine (L1) ; runtime dé-identifié (L2) puis **correctif urgent L2b** — le L2 déployé avait renommé les définitions sans les 50 fichiers consommateurs, **rompant toutes les notifications mobiles** sans erreur ni entité indisponible (R4 réalisé) ; dettes soldées (L2c : critères de clôture 2 et 5 réécrits, tous deux intestables ou faux) ; documentation active réalignée (L3a : 25 références mortes ; L3c : dernières annotations « ex-… » de C32, S7 tombe à **0 signal**) ; instance migrée (L5 : `person.*` au registre, 8 `input_text` capturés puis restaurés, 4 entités orphelines purgées) ; **verrou S7 étendu aux adultes (L4)**, mergé en dernier — `CRITICAL` runtime / `WARNING` doc active / historique gelé exempté, détection par **frontière de lettre** (c'est la frontière de mot qui laissait passer `telephone_arnaud_notify`, cause racine de L2b). **D1–D4** : dépôt public conservé, résidu historique assumé, canon `parent_1`/`parent_2`, identité Git de l'auteur maintenue. | **L6 — solder la validation terrain.** **R4 levé** (notification reçue, confirmée ; les 62 automatisations d'alerte partagent cette cible unique). **Critère 3 vérifié** (0 entité *live* portant un prénom hors D3/D4 ; 29 entrées subsistent au registre, toutes désactivées et sans état). **R1 ✅ levé (2026-07-20)** — transition **entrante** observée en occurrence naturelle (aucune panne fabriquée) : `presence_famille_securite` `off → on` à **19:02:37**, stabilisé à **19:03:12** après une micro-oscillation de 6 s (yoyo `D-PRES`, absorbé par construction par `delay_on 15 s`). L'établissement est prouvé **à son aboutissement** et non maillon par maillon — `presence_enfants`, `presence_famille` et `presence_famille_securite_confirmee_alarme` ne sont pas historisés — preuve jugée **suffisante par arbitrage propriétaire**. **L6 soldé.** **L7 non prononçable en l'état** : le critère de clôture 4 exige l'agrégat éprouvé sur les **deux contributeurs**, or l'établissement du 2026-07-20 n'en a mobilisé qu'**un seul** (second non observable à court terme). Trancher entre **attendre** et **requalifier le critère 4**, puis L7. | [`04_chantiers/transverses/chantier_desidentification_surface_exposee.md`](04_chantiers/transverses/chantier_desidentification_surface_exposee.md) · [`04_chantiers/transverses/chantier_restructuration_chambres_enfants.md`](04_chantiers/transverses/chantier_restructuration_chambres_enfants.md) |
| **C34** | Comportement au redémarrage, au rechargement YAML et au rechargement d'intégration | transverse | **P1** | **Ouvert (2026-07-21) — vague 1 close : VMC et deshumidificateur audites, contre-audites, puis rectifies sur preuves runtime.** Périmètre : 7 domaines à action physique (climatisation, chauffage, VMC, déshumidificateur, arrosage, alarme, éclairage) × 3 familles d'événements (reboot HA, reload YAML, reload d'intégration) + phases transitoires. **Invariant recherché** : une opération technique ne doit pas provoquer par elle-même une activation, coupure, révocation, impulsion, rejeu ou changement physique injustifié — **sans présumer** que le maintien de l'état antérieur soit toujours correct. Sources recensées : **103 automatisations, 35 scripts, 131 templates, 140 contrats, 29 chantiers, 349 helpers, 85 checkers**. Sujet **sans propriétaire documentaire unique préexistant** : `restauration_etat_helpers.md` et `separation_decision_action.md` en couvrent chacune un volet. **C32 n'est pas retenu** comme preuve de maîtrise des reloads (résidu `update.prise_chambre_enfants_2`, 2026-07-20 21:56). | Lancer la **vague 1 — VMC + déshumidificateur** (§7). Branche B retenue pour le deshumidificateur, lot restant au 10.6 du rapport. Puis vagues 2 à 4, contre-audit, portefeuille, solutions. **Aucun reboot, reload ou panne provoquée** : les 4 critères de clôture sont documentaires. | [`chantier_comportement_reboot_reload_domaines.md`](04_chantiers/transverses/chantier_comportement_reboot_reload_domaines.md) |
| **C35** | Mise en conformité du domaine VMC avec le contrat v2.4 | vmc | P2 | **Ouvert (2026-07-21) — Lots 1, 2c, 3 et 4 intégrés ; L2a acquis ; verrou contractuel levé ; corpus probatoire acquis (2026-07-22) ; référence terrain PARTIELLE L5 établie puis **L5 SOLDÉ (2026-07-22)** ; **L6 INTÉGRÉ (#523)** ; **jalon actif L7.0** ; arbitrage de calibration L2b passe 1 INTÉGRÉ (2026-07-22, #514), passes 2 à 4 INTÉGRÉES (#515, #517, #518, #519), clarification contractuelle INTÉGRÉE (#520) et amendement `vmc.md` **v2.2 INTÉGRÉ ET NORMATIF (#521)** ; arbitrage de calibration L2b **passe 5 INTÉGRÉE (#522)**, paramètres provisoires arrêtés, **L2b SOLDABLE** ; **L6 analyse d'impact PRÉPARÉE (2026-07-22)**, **sixième écart formel** relevé.** Le contrat [`vmc.md`](../contrats/vmc.md) **v2.0** est **intégré et normatif**, co-commité avec le co-changement d'aération (VMC qualifiée de consommateur **non décisionnel**) ; l'implémentation le contredit sur **cinq écarts contractuels formels**. **L'audit de calibrabilité a révélé une incompatibilité interne au contrat** : la décision B impose de reconnaître un épisode restant sous frontière absolue, **le critère de niveau seul est écarté sur preuves** (les pics vespéraux culminent là où la pièce séjourne 20 à 44 % du temps), **toute solution restante exige une comparaison temporelle**, or le **§2.2 interdit tout historique de mesures**. Le Lot 2 s'est donc scindé en **L2a** (décisions acquises : seuil absolu seul écarté, aucun modulateur retenu), **L2c ✅ soldé (2026-07-21)** — `vmc.md` **v2.0 → v2.1**, **intégré et normatif** : le §2.2 bis autorise une **observation glissante bornée** en condition d'entrée uniquement, non persistée au redémarrage, ne participant ni au maintien ni à la libération ; §4.4 bis distingue profondeur insuffisante et mesure indisponible ; §9.1 bis fixe le comportement au redémarrage — et **L2b** (calibration finale, **ordonnancée après L5**). **Le verrou contractuel est levé, mais aucune calibration n'est acquise et la calibrabilité avec les capteurs existants reste à démontrer.** Cinq paramètres restent non calibrables, dont « suffisamment assaini » : **aucune redescente d'épisode n'a jamais été observée en haute vitesse** (1 chevauchement sur 9, n=1, ininterprétable) — le régime à calibrer n'est pas observé : (1) `binary_sensor.aeration_preferable_etage` conditionne encore la voie humidité alors que le contrat lui retire tout rôle décisionnel (§4.3) ; (2) les frontières de libération sont définies, validées et affichées en UI mais **non consommées** (§6.4, §10.4) ; (3) aucun besoin hystérétique — comparateur à frontière unique (§2.2) ; (4) aucun état humidité **par pièce**, l'identité de la pièce à l'origine du besoin n'est pas préservée (§2.3) ; (5) ni restauration au redémarrage conforme au §9.1, ni maintien du besoin sur mesure inexploitable conforme au §4.4. **Aucun chantier préexistant ne portait ce périmètre** : C34 audite le comportement sous opération technique et non un modèle de décision ; l'item VMC du backlog hystérésis ne couvre que l'écart (2) et n'est pas ordonnancé — il est **absorbé** ici. Le tableau de couverture de C34 constate « VMC | 0 chantier ». **Séquence en 10 lots** : contrats → calibration §14 → définition des preuves → audit `arsenal-runtime` → référence avant → analyse d'impact → correction par lots → preuve après → vérification de l'effet §15.1 → clôture. **Aucune correction runtime avant définition des preuves (L3).** **Risque principal** : la chaîne de décision **n'est pas historisée** (décision, verdict d'aération, agrégat d'étage, relais, intention) — un comportement révisé ne serait pas plus vérifiable que l'actuel. **Hors périmètre** : doctrine du domaine aération, décomposition du capteur composite, couche d'exécution physique, dette de duplication du fichier d'aération (indépendante). Ouverture documentaire, **aucun contrat, runtime, UI ni checker modifié**. | ~~Intégrer les contrats (L1)~~ **✅** · ~~Intégrer l'amendement L2c~~ **✅** · ~~Définir le dispositif de preuve (L3)~~ **✅ soldé (2026-07-21)** — `protocole_dispositif_preuve_vmc.md` intégré ; frontière tranchée : Arsenal = définition/interprétation, arsenal-runtime = acquisition/extraction. **Arbitrage 2026-07-21 : l'effet de la haute vitesse est acquis par l'usage ; la mesure du débit et l'identification du moteur sortent du dispositif.** ~~Conduire L4~~ **✅ soldé (2026-07-21) : moyens suffisants** — trace jugée suffisamment autoritative par construction, **aucune exposition diagnostique ni modification de `recorder.yaml`** ; la frontière de libération étant un **niveau** et non une cinétique, « suffisamment assaini » est calibrable sur l'historique existant. **Corpus acquis (2026-07-22)** : 38 sauvegardes non chiffrées extraites dans `arsenal-runtime`, schéma `v53` homogène, **≈ 202,6 j de couverture unique après déduplication** (la valeur de 199,6 j annoncée au solde de L4 était antérieure à la déduplication). **L5 — référence terrain PARTIELLE établie (2026-07-22)**, méthode tranchée : caractérisation statistique seule. Acquis : dérive saisonnière robuste d'**≈ 20 points** écartant la frontière absolue annuelle comme critère **unique** (elle reste candidate comme voie d'entrée forte en composition), limites instrumentales par pièce, trace déclarative de haute vitesse recomptée (779 transitions, 205 périodes ON, ≈ 217,6 h, 22 épisodes chevauchés ≥ 60 min). **Non acquis — L5 non soldé** : comparaison basse / haute vitesse **non concluante**, débit physique, mécanisme de libération, frontière OFF, durée minimale, périmètre définitif du séjour. **Question contractuelle ouverte** : la dérive affecte aussi la signification d'une frontière de libération absolue ; le §2.2 bis n'autorise la fenêtre glissante qu'en **entrée**, une référence dynamique pour maintenir ou libérer exigerait un nouvel examen contractuel. Preuve opérationnelle : `arsenal-runtime` commit `37a6bd69520be492d4377623d6eb64ff3ad82900`. **L2b — arbitrage de calibration, passe 1 INTÉGRÉE (2026-07-22, #514)**, [`arbitrage_calibration_entree_vmc.md`](04_chantiers/vmc/arbitrage_calibration_entree_vmc.md), préparé pour intégration. Décidé : **machine contractuelle complète par lots progressifs** (et non un correctif transitoire) ; **paramètres différenciables par pièce** — un triplet unique est réfuté par les mesures ; salle de bain parents **`W = 20 min / D = 5,0 pts`**, **`S = 74 %` provisoire**, performance rétrospective réelle qualifiée **82 à 90 %** après correction de la circularité ; **machine locale obligatoire pour la salle de douche enfants** — le système doit être prêt avant l'usage, la faible volumétrie actuelle ne vaut pas absence de besoin — **calibration enfants non acquise** (pas de 1,00 pt, bruit p99 3,00, aucun pic n'atteignant 74 %, corpus d'usage léger) ; **séjour sans besoin humidité local autonome** dans la première mise en conformité, sans attribution définitive à O3 ; **retrait du verrou `aeration_preferable_etage`** sur la voie humidité (écart 1, déjà exigé au §4.3) ; **mécanisme de durée minimale existant conservé**, 15 min **non calibrées**, **§14.4 non soldé**. Triplet provisoire enfants proposé — **S = 74 % / W = 30 min / D = 5,0 pts**, aucune valeur calibrée, révision conditionnée à des usages réels suffisants. **Passe 2 INTÉGRÉE (#515)** — [`arbitrage_liberation_vmc.md`](04_chantiers/vmc/arbitrage_liberation_vmc.md) : **aucun mécanisme de libération retenu**, preuves insuffisantes pour départager seuil OFF absolu, OFF différencié par pièce et retour de niveau avec condition de stabilité ; référence dynamique **incompatible en l'état** (§2.2 bis) et paramètres saisonniers **à qualifier** — les deux exigeraient un co-changement contractuel. Bande morte **non déterminée** : le p99 calme ne vaut pas largeur de bande morte ; l'amplitude courte observée — **1,80 pt** parents, **3,00 pts** enfants — est un **repère probatoire de conception** dont toute proposition devra démontrer la robustesse, et non une largeur minimale acquise ; frontières enfants représentables au **point entier**. **§14.4 non soldé** : mécanisme conforme aux §8.2 et §8.3, mais **aucune spécification de cyclage documentée** et durée minimale **non préservée au redémarrage** ; 15 min = défaut de repli du code, ni calibré ni décidé. **Travail probatoire de la libération conduit (2026-07-22, `arsenal-runtime` `8849a054`)** : la libération au **niveau absolu seul est RÉFUTÉE** — un besoin ouvert par la voie d'évolution naît à un niveau déjà inférieur à la frontière OFF dans **61 à 97 % des cas** selon la pièce et la sonde, et se referme dans la minute sans assainissement ; aucune valeur de frontière ne corrige ce défaut. La condition « N mesures consécutives » est **écartée** (3 mesures = 30 min en hiver contre 3 min au printemps). Une résolution **sans mémoire** existe — plancher de niveau instantané sur la voie d'évolution — mais elle **rouvrirait le critère d'entrée de la passe 1** et coûte 20 à 35 points de couverture d'épisodes. **Aucune frontière OFF n'est recommandée** : le problème saisonnier persiste (durée médiane de besoin 803 min en hiver contre 7 min en été). **Point de clarification contractuelle consigné** : le §6.4 vise « la frontière d'entrée » au singulier alors que l'entrée comporte deux branches depuis la passe 1. **Passe 3 intégrée (2026-07-22, #517), révision préparée** — [`arbitrage_architecture_liberation_relative_vmc.md`](04_chantiers/vmc/arbitrage_architecture_liberation_relative_vmc.md) : toutes les options de libération relative **à mémoire** — référence d'ouverture, ligne de base pré-épisode, pic, modèle de phases — sont **écartées**, le §2.2 interdisant nommément mémoire d'épisode, valeur de pic et historique de mesures. Les six preuves ont été acquises (`arsenal-runtime` `9723a5bd`) et **RÉFUTENT la famille « référence physique instantanée »** : l'écart d'humidité absolue à l'air extérieur **s'inverse en été** — 81 % des épisodes estivaux et 100 % de ceux à air extérieur très humide seraient libérés dès le pic, 96 % en salle de douche enfants toute l'année — ce que le **§7.4 interdit**, une saison entière d'inopérance valant interdiction déguisée. La variante à **référence intérieure** fonctionne pour les **parents seulement** (7 % libérés au pic, retour médian 23 à 41 min), **échoue pour les enfants** (52 à 67 %), introduit une **dépendance inter-pièces** contraire à la localité du besoin et exigerait un co-changement plus large pour un résultat partiel : **conservée comme piste, non retenue**. **REPLI RETENU : le plancher instantané sur la voie d'évolution**, faute de mécanisme relatif général compatible, robuste et explicable. Qualifications indissociables : il **n'est pas une amélioration gratuite**, réduit la couverture de **20 à 35 points**, **rouvre l'arbitrage d'entrée de la passe 1** dans sa seule **forme**, ses valeurs **restent à calibrer**, et les triplets `74/20/5` et `74/30/5` **ne sont pas réécrits**. **Aucun co-changement contractuel requis.** **Passe 4 préparée (2026-07-22)** — [`arbitrage_calibration_plancher_liberation_vmc.md`](04_chantiers/vmc/arbitrage_calibration_plancher_liberation_vmc.md), preuves `arsenal-runtime` `47fa8a49` et `2aeaa237`. Le plancher corrige le défaut d'ouverture et son coût est chiffré — 13 à 34 % d'épisodes perdus, **presque tous estivaux**. Mais **aucune frontière OFF fixe ne convient** : la salle de bain parents est au-dessus de 58 % **89 % du temps en hiver**, si bien qu'une frontière basse produit des besoins de plusieurs jours — 20 besoins de plus de 24 h concentrant 94 % du temps cumulé, le plus long de 343 h — tandis qu'une frontière assez haute pour libérer en hiver ne voit que **8 des 42 épisodes estivaux** et **échoue le contrôle du §7.4**, une saison entière d'inopérance valant interdiction déguisée. **FAMILLE RETENUE : frontière de libération modulée par la température extérieure instantanée**, le plancher la suivant selon `P = OFF + H`, **`H` restant à calibrer par pièce** et devant rester représentable au point entier pour les enfants — à temps de fonctionnement égal, la couverture estivale passe de **8 à 24 épisodes sur 42**. Le découpage **calendaire est écarté** comme mécanisme principal : la durée maximale d'un besoin passe de 85 à 198 h selon la position, conventionnelle, des bornes, et le p10 mensuel varie de 9 points à l'intérieur d'une même saison. **Aucune constante n'est calibrée**, ni `A`, ni `B`, ni `H` : la relation sous-jacente (p10 ≈ 68,5 − 0,87 × température extérieure, R² 0,71) repose sur 7 points d'une seule année, février faisant exception, le coude observé restant un repère de conception ; et la bande de 4 points employée dans les simulations est un **candidat de construction**, **aucun balayage de `H` n'ayant été conduit** dans le modèle modulé. **Ambiguïté contractuelle signalée** : le §7.4 admet les contraintes thermiques comme modulateurs d'une frontière mais est rédigé pour le **déclenchement**, non pour la **libération**. **Clarification contractuelle INSTRUITE (2026-07-22)** — [`arbitrage_modulateur_liberation_vmc.md`](04_chantiers/vmc/arbitrage_modulateur_liberation_vmc.md), préparée sur branche, **aucun contrat modifié**. Constat : le **§7.4 n'est pas le seul obstacle ni le principal** ; le **§6.4** — « la libération dépend exclusivement de la mesure de la pièce » — est la clause décisive, plus contraignante que l'ambiguïté du §7.4 sur le déclenchement. Distinction établie entre **mesure comparée** et **point de comparaison**, et entre une **mesure physique brute instantanée** et le **verdict composite d'opportunité relevant d'O3** que le §4.3 écarte nommément — la température extérieure n'étant pas un critère mais une mesure, le §1.2 et le §2.4 ne sont pas concernés. Trois options de rédaction comparées : ne rien changer **non tenable** (le repli fixe échoue le §7.4), amender le seul §7.4 **insuffisant** (contradiction interne avec le §6.4), amender **§6.4 et §7.4 conjointement** seule option cohérente. Rédaction proposée et **non appliquée** sur les §6.4, **§7.4 bis**, §12.3 et §14.3, avec **bornage à double sens** — ni libération immédiate ni libération impossible dans un régime durable, les deux valant interdiction déguisée — explicabilité, et traitement de l'**indisponibilité de la grandeur modulante**, cas neuf où la frontière devient non calculable alors que la mesure de la pièce ne l'est pas. **CO-CHANGEMENT ACCEPTÉ (2026-07-22)** — amendement `vmc.md` **v2.1 → v2.2** **préparé sur branche, non intégré** : le §6.4 distingue la **mesure comparée** du **point de comparaison**, la libération continuant de porter sur la seule mesure de la pièce tandis que la **valeur** de la frontière peut être paramétrée par une mesure physique instantanée extérieure au logement, à l'exclusion de toute autre pièce et de tout verdict composite ; **§7.4 bis** nouveau, posant quatre conditions cumulatives — **bornage à double sens**, **ni libération immédiate ni libération impossible** dans un régime durable, les deux valant interdiction déguisée, **explicabilité**, et **indisponibilité** de la grandeur modulante entraînant le maintien du besoin (§4.4) avec garde ne pouvant tenir lieu de condition métier (§8.3) ; **§10.2** cinq exigences d'exposition supplémentaires, un besoin maintenu faute de frontière calculable devant être distinguable d'un besoin dont la condition est calculée et non satisfaite ; **§12.3** trois non-conformités caractérisées ; **§14.3** admissibilité sans obligation. **Aucune calibration chiffrée** : ni grandeur modulante, ni loi de modulation, ni bornes. Aucune mémoire introduite, le §2.2 n'étant pas concerné. **`vmc.md` v2.2 est INTÉGRÉ et NORMATIF dans `main` (#521).** **Arbitrage de calibration — passe 5, paramètres provisoires (2026-07-22), préparé sur branche** : le **balayage de `H`** est conduit dans le modèle modulé — lacune de la passe 4 **comblée** —, `H = 4` étant le coude, il divise le battement par deux par rapport à `H = 2` pour sept épisodes de moins ; **découverte structurante** — le **plafond de la frontière modulée fixe la bande morte de la voie de niveau** (`S − plafond`), le plafond de 72 employé en passe 4 ne laissant que **2 points** et 18 % de libérations en moins de cinq minutes ; **plafond retenu à 70**, donnant une **bande morte unique de 4 points sur les deux voies d'entrée**. **Paramètres provisoires arrêtés, différenciés par pièce** — salle de bain parents `A = 78 / B = 0,8 / H = 4 / bornes [50, 70]` : couverture **102/138 (74 %)**, 26/32 hiver · 54/64 printemps · 22/42 été, **13 % du temps en besoin**, médiane 1,0 h, 6 besoins de plus de 24 h, 2,1 battements par mois, 14 % libérés en moins de cinq minutes — **meilleur que le repli fixe sur les trois axes simultanément** (86/138 pour 17 % du temps et 8/42 en été) ; salle de douche enfants `A = 66 / B = 1,0 / H = 4 / bornes [50, 70]` en **représentation entière** : **19/27 (70 %)**, 1/2 hiver · 3/6 printemps · 15/19 été, **7 % du temps**, 0,4 battement par mois, **0 % libéré en moins de cinq minutes** ; `A = 78` est préféré au coude `A = 76` de la passe 4, ramenant le temps de 20 % à 13 % et les besoins longs de 12 à 6 pour quatre épisodes de moins, **la contrepartie sonore et énergétique du §15.1 le justifiant**. **Contrôle du §7.4 bis condition 2 SATISFAIT** pour les deux pièces — voie opérante dans les trois saisons, libération ni immédiate ni impossible —, avec **deux réserves énoncées** : l'hiver enfants ne compte que **deux épisodes au corpus**, ce qui n'est pas une démonstration d'opérance mais la totalité de la donnée disponible, et les 14 % de libérations rapides des parents relèvent de la voie de niveau, leur suppression coûtant 28 % de temps de fonctionnement à plafond 64. **Robustesse** — **`B` est le paramètre sensible** chez les parents, ± 0,2 faisant varier la couverture de 83 à 113 épisodes et le temps de 7 % à 24 % : c'est **le paramètre à exposer en premier** ; le point enfants est stable, par faible effectif et non par robustesse démontrée ; chez les parents le **plancher est inerte** et le plafond gouverne, chez les enfants **l'inverse**. **Garde d'indisponibilité de la grandeur modulante arbitrée** — disponibilité réelle mesurée à **96,4 %** (280 020 points, 6 ruptures de couverture réelles isolées par le témoin indépendant contre 454 silences de capteur, cumul 7,36 j, plus longue **71,1 h**) : frontière **non calculable** → **maintien du besoin actif sans exception**, voie de niveau restant opérante et voie d'évolution non calculable si le besoin est inactif, **aucune bascule automatique vers une frontière fixe** — mécanisme dont l'inopérance estivale est démontrée et qui échoue le §7.4 —, **aucun fallback silencieux**, `unknown`/`unavailable` **ne valant jamais libération** ; traitement **symétrique du §9.1 bis**, aucune doctrine nouvelle. **Aucune garde de libération retenue** : le cas visé — panne **isolée** du capteur extérieur — **n'est pas observé dans le corpus**, et une temporisation calibrée sur rien serait la temporisation tenant lieu de condition métier que le §8.3 interdit ; **conséquence assumée, risque R4**. **§14.4 SOLDÉ** — les **15 minutes sont conservées comme valeur provisoire assumée**, décidées, tracées et motivées, et non reconduites par défaut : le corpus n'étaye **aucune autre valeur**, il étaye en revanche le **maintien d'un mécanisme** (14 % des besoins parents produiraient sinon des commutations à moins de cinq minutes), et la zone est **peu sensible** ; **aucune exigence constructeur de cyclage n'existe** à la connaissance du chantier et primerait si elle devenait disponible. **L2b est SOLDABLE** au sens où tout paramètre dispose d'une valeur **arbitrée, motivée et traçable** — ce qui **ne déclare aucune valeur définitive**, **ne solde pas L5**, **n'autorise aucune correction runtime** et **ne clôt pas C35**. Restent ouverts : hiver enfants, panne isolée du capteur extérieur, absence de garde de libération, **reproductibilité inter-annuelle** de la loi `A − B × T_ext` (sept points d'une seule année, février en exception), et **mode d'exposition des paramètres renvoyé à L6**. Preuves : `arsenal-runtime` `76451bf` (support passe 1), `625a349` (contre-audit), `132072bf` (profondeur temporelle enfants, fondant `W = 30 min`), `8849a054`, `9723a5bd`, `47fa8a49`, `2aeaa237` et **`475f43a`** (passe 5). **L6 — analyse d'impact runtime, UI et CI, PRÉPARÉE (2026-07-22), aucun patch produit** : `input_number.vmc_seuil_off` et `vmc_co2_seuil_off` sont **déclarés, exposés en UI, contrôlés en intégrité et consommés par aucune décision** — l'écart n° 2 du chantier est ainsi **quantifié** et constitue une **non-conformité au §10.4**, aggravée par une carte de réglages qui **promet** le retour sous seuil d'arrêt et par une carte de diagnostic qui colore la valeur d'après ce seuil inopérant ; **`sensor.temperature_jardin` n'est pas un capteur brut** mais une **façade de publication** avec état persistant, TTL de 1 800 s, modes `fusion`/`memoire`/`abstention` et capteur de statut à **cause énumérée** — la garde d'indisponibilité du §7.4 bis est donc **implémentable sans créer aucune entité**, et le runtime consommerait **exactement l'objet sur lequel la passe 5 a calibré**, la qualification de « mesure instantanée » restant à préciser sans co-changement apparent ; l'observation glissante du §2.2 bis dispose d'un **motif établi** dans le dépôt (`13_sensor_platforms/statistics/`) et les **cinq sources nécessaires sont déjà dans l'allowlist Recorder**, donc **aucun élargissement de rétention n'est requis pour la décision** — une **tension à qualifier** subsiste avec le §9.1 bis, la plateforme rechargeant sa fenêtre depuis l'historique alors que le contrat la dit non restaurée, écart allant dans le sens conservateur ; **un besoin doit devenir un état écrit et non calculé**, un capteur `template` ne pouvant porter d'hystérésis persistante — conséquence architecturale majeure de L7, extension d'une répartition déjà pratiquée par le domaine ; **19 paramètres** contre 5 aujourd'hui, dont les bornes que le §7.4 bis impose **configurables et exposables** — **la structure de stockage est un arbitrage propriétaire préalable à L7.1**, les valeurs de calibration logées en curseurs devenant écrasables sans trace —, avec un **point de migration bloquant** : un helper sans clé `initial:` est `unknown` à sa première apparition et **aucun repli silencieux n'est admis** depuis la passe 5, le style `| float(70)` / `| int(15)` du runtime actuel étant lui-même contraire à cette doctrine tandis que le capteur d'intégrité en donne déjà le contre-modèle ; **sixième écart contractuel formel relevé** — `sensor.vmc_intention` calcule sa cause sur une approximation de lisibilité **susceptible de diverger de la décision**, ce que le **§11.2 interdit**, écart antérieur au chantier et rattaché à aucun lot ; les invariants de `binary_sensor.parametres_invalides_vmc` deviennent obsolètes et devront inclure **`S > borne_haute`**, faute de quoi la bande morte de la voie de niveau est nulle ; **le critère de clôture 8 ne se vérifie pas seul**, le checker d'aération n'interdisant que les consommateurs **non listés** ; **L8 exigera d'historiser l'état de besoin par pièce et la valeur courante de la frontière**, ajout ciblé compatible avec l'interdiction d'élargissement large. **Deux lots proposés : L7.0** propriétaire des paramètres et migration, **L7.7** UI, intégrité et CI. **ARBITRAGE PROPRIÉTAIRE RENDU (2026-07-22), préparé sur branche** — [`04_chantiers/vmc/arbitrage_propriete_parametres_vmc.md`](04_chantiers/vmc/arbitrage_propriete_parametres_vmc.md) : **structure mixte** retenue contre les 19 helpers — **11 helpers persistants** pour les paramètres opérationnellement configurables (`S`, `W`, `D`, borne basse et borne haute par pièce, durée minimale de haute vitesse), **6 constantes versionnées** pour les coefficients de calibration `A`, `B` et `H` différenciés par pièce, au motif que ce sont des **choix de calibration et non des réglages utilisateurs ordinaires** : les loger dans des curseurs les rendrait modifiables **hors Git, sans revue, sans trace** et potentiellement **en contradiction avec les preuves de L2b** — l'UI **affiche** leur valeur et leur provenance mais **ne les édite pas** ; les bornes sont des helpers **par obligation contractuelle**, le §7.4 bis condition 1 les exigeant configurables et exposables. **Invariant de propriété** : une **seule définition autoritative** par valeur, **aucun doublon helper/constante** — d'où l'exposition des §10.2 exigences 20 à 24 portée **par l'entité qui calcule**, sous forme d'attributs, et non reconstruite ailleurs —, **aucun repli numérique silencieux** — le style `| float(70)` / `| int(15)` est proscrit pour les nouveaux paramètres, un helper indisponible rendant la frontière **non calculable** —, et **aucun writer permanent** sur les helpers après initialisation, l'utilisateur en demeurant seul auteur. **Mandat de migration L7.0** : initialisation **contrôlée** des nouveaux helpers **lorsqu'ils sont `unknown`** et à cette seule condition, valeurs d'amorce issues de la passe 5, `vmc_seuil_off` supprimé ou requalifié, et devenir explicite de la valeur utilisateur de `vmc_seuil_on` qui passe de global à par pièce — **une initialisation unique n'est pas un repli déguisé**, un repli s'appliquant à chaque évaluation et masquant l'indisponibilité. **Ordonnancement arrêté : L7.0 → L7.1 → L7.2 → L7.3 → L7.4 → L7.5 → L7.6 → L7.7**, **L7.0 bloquant** — aucun calcul avant d'avoir déterminé où vit chaque paramètre — et **L7.7 obligatoire mais final**, devant contrôler et afficher l'**architecture réellement livrée** : suppression des promesses mensongères des seuils OFF, alignement de `sensor.vmc_intention` sur la décision autoritative, diagnostic des paramètres invalides et des états d'abstention, retrait de l'ancien consommateur de l'allowlist, gardes CI de l'architecture finale ; **exception explicitement admise** — la modification du checker protégeant le retrait d'`aeration_preferable_etage` peut être portée par **L7.2** si elle en constitue la preuve, ce qui rend le **critère de clôture 8 vérifiable**. **Réconciliation du dénombrement** : 11 helpers + 6 constantes + les **2 seuils CO₂ existants** = 19 ; **point non tranché** — `vmc_co2_seuil_off` n'étant consommé par aucune décision, soit la voie CO₂ acquiert une libération qui le consomme, soit il disparaît au titre du §10.4, **renvoyé à L7.4 ou L7.6**. **L5 SOLDÉ (2026-07-22), préparé sur branche** — [`04_chantiers/vmc/solde_reference_terrain_vmc.md`](04_chantiers/vmc/solde_reference_terrain_vmc.md), preuve `arsenal-runtime` **`b2fd782`** : les cinq points laissés ouverts sont repris un par un — le **débit est hors dispositif** par arbitrage antérieur, le **mécanisme de libération est arbitré et calibré**, les **arbitrages ont été rendus**, **aucune observation complémentaire ne relève de L5**, et la **corroboration décision → commande → relais est impossible sur ce corpus**. Ce dernier point est **vérifié dans les 38 bases et non déduit de la configuration** : balayage exhaustif sur les préfixes `vmc` et `aeration`, **1 entité de la chaîne sur 11** présente — la décision `binary_sensor.vmc_haute_vitesse_requise`, le verrou `binary_sensor.aeration_preferable_etage`, les relais `switch.vmc_l1` et `vmc_l2`, la conformité, la cohérence, l'intention et les trois paramètres sont **tous absents**, seule subsistant la trace déclarative `input_boolean.vmc_haute_vitesse` (38 bases, 2 162 lignes). **Conséquences définitives sur ce corpus** : la **règle d'entrée en vigueur — `HR ≥ seuil` ET `aération favorable` — n'est pas rejouable**, l'une de ses deux entrées manquant, le système actuel ne pouvant être **observé que par son résultat** ; **R1 est avéré puis clos par constat et non par résolution**, un recalcul hors ligne du verdict d'aération — qui dépend d'une quinzaine d'entrées non historisées, dont une saison déclarée et douze paramètres réglables — étant une **reconstruction et non une preuve**. **Le besoin métier initial est mesuré pour la première fois**, avec la **règle de couverture exacte de la passe 5** : **64/138 (46 %)** en salle de bain parents, dont **27/32 en hiver** mais **32/64 au printemps et 5/42 en été**, et **2/27 (7 %)** en salle de douche enfants — **le défaut n'est pas uniforme, il est saisonnier** ; l'hypothèse du verrou d'aération, concordante avec l'inversion estivale de l'écart d'humidité absolue mesurée en passe 3, **n'est pas isolable** faute d'historisation. **Référence *avant* arrêtée** sur 204,2 jours : **205 périodes ON**, **217,6 h**, **4,44 % du temps**, médiane **43 min**, p90 2,52 h, max 6,30 h, **22 périodes de moins de 15 min (11 %)**, **4,8 battements/mois**, saisonnalité 38,8 / 21,0 / 44,9 h par mois — avec un **creux d'avril de 3,3 h consigné et inexpliqué**, le corpus ne permettant de trancher entre panne, absence et changement de réglage. **Base de comparaison de L8 fixée avant le changement**, grandeurs, règle, période et **dissymétrie assumée** — AVANT = haute vitesse **exécutée**, toutes causes, logement entier ; APRÈS = **besoin simulé**, voie humidité seule, par pièce : parents **64/138 → 102/138** dont **5/42 → 22/42 en été**, enfants **2/27 → 19/27**, la couverture hivernale restant **inchangée (27 contre 26)** — la révision ne promet rien là où le système fonctionne déjà. **Trois réserves demeurent, non effacées par le solde** : chaîne de décision non historisée, donc L8 comparera des **résultats** et non des décisions ; **étiquetage manuel du §4.D du protocole jamais produit**, alors qu'il y était déclaré nécessaire et les critères C et D « indécidables » sans lui — **c'est une des raisons pour lesquelles les valeurs de L2b restent provisoires** ; comparaison basse / haute vitesse toujours **non concluante**, sans portée bloquante depuis que le débit est hors dispositif. **Critère de clôture 5 SATISFAIT.** **L7.0 PRÉPARÉ (2026-07-22), premier lot de correction runtime du chantier** — [`04_chantiers/vmc/realisation_l70_parametres_vmc.md`](04_chantiers/vmc/realisation_l70_parametres_vmc.md) : création de **dix helpers persistants différenciés par pièce** — `S`, `W`, `D` et les deux bornes de la frontière modulée pour la salle de bain parents et la salle de douche enfants —, portant à **onze** avec `vmc_duree_min_haute` inchangé le compte arrêté par l'arbitrage ; **les bornes sont des helpers par obligation contractuelle**, le §7.4 bis condition 1 les exigeant configurables et exposables. **Automatisation d'initialisation `10190000000006`**, `mode: single`, écrivant la valeur d'amorce **uniquement sur un helper disponible et sans valeur numérique**, et jamais sur un helper renseigné : un `input_number` étant restauré par Home Assistant, la condition d'écriture ne peut plus être satisfaite après le premier passage. **`unknown` et `unavailable` ne sont PAS traités de la même manière** — `unavailable` **jamais d'écriture**, cet état ne disant rien de la valeur mais seulement que la plateforme n'a pas fini de restaurer l'entité ; **`unknown` persistant, l'entité étant par ailleurs disponible → INITIALISATION**, cas **normal** d'un helper nouvellement déclaré sans état à restaurer, après la persistance de deux minutes et l'attente bornée — **l'exclure rendrait le mécanisme inopérant**, aucun helper neuf ne pouvant jamais sortir de cet état ; état **numérique conservé absolument**, égal ou non à l'amorce ; **indisponibilité temporaire puis restauration numérique → aucune réinitialisation**. Ce qui protège l'utilisateur n'est pas l'exclusion de `unknown` mais la **conjonction de trois gardes** : `unavailable` toujours exclu, persistance exigée, attente bornée. **Checker de comportement dédié** `check_vmc_initialisation_contracts.py` — le prédicat Jinja est **extrait du YAML contrôlé** et **évalué**, aucune logique n'étant reproduite dans le test, une copie le rendant aveugle à toute dérive ; il démontre que `unknown` **est** sélectionné après la garde temporelle, que `unavailable` ne l'est **jamais**, qu'une valeur numérique restaurée **même différente de l'amorce** n'est jamais écrasée, et qu'un **épisode `unavailable` suivi d'un retour numérique** laisse la valeur intacte **aux trois instants** ; auto-test à trois mutants — **ce n'est pas un repli déguisé**, un repli s'appliquant à chaque évaluation et masquant l'indisponibilité. Deux déclencheurs, démarrage et passage à `unknown`/`unavailable`, ce second cas couvrant un rechargement sans redémarrage. **Notification persistante** énumérant les paramètres initialisés et **rappelant la valeur du seuil historique global `vmc_seuil_on`**, qui n'est pas modifié : la bascule du global vers le par-pièce doit être **constatable et non subie**, la valeur ne pouvant être connue à l'écriture puisque L5 a établi que ce helper **n'est pas historisé**. **Lot strictement ADDITIF** : aucune entité supprimée, renommée ni modifiée, **aucun calcul implémenté**, **comportement du runtime inchangé**. **Reports délibérés et motivés** : les constantes `A`/`B`/`H` à **L7.4**, avec l'entité qui les calcule, l'invariant de propriété interdisant tout doublon entre calcul et exposition ; **l'exposition UI des nouveaux helpers à L7.7**, car le §10.4 impose que toute frontière exposée soit celle que le système consomme et que les afficher créerait **la non-conformité même que L6 a documentée** sur `vmc_seuil_off` ; la suppression de `vmc_seuil_off` à L7.7 avec ses consommateurs ; le retrait de `vmc_seuil_on` à L7.1/L7.3, tant qu'il gouverne la décision en vigueur. **Contrôles** : les **85 checkers `arsenal_contracts` exécutés**, tous passent, dont structure `03_input_numbers`, clé `initial`, identifiants et préfixes d'automatisation, contrat VMC, paramètres invalides et notifications ; **deux échecs constatés sont ANTÉRIEURS au lot**, vérifié en les rejouant sur `main` — `lovelace_no_inline_templating` (auto-test sensible au séparateur de chemin Windows) et `vacances` —, **ni causés ni aggravés**, et hors périmètre de C35. **Les six écarts contractuels demeurent entiers** : L7.0 en prépare la résorption sans en résorber aucun. **L7.0 INTÉGRÉ (#526).** **L7.1 PRÉPARÉ (2026-07-22), premier lot du chantier qui MODIFIE LE COMPORTEMENT** — [`04_chantiers/vmc/realisation_l71_besoins_locaux_vmc.md`](04_chantiers/vmc/realisation_l71_besoins_locaux_vmc.md) : création de **deux besoins locaux par pièce**, `binary_sensor.vmc_besoin_humidite_sdb_parents` et `..._sdb_enfants`, chacun ne lisant que la mesure de **sa** pièce (§2.3) et consommant **son** paramètre de L7.0 ; `binary_sensor.vmc_haute_vitesse_requise` **cesse d'être un comparateur et devient une agrégation pure** — il ne lit plus aucune mesure de pièce et n'applique plus aucune frontière propre, ce que le **§2.4** exige d'un niveau d'agrégation —, son `entity_id` étant **conservé**, donc **aucune migration d'historique**. **Deux changements de comportement, énoncés et non minorés** : (1) **le séjour quitte la voie humidité** — arbitrage de la passe 1 appuyé sur L5, le corpus ne justifiant pas de lui conférer un besoin humidité **local autonome**, son rattachement définitif à un objectif restant **non prononcé** ; effet attendu, une humidité élevée du seul séjour ne déclenchera plus par la voie humidité, la voie CO₂ étant inchangée ; (2) **chaque pièce compare à son propre paramètre**, amorcé à 74 %, et non plus au helper global — **écart non chiffrable depuis le dépôt**, L5 ayant établi que `input_number.vmc_seuil_on` **n'est pas historisé**, la notification de L7.0 en donnant la valeur au moment de la bascule. **Explicabilité servie dès ce lot** : exigences 1, 2, 5, 6, 7, 8 et 9 du §10.2 — état, condition d'entrée **formulée comme condition d'état et jamais comme récit** (§10.3), mesure, **frontière réellement consommée** (§10.4), pièce d'origine, **abstention distincte de l'état métier**, composition globale. **Aucun repli silencieux introduit** : mesure ou frontière inexploitable rend le besoin **indisponible** avec cause énumérée, le `| float(0)` de l'ancien comparateur — qui faisait lire zéro à un capteur en panne — **disparaissant de la voie humidité**. **Besoins NON hystérétiques à ce stade**, entrée et libération partageant la frontière de niveau : faute de bande morte, le **§9.1 n'a pas d'objet**, ses cas 1 et 2 couvrant tout l'espace et un état restauré étant immédiatement écrasé par la mesure courante — d'où des capteurs `template` et non des états écrits, la question ne se posant qu'à **L7.4**. **Précision apportée au constat de L6** : le dépôt pratique déjà une hystérésis `template` via `is_state(this.entity_id, …)`, qui tient **en session** mais **ne survit pas au redémarrage** — le constat de L6 vaut donc pour la **persistance** exigée par le §9.1, non pour l'hystérésis en général, et L7.4 tranchera en connaissance de cause. **Verrou d'aération DÉPLACÉ et non retiré** : il vivait dans l'agrégation, il vit désormais dans les deux besoins locaux, où **L7.2** le supprimera pièce par pièce — d'où une mise à jour **mécaniquement requise** de la liste des consommateurs légitimes du checker d'aération, `haute_vitesse_requise.yaml` en **sortant** et les deux besoins y **entrant à titre explicitement temporaire**, leur retrait constituant la **preuve du lot L7.2** et servant le **critère de clôture 8** ; `intention.yaml` et `delta_humidite_absolue_favorable.yaml` y **restent**, leur lecture étant **non décisionnelle** (§4.3, §10.5). **ÉCART n° 6 RÉSORBÉ DANS LE MÊME LOT, et non reporté** : ce lot rendait la divergence **certaine** — l'intention aurait désigné « Séjour » comme cause alors que le séjour ne décide plus rien, et comparé à une frontière **que le système ne consomme plus** (§10.4) —, or **une divergence introduite aujourd'hui ne peut pas être réparée plus tard**. `sensor.vmc_intention` **dérive désormais sa cause de l'attribut autoritatif `composition`** de la décision et **ne recalcule plus rien** : ni mesure de pièce, ni frontière, ni comparaison — le **§11.2 est satisfait par construction**, la cause étant calculée à partir de la décision elle-même. `unique_id` inchangé donc **`entity_id` conservé**, aucune migration. **Quatre attributs de recalcul retirés** — `humidite_max`, `piece_dominante`, `co2_sejour`, `delta_humidite_absolue` —, **aucun n'étant consommé par l'UI**, qui ne lit que `cause` : ce n'est donc **pas** une modification d'UI, réservée à L7.7. **Aucun repli silencieux** : composition absente ou vide → « Composition non exposée par la décision » ; décision indisponible → « Décision indisponible » et état « Intention indisponible » — dans les deux cas **aucune pièce n'est nommée**, ce qui est testé. **Test 4 comportemental** ajouté à `check_vmc_contracts.py` : le gabarit Jinja de `cause` est **extrait du fichier contrôlé puis évalué**, aucune logique n'étant reproduite dans le test ; il vérifie l'absence de toute source indépendante, la dérivation depuis `composition`, le comportement sur quatre compositions — dont une contenant le séjour et une décision `off` — et sur quatre absences de source. **Preuve négative établie** : un gabarit muté pour recalculer depuis `sensor.humidite_relative_sejour` fait **échouer le checker**. Effet de bord : `intention.yaml` **sort** de la liste des consommateurs légitimes du verdict d'aération, un pas de plus vers le **critère de clôture 8** sans lot dédié. **Contrôles** : les 86 checkers exécutés, tous passent, hors les **deux échecs antérieurs** `lovelace_no_inline_templating` et `vacances`, ni causés ni aggravés ; gates documentaires vertes. **L7.1 INTÉGRÉ (#527).** **L7.2 PRÉPARÉ (2026-07-22)** — [`04_chantiers/vmc/realisation_l72_retrait_verrou_aeration_vmc.md`](04_chantiers/vmc/realisation_l72_retrait_verrou_aeration_vmc.md), preuve `arsenal-runtime` **`321f923`** : le verdict d'aération est **RETIRÉ des deux besoins locaux**, où L7.1 l'avait déplacé précisément pour pouvoir le supprimer **là où il devait l'être** — **ÉCART CONTRACTUEL n° 1 RÉSORBÉ**. Motif contractuel : ce verdict évalue l'opportunité d'ouvrir des fenêtres, à l'échelle d'un **volume** et sur une échelle de temps **longue**, donc relève de **O3**, et le **§1.2** interdit qu'un critère servant O3 conditionne une extraction locale (§4.3). Chaque besoin ne lit désormais **aucune grandeur extérieure à sa propre pièce**. **GAIN MESURÉ, et il est plus faible qu'espéré** : salle de bain parents **64/138 → 73/138 (53 %)**, soit **+9 épisodes entièrement au printemps** (32/64 → 42/64), **hiver 27 → 26** et **été 5/42 → 5/42, inchangé**. **L'hypothèse posée par le solde de L5 sur le rôle estival du verrou est PARTIELLEMENT INFIRMÉE** : en été, ce n'est pas le verrou qui bloque mais **le niveau lui-même**, la dérive saisonnière de ≈ 20 points établie par L5 plaçant 74 % hors d'atteinte pendant la saison chaude — le verrou était le facteur limitant **au printemps**, pas en été. Une hypothèse formulée par un lot antérieur et infirmée par un lot ultérieur **doit être dite**. **RÉGRESSION LOCALE ASSUMÉE** : **aucun des 27 épisodes** de la salle de douche enfants n'atteint 74 %, donc **aucun besoin exprimé** entre L7.2 et L7.3 — la pièce est **moins bien servie qu'avant**, la trace en couvrant 2 sur 27 par la voie CO₂ ou par commande manuelle ; résorption par la **voie d'évolution de L7.3**, seule capable de reconnaître un épisode qui n'atteint jamais la frontière de niveau. **COÛT DE L'ÉTAT TRANSITOIRE FAIBLE** : besoin brut de 309 périodes dont **64 % de moins de cinq minutes**, mais **la durée minimale exécutive en fusionne 57 %**, ramenant à **20 commutations par mois pour 1,9 % du temps**, durée médiane portée de 2 à 31 minutes — **un besoin qui bat n'est pas une VMC qui bat**. Ce n'est donc pas le coût de l'état transitoire qui pose problème, **c'est son inefficacité** : l'essentiel du gain attendu dépend de **L7.3 et L7.4**. **CRITÈRE DE CLÔTURE 8 RENDU AUTO-VÉRIFIABLE** : L6 avait établi qu'il ne se vérifie pas seul, le checker d'aération n'interdisant que les consommateurs **non listés** ; le trou est fermé **par l'autre bout** par un **test 5** de `check_vmc_contracts.py` interdisant toute consommation du verdict dans la **voie de décision VMC** — agrégation, besoins, intention, automatisations, scripts, **11 fichiers** —, **quelle que soit l'allowlist**, avec **preuve négative établie**. Effet de bord noté : la seule **mention** de l'identifiant du capteur, fût-ce en commentaire, suffit à faire d'un fichier un consommateur au regard de T10 ; les en-têtes **décrivent** donc le verdict retiré **sans le nommer**, et le disent. **Point non tranché** : `vmc/delta_humidite_absolue_favorable.yaml` demeure listé — **reflet UI non décisionnel** (§4.3, §10.5) —, de sorte que la **rédaction littérale** du critère 8 n'est pas atteinte alors que sa **substance** l'est ; deux issues renvoyées à **L7.7**, relire le critère comme portant sur la voie de décision, ou déplacer ce reflet hors du domaine VMC. **L7.2 INTÉGRÉ (#528).** **L7.3 PRÉPARÉ (2026-07-22)** — [`04_chantiers/vmc/realisation_l73_entree_dynamique_vmc.md`](04_chantiers/vmc/realisation_l73_entree_dynamique_vmc.md), preuve `arsenal-runtime` **`16326b1`** : deux capteurs `platform: statistics` fournissent la **valeur de référence** de l'observation glissante bornée du §2.2 bis — `value_min` sur **20 min** en salle de bain parents et **30 min** en salle de douche enfants, fenêtres de la passe 1, celle de 30 min étant fondée sur la **profondeur disponible pendant les montées réelles** et non sur l'intervalle médian global entre états. **Aucun élargissement Recorder requis**, les deux sources étant déjà dans l'allowlist. **Ce n'est pas une mémoire** : le §2.2 interdit mémoire d'épisode, instant de début, durée écoulée, valeur de pic, historique, compteur et timer — ces capteurs exposent une **statistique bornée de la mesure courante**, que le §2.2 bis autorise, **en condition d'entrée seulement**. **POINT OUVERT PAR L6 CLOS PAR CONSTAT** : les attributs `age_coverage_ratio`, `buffer_usage_ratio` et `source_value_valid` sont **présents dans les 38 bases** sur deux témoins déjà en service munis de `max_age` — les exigences **12, 17 et 19** du §10.2 sont donc servables **sans créer aucun capteur supplémentaire**. **Les neuf exigences 11 à 19 sont exposées** en attributs de chaque besoin, sans repli silencieux, l'absence étant **nommée** ; un attribut `evolution_role` énonce en clair que la voie est **exposée mais non décisionnelle**, l'explicabilité ne devant pas laisser croire qu'un critère affiché est un critère appliqué. **LA VOIE D'ÉVOLUTION N'EST PAS RENDUE DÉCISIONNELLE, ET C'EST UNE IMPOSSIBILITÉ DÉMONTRÉE, NON UNE PRUDENCE** : elle existe précisément pour reconnaître un épisode qui **n'atteint jamais** la frontière de niveau, or tant que la seule frontière de libération disponible **est cette même frontière**, un besoin ouvert par l'évolution **naît déjà sous elle** et est libéré à l'évaluation suivante — **contradiction interne de la machine à ce stade, qu'aucune valeur de `W` ni de `D` ne lèverait**. Coût chiffré d'une décisionnalisation prématurée : **784 besoins supplémentaires** chez les parents, durée de vie médiane **25 s**, **91 % libérés en moins de cinq minutes**, sans aucun bénéfice de couverture. **Nuance consignée** : la durée de vie observée n'est pas une durée de besoin mais le **délai jusqu'à la mesure suivante** — la salle de douche enfants publiant plus rarement, sa médiane de 328 s paraît plus longue **sans que le besoin y soit mieux tenu**, aucun de ses épisodes n'atteignant `S`. **TENSION AVEC LE §9.1 bis QUALIFIÉE ET NON TRANCHÉE** : le contrat pose que l'observation glissante « n'est pas restaurée, elle repart vide », alors que la plateforme **recharge** sa fenêtre depuis l'historique — mais les **trois invariants du §9.1 bis sont respectés** (aucun besoin restauré perdu, aucun faux besoin créé, aucune valeur fabriquée) : **l'écart porte sur la lettre, non sur les invariants**, et va dans le sens conservateur. Aucun co-changement tant que la voie n'est pas décisionnelle ; **L7.4 devra le reprendre**. **Garde CI à deux faces** — un **test 6** vérifie que l'exposition **existe** et que la référence glissante **n'entre pas** dans l'état, avec **deux preuves négatives établies** ; ce second contrôle est **explicitement daté** et devra être **retiré par L7.4**, son échec étant alors le **signal attendu** et non une régression, ce que le message d'erreur énonce. **L7.3 INTÉGRÉ (#529).** **L7.4 PRÉPARÉ (2026-07-22), LOT PIVOT DU CHANTIER** — [`04_chantiers/vmc/realisation_l74_machine_hysteretique_vmc.md`](04_chantiers/vmc/realisation_l74_machine_hysteretique_vmc.md) : la **machine hystérétique complète** est en place — `OFF = borne(A − B·T_ext)`, `P = OFF + H`, **entrée par niveau OU par évolution au-dessus du plancher**, **libération sous la frontière modulée** —, répartie sur quatre objets à rôle unique : une **frontière calculante** par pièce détenant `A`, `B` et `H` en **définition autoritative unique**, un **`input_boolean` porteur de l'état booléen minimal**, une **façade métier** d'exposition conservant l'`entity_id`, et **deux automatisations** de transition (`10190000000007`, `10190000000008`) qui **ne recalculent aucune frontière**. **ÉCARTS n° 2, 3 et 4 RÉSORBÉS — cinq sur six le sont désormais.** **Porteur d'état** : un `input_boolean`, seul porteur restauré nativement sans Python — l'idiome `is_state(this.entity_id, …)` du dépôt tient l'hystérésis **en session** mais **ne survit pas au redémarrage**, ce que le §9.1 exige ; un booléen **ne peut structurellement rien porter d'autre**, ce qui rend l'infraction au §2.2 impossible plutôt que seulement interdite ; **auteur unique**, ces états ne sont **pas** des paramètres et ne figurent dans aucun réglage éditable. **Les cinq engagements de L7.0 sur `A`, `B` et `H` sont tenus et CONTRÔLÉS EN CI** : versionnées, différenciées par pièce — parents `78 / 0,8 / 4`, enfants `66 / 1,0 / 4` —, **définies une seule fois**, **exposées par l'entité qui calcule**, jamais dupliquées ni remplacées par un repli. **Frontières entières** pour la salle de douche enfants, dont le capteur restitue au point entier : une frontière décimale y serait **inatteignable par construction**. **GARDE D'INDISPONIBILITÉ STRUCTURELLE (§7.4 bis condition 4)** : la libération exige `off_frontiere is not none` — frontière non calculable, la condition est **fausse**, aucune branche ne s'active, **le besoin actif demeure** ; `unknown` et `unavailable` **ne valent jamais libération** ; **aucune bascule vers une frontière fixe**, ce serait substituer silencieusement un mécanisme dont l'**inopérance estivale est démontrée** (8/42) et qui **échoue le §7.4** ; la voie de niveau, qui n'en dépend pas, demeure opérante. **Aucune garde de libération retenue**, conformément à la passe 5 — **risque R4 assumé**, pire cas observé 71 h. **ÉCART RATTRAPÉ, qui aurait échappé sans relecture de la simulation** : la condition de **profondeur de fenêtre** (`age_coverage_ratio ≥ 0,5`, demi-fenêtre minimale) de la calibration de la passe 5 est reprise **à l'identique** — sans elle, l'implémentation aurait été **plus permissive que la calibration qui la fonde**. Elle sert le **§9.1 bis** (le critère dynamique ne peut pas créer de besoin sur fenêtre insuffisante) et le **§4.4 bis** (une profondeur insuffisante n'est ni une mesure indisponible ni un critère non satisfait), et **ne révoque aucun besoin**. **Explicabilité** : 24 attributs par besoin, dont les **conditions d'entrée, de maintien et de libération** (§10.2, 2 à 4) et les **frontières réellement consommées** (§10.4) ; sur la frontière, les **exigences 20 à 24**. Attribut **`maintenu_faute_de_frontiere`** : le §10.2 impose qu'un besoin maintenu faute de frontière calculable soit **distinguable** d'un besoin dont la condition est calculée et non satisfaite, **les confondre étant une non-conformité (§12.3)**. Tous les motifs sont des **conditions d'état**, jamais des récits (§10.3). **EFFET ATTENDU**, simulé sur le corpus et non mesuré sur le terrain : parents **73/138 → 102/138** dont **été 5/42 → 22/42** ; **salle de douche enfants 0/27 → 19/27, entièrement par la voie d'évolution**, aucun de ses épisodes n'atteignant la frontière de niveau. Coût 13 % et 7 % du temps, 2,1 et 0,4 battements par mois. **Divergence consignée** : la simulation **ignorait** les instants où la température extérieure est indisponible, là où l'implémentation **maintient** le besoin — **l'implémentation est donc plus conservatrice** sur les 3,6 % du temps concernés, et ces chiffres sont **une estimation, pas une prédiction**. **Contrôles** : le **garde-fou daté de L7.3 est retiré**, comme annoncé, remplacé par un **test 6 révisé** — l'évolution n'intervient ni dans le maintien ni dans la libération — et un **test 7** — exigences 20 à 24 exposées, frontière ne lisant aucune mesure de pièce (§6.4), **constantes définies nulle part ailleurs**, **libération vérifiant la calculabilité** ; **deux preuves négatives établies**. 86 checkers exécutés, tous passent hors les **deux échecs antérieurs** ; YAML valides ; gates documentaires vertes. **RÉÉVALUATION AU DÉMARRAGE COUVERTE (§9.1 cas 1 à 3)** : chaque machine porte un déclencheur `homeassistant.start` qui **confronte immédiatement l'état restauré aux mesures courantes** — cas 1, la mesure est au-dessus de la frontière d'entrée et la branche entrée s'active, **l'état restauré ne faisant pas autorité contre elle** ; cas 2, la mesure est sous la frontière de libération, même remarque ; cas 3, la mesure est **dans la bande morte** et l'état restauré est **conservé**. Les trois cas sont servis **par la même machine, sans branche particulière**, les conditions étant évaluées sur la **mesure courante** et jamais sur l'état restauré. **Sans ce déclencheur**, un état restauré incohérent subsisterait jusqu'à la publication suivante — **plusieurs minutes en salle de douche enfants** —, alors que le §9.1 pose que les cas 1 et 2 **priment inconditionnellement** et que le §12.3 range parmi les non-conformités « un état restauré l'emportant sur une mesure courante hors bande morte » : **c'est ce lot qui crée un état restaurable, c'est donc à lui de le confronter**. **Contrôlé en CI avec preuve négative.** Demeurent à L7.5 : le **cas 4** — aucun état valide restaurable —, son exposition (§10.2 exigence 10) et le §4.4. **CO-CHANGEMENT CONTRACTUEL — `vmc.md` v2.2 → v2.3** : la tension relevée par L7.3 **ne pouvait plus rester ouverte**, la voie d'évolution devenant décisionnelle et la fenêtre reconstituée au démarrage ayant désormais un **effet réel sur la décision**. Le §9.1 bis disait « l'observation glissante n'est pas restaurée, elle repart vide » et le §12.3 prohibait « une observation glissante persistée au redémarrage » ; or la plateforme employée **recharge sa fenêtre depuis l'historique enregistré**, de sorte qu'une lecture littérale rendrait l'implémentation **non conforme**. **Distinction retenue** : l'interdiction porte sur la **persistance de l'observation elle-même** — reporter au-delà du redémarrage une valeur de référence, une évolution calculée ou une conclusion —, **non sur la provenance des mesures** qui remplissent la fenêtre ; c'est la même nature de distinction que celle retenue en v2.2 entre **mesure comparée** et **point de comparaison**. **§9.1 ter nouveau** : la reconstitution est **admise sous quatre conditions cumulatives** — mesures **réellement enregistrées** de la même grandeur et de la même pièce sans aucune valeur fabriquée ; **bornage par la fenêtre nominale**, faute de quoi ce serait un **historique prohibé par le §2.2** ; **profondeur réellement disponible exposée** et critère non calculable tant qu'elle est insuffisante ; **aucun besoin restauré révoqué et aucun besoin créé** qu'une fenêtre remplie en fonctionnement n'aurait pas créé. **§12.3** précisé et complété. **L'amendement n'autorise aucun historique, ne restitue ni instant ni durée ni pic ni état antérieur, n'affaiblit aucun invariant, ne calibre rien et ne rend obligatoire aucune reconstitution.** La conformité est ainsi **établie par le contrat et non par tolérance** — l'alternative aurait été de livrer un runtime dont une clause littérale interdit le fonctionnement, ce que la doctrine proscrit : **les contrats font autorité**. **L7.6 INTÉGRÉ (#532).** **L7.7 PRÉPARÉ (2026-07-22), LOT FINAL DE LA SÉQUENCE DE CORRECTION RUNTIME** — [`04_chantiers/vmc/realisation_l77_ui_integrite_ci_vmc.md`](04_chantiers/vmc/realisation_l77_ui_integrite_ci_vmc.md). **CO-CHANGEMENT CONTRACTUEL `vmc.md` v2.3 → v2.4**, portant l'**arbitrage propriétaire option A** : conserver l'`input_boolean`, accepter que l'origine du `off` ne soit pas distinguable, **contractualiser la limite**. **§9.1 quater nouveau** — « lorsqu'aucun état restaurable n'existe, le besoin est initialisé inactif ; si le porteur ne permet pas de distinguer cet état d'un état inactif restauré, cette origine peut rester non exposée ; **aucune information de reconstruction ne doit être fabriquée ou inférée** » —, encadré de trois interdits : **fabriquer** un indicateur qui ne peut pas se déclencher, **inférer** l'origine d'un délai, d'un compteur ou d'un historique — ce serait reconstituer une mémoire prohibée par le §2.2 —, et **étendre la dispense au-delà de cette origine**. **La dispense porte sur une origine non observable, jamais sur une observation que l'on renoncerait à exposer** : la clause décrit une **limite du porteur**, non une tolérance de principe, et si un porteur distinguant l'origine était retenu, l'exigence 10 redeviendrait pleinement applicable. §9.1 cas 4 et §10.2 exigence 10 conditionnés au porteur ; **§12.3** complété. **Aucun nouveau porteur, aucune modification de machine**, conformément à l'arbitrage. **ÉCART n° 5 RÉSORBÉ — LES SIX ÉCARTS LE SONT DÉSORMAIS.** **INTÉGRITÉ DES PARAMÈTRES REFONDUE** : les invariants portaient sur les seuils **globaux**, supprimés par la mise en conformité ; ils sont remplacés par ceux de l'architecture **réellement livrée** — bornes ordonnées par pièce (§7.4 bis condition 1), **seuil de niveau > borne haute** (invariant **découvert en calibration** : la borne haute fixe la bande morte de la voie de niveau, et trop haute un besoin ouvert au niveau serait libéré aussitôt, §7.4 bis condition 2), fenêtre et évolution strictement positives, hystérésis CO₂ (invariant qui existait déjà mais **portait sur une frontière consommée par rien** avant L7.6), durée minimale positive. L'attribut `bande_morte_niveau` **affiche la valeur courante par pièce** : l'invariant le plus subtil du chantier devient lisible d'un coup d'œil. **Aucun repli numérique**, `float(none)` partout. **UI — CE QUI EST AFFICHÉ EST CE QUI EST APPLIQUÉ** : le **texte trompeur** annonçant « retour sous le seuil d'arrêt », alors que ce seuil n'était consommé par aucune décision (§10.4), est remplacé par une explication dont **chaque phrase décrit une règle effectivement appliquée** ; les **treize paramètres réellement consommés** sont exposés par pièce puis par voie, avec la **frontière d'arrêt courante affichée** sous les bornes ; les **seuils globaux `vmc_seuil_on` et `vmc_seuil_off` sont SUPPRIMÉS**, les laisser aurait maintenu la non-conformité §10.4 que ce lot corrige — **conséquence assumée** : la notification de migration de L7.0 ne peut plus lire l'ancienne valeur et **signale le retrait** au lieu d'afficher `unknown`, l'information étant perdue et mieux valant le dire que l'afficher faussement. La carte `vmc_capteur` **ne colore plus d'après une règle fictive** : elle lit par pièce le seuil **local**, la **frontière modulée telle que calculée** et l'**état du besoin de cette pièce**, la pièce étant passée **explicitement** — la déduire de l'unité était un raccourci qu'une nouvelle pièce aurait rompu en silence ; la carte du **séjour** disparaît de la voie humidité. Le **contexte non décisionnel** « Extraction utile / inutile », qui laissait croire que le verdict d'aération gouvernait l'extraction, devient « Air extérieur — plus sec / plus humide dehors » : un **fait**, non un verdict d'action (§10.5). Le **diagnostic expose la machine** — besoins par pièce et par voie avec conditions et situations de maintien, frontières modulées avec plancher, cause de non-calculabilité, grandeur modulante et **statut de son axe**, intégrité avec cause et **bande morte courante**. **CRITÈRE DE CLÔTURE 8 SATISFAIT À LA LETTRE COMME AU FOND** : `delta_humidite_absolue_favorable.yaml` est **déplacé sous `aeration/`**, dont il relève réellement — reflet UI de l'écart d'humidité absolue, sans aucun rôle VMC —, `unique_id` inchangé donc **`entity_id` conservé et aucune migration d'historique** ; **plus aucun fichier du domaine VMC ne figure dans l'allowlist d'aération**. **LE DERNIER REPLI SILENCIEUX DISPARAÎT** : le `| int(15)` de `gestion_auto.yaml`, signalé par L7.6 hors de son périmètre strict, est retiré — une durée illisible ne doit ni prolonger indéfiniment la haute vitesse ni la couper sans délai, donc **la descente est suspendue**, le système demeure dans son état courant et le capteur d'intégrité en expose la cause. Le §12.3 proscrivant « une mesure inexploitable traitée comme une valeur numérique », **le refuser pour la décision et l'accepter pour la commande aurait été une distinction de convenance**. **Contrôles** : 86 checkers exécutés, tous passent — contrat VMC, aération, recorder, paramètres invalides, initialisation —, hors les deux échecs antérieurs ; vérifié explicitement qu'aucune lecture des seuils globaux ne subsiste ; gates documentaires vertes. **TROIS CRITÈRES DE CLÔTURE ACQUIS PAR CE LOT — 6, 7 et 8** ; le critère 6 est acquis **sur le code et la CI**, restant à confirmer sur le comportement observé. **Ne modifie pas `/config`** — le déploiement est un acte distinct, jamais engagé —, **ne recalibre rien**, **ne modifie aucune machine de besoin**, **ne crée aucun porteur** et **ne clôt pas C35** : restent **L8** preuve après changement, **L9** vérification du §15.1 et **L10** passe documentaire finale. **Le déploiement vers `/config` est le prochain acte, et il appartient au propriétaire** **CORRECTIF DE DÉPLOIEMENT (2026-07-22), préparé sur branche** — [`04_chantiers/vmc/correctif_deploiement_l7_vmc.md`](04_chantiers/vmc/correctif_deploiement_l7_vmc.md). Le déploiement en `/config` a révélé **deux défauts**, l'un fonctionnel et l'autre d'affichage, de **racine commune** : une hypothèse non vérifiée, et une doctrine non lue. **(1) FONCTIONNEL — les paramètres n'ont jamais été initialisés.** L'automatisation `10190000000006` de L7.0 n'écrivait que si un helper était « disponible et non numérique » ; or **un `input_number` sans état restaurable ne prend jamais `unknown` : il prend son `min`** — vérifié sur les 38 bases, **73 helpers et 32 065 lignes d'état, aucun état non numérique** — et confirmé au déploiement par l'**absence totale de notification**. Les dix helpers ont démarré à leur minimum : **bornes 40/40, seuils 50, fenêtres 5 min, évolutions 1 pt**, d'où `OFF = 40` en permanence, la modulation écrasée, une entrée à 50 % ou sur une montée d'un point en cinq minutes, et une libération à `HR ≤ 40` alors que L5 a mesuré une ligne de base entre **53 et 63 %** : **un besoin ouvert ne se libérait quasiment jamais**. **C'EST LA DEUXIÈME OCCURRENCE DE LA MÊME ERREUR** — L7.5 avait déjà démontré qu'un `input_boolean` sans état restaurable apparaît à `off` et jamais à `unknown`, sans que le raisonnement soit porté à `input_number` ; la vérification tenait en une requête. **Aucun mécanisme automatique ne peut corriger cela** : une fois un helper à son minimum, **rien ne distingue « jamais initialisé » de « réglé au minimum »** — exactement la limite d'origine non observable que le **§9.1 quater** énonce pour l'état des besoins, de sorte qu'un amorçage automatique écraserait tôt ou tard un réglage délibéré. L'automatisation est donc **SUPPRIMÉE** et remplacée par une automatisation d'**amorçage manuel `10190000000011` DONT LA CLÉ `trigger` EST VIDE** : elle ne peut jamais s'exécuter d'elle-même, et cette absence est **vérifiable et vérifiée** (VINIT-001). **Forme retenue et motivée** : le test 2 du contrat VMC interdisant le terme `vmc_seuil` dans tout fichier de `10_scripts/vmc/`, un script d'amorçage y contreviendrait par construction — **plutôt que d'affaiblir une garde CI pour y loger le correctif**, la forme est une automatisation sans déclencheur, qui offre la même garantie et une de plus. **Elle n'a pas été exécutée : aucune valeur n'a été écrite dans le runtime.** Aucune logique métier n'est modifiée — machine, agrégation, commande et relais inchangés. **(2) UI — la doctrine `00_documentation_arsenal/ui/` (27 documents normatifs, dont `pattern_dashboard.md` et `pattern_dashboard_reglages.md` opposables) N'AVAIT PAS ÉTÉ LUE avant L7.7.** Deux défauts en ont résulté. **Réglages** : grilles à **3 colonnes**, alors qu'aucun autre dashboard de réglage du dépôt ne dépasse 2 et que la plupart n'emploient aucune grille — sous ~160 px en colonne masonry, une tuile à contrôle `numeric-input` tronque ; corrigé par des tuiles **une par ligne** groupées par `sub_section_header`. **Les `tile` sont CONSERVÉES, et c'est la doctrine qui l'impose** : le §🟩 en fait la forme « par défaut et suffisante » pour l'édition d'un Seuil et le §🚫 range leur remplacement massif parmi les **non-objectifs** — les remplacer aurait été une faute. Deux manques comblés : la carte **« effet réel »** que le §🔎 rend **obligatoire** pour un Seuil dont l'effet n'est pas lisible, externalisée dans `includes/cartes/vmc/`, seule convention du dépôt ; et le bandeau adossé à `parametres_invalides_global` comme l'exige le §✅. **Diagnostic** : trois cartes `type: entities` totalisant 26 lignes dont 18 attributs à libellés longs, **VMC étant le seul des 13 dashboards de diagnostic à en employer** ; remplacées par `custom:button-card` sur **templates et socles existants sans en créer aucun** — `carte_etat_interprete`, `carte_attribut_categoriel_interprete`, `socle_kpi_label_72`, `socle_info_72`, `carte_capteur_etat_textuel` —, avec **hiérarchie plutôt que mur de cartes**, le détail maintien/libération d'un besoin étant placé sous un `conditional` puisqu'il n'a de sens que si le besoin est actif. **Couleurs** : palette déclarée au site d'appel, aucune couleur décorative, **gris indisponibilité** comme seul repli. **ÉCART TEXTE / PRATIQUE CONSIGNÉ, NON TRANCHÉ** : `pattern_dashboard.md` liste `entities` et `markdown` comme **autorisés**, alors que la pratique est plus stricte — 13 dashboards de diagnostic sur 13 n'emploient que `custom:button-card`. **Ce lot applique et garde la pratique sans modifier la doctrine** : l'aligner relève d'un **arbitrage propriétaire**, non d'un correctif. **TROIS GARDES CI**, chacune avec **preuve négative établie** : **VINIT-001** l'amorçage n'a aucun déclencheur ; **VINIT-002** aucune automatisation VMC ne conditionne un paramètre à `unknown`/`unavailable`, sans faux positif sur un `sensor` ; **VINIT-004** table d'amorce complète ; **§UI (a)** aucune carte native dans le diagnostic VMC ; **§UI (b)** aucune grille de plus de 2 colonnes dans les réglages. Le checker `check_vmc_initialisation_contracts.py` **change d'objet** : il vérifiait le comportement d'un mécanisme, il garde désormais **son absence**. **Reste à faire, acte propriétaire** : déployer, puis **exécuter l'amorçage manuel sur instruction explicite**, puis vérifier que `parametres_invalides_vmc` repasse à `off`. | [`04_chantiers/vmc/chantier_mise_en_conformite_vmc_v2.md`](04_chantiers/vmc/chantier_mise_en_conformite_vmc_v2.md) |
---

## ② Parqués — *chantiers réels, délibérément non ordonnancés*

| ID | Chantier | Domaine | Prio | Statut | Source faisant foi |
|----|----------|---------|------|--------|--------------------|
| **C5** | Observabilité auto-ajustement courbe (lots P8–P9 restants) | chauffage | P2 | ouvert — non ordonnancé ; **P1–P7 livrés** (P4–P7 le 2026-07-02, cf. source), restent **P8** (validation `76` §11 + calibration) et **P9** (clôture) ; aucun changement de comportement | [`04_chantiers/chauffage/ch_observabilite_auto_ajustement_courbe.md`](04_chantiers/chauffage/ch_observabilite_auto_ajustement_courbe.md) |
| **C11** | Arrosage — modulation bornée de durée par critères physiques | arrosage | P3 | **cible doctrinale ouverte, runtime fermé.** Distingue « combien de temps arroser » (ce chantier) du « quand arroser » (Lot 4 / C10). Parqué derrière prérequis P1–P4 : validation terrain C10 (P1), capteurs sol fiabilisés + tarissement (P2), runtime canal demande climatique ET₀/VPD (P3 — **✅ livré** en observation/diagnostic seul, `12_template_sensors/arrosage/demande_climatique.yaml`), contrat dédié rédigé (P4). **Réveil** : P1–P3 réunis ⇒ rédaction du contrat « modulation de durée » (P4), puis runtime borné/désactivable/observé à blanc. Aucun runtime **de modulation**, aucune UI à ce stade (le runtime P3 livré est une **infrastructure d'observation**, pas la modulation). | [`02_conception/arrosage/cadrage_modulation_duree_arrosage.md`](02_conception/arrosage/cadrage_modulation_duree_arrosage.md) |
| **C17** | Précipitations — autorité unique d'acquisition sécurisée, qualifications plurielles | météo / précipitations | P2 | **arbitré (2026-07-11) — direction incrémentale, cadrage livré ; non ordonnancé.** Ex-`D-PLUIE` promu. « Autorité unique » recadrée = **acquisition sécurisée partagée**, PAS fusion — les qualifications (any-rain `pluie_en_cours` / forte `intention_pluie_forte` / récente `pluie_recente`) sont **plurielles par design**. Distinction métier gravée : chambres = protection fenêtre ouverte (détection précoce) ; séjour = protection préventive sous pluie forte, baie même fermée (`intention_pluie_forte` Netatmo-seul **correct** — seul le pluviomètre quantifie ≥ 2,5 mm) ⇒ **« séjour aveugle » retiré, acceptable par design**. Dette restante, sans incident actif. **Lot 1 (contrat de production) LIVRÉ** : contrat normatif [`meteo/pluie_production.md`](../contrats/meteo/pluie_production.md) + checker CI `contracts_pluie_production.yml` (8 tests ; source quantitative exclusive « pluie forte » INV-PROD-3 ; **verrou anti-régression C16** INV-PROD-5) ; frontière orpheline `volets_pluie.md` §5.2 **comblée** ; distinction chambres/séjour gravée et opposable. Reste lot 2 **sécurisation locale des sources** (séquencé derrière la validation terrain C16). Big-bang et unification séjour **exclus**. **Réveil** : go opérateur ⇒ engager le lot 2. | [`02_conception/meteo/cadrage_autorite_unique_precipitations.md`](02_conception/meteo/cadrage_autorite_unique_precipitations.md) · [`../contrats/meteo/pluie_production.md`](../contrats/meteo/pluie_production.md) |
| **C21** | Préparation COOL du retour de Vacances | climatisation | P3 | **réservé/parqué (2026-07-14) — dépend de C20.** Préparation thermique avant `input_datetime.fin_vacances` : fenêtre bornée, **vérité opérationnelle à écrivain unique** (orchestrateur, patron pré-confort chauffage **sans ses dettes**), **durée transitoire réglable** (D11), **consigne dédiée** (D10, 3ᵉ contexte thermique), **neutralisation sélective** du veto composite (jamais aération/fenêtres/horaire/ext), fail-closed, boot idempotent, `fin_vacances` (D13), fin de fenêtre sans retour (D12), présence terminale. **Réveil** : livraison + validation de C20. | [`04_chantiers/climatisation/chantier_preparation_retour_vacances_cool.md`](04_chantiers/climatisation/chantier_preparation_retour_vacances_cool.md) · [`02_conception/climatisation/cadrage_decision_politique_absence_vacances_cool.md`](02_conception/climatisation/cadrage_decision_politique_absence_vacances_cool.md) |

---

## ③ À arbitrer / dormants — *décision en attente, aucun travail défini*

| ID | Sujet | Domaine | Statut | Déclencheur de réveil | Source faisant foi |
|----|-------|---------|--------|------------------------|--------------------|
| **D-C18-CD** | Résiduels d'arbitrage C18 — sens positif de `degrade` & mise à jour topologie docs 08/10 | arrosage | **dormant — deux arbitrages distincts, non séquencés, aucun travail défini** (C18 clos par ailleurs) | **D-C18-C** : retenir ou écarter l'option C — enrichir `degrade` d'un critère batterie faible / échecs récents pour lui donner valeur de signal (arbitrage de fond). **D-C18-D** : décider si l'inventaire topologique 08/10 doit être mis à jour (mention de contexte vs mise à jour). | [`04_chantiers/arrosage/chantier_semantique_sante_pont_rain_bird.md`](04_chantiers/arrosage/chantier_semantique_sante_pont_rain_bird.md) |
| **D-PRES** | Dette de modélisation de la présence | présence | **dossier d'arbitrage dormant — annoté** (non décisionnel) | incident reproductible sur un consommateur de présence **brute**, ou ouverture d'une des 6 questions du §9 ; **inclut l'arbitrage ex-C4 « étendre la présence confort thermique stabilisée hors COOL/DRY vs la confiner » (§9 q5 / piste P-F)**. **Note (2026-06-20)** : un incident sur consommateur de présence **brute** (yoyo de désarmement alarme à proximité) a été **mitigé localement** par `binary_sensor.presence_famille_securite_confirmee_alarme` (`delay_on 15 s`, désarmement seul ; armement et signal brut **inchangés**) — **précédent partiel de la piste P-F appliquée au domaine alarme** ; **R1–R8 non clos**, **arbitrage ex-C4 / §9 q5 / P-F côté confort/clim intact** (réf. `../contrats/alarme/30_decision_centrale.md`). | [`02_constats/transverses/cadrage_dette_modelisation_presence.md`](02_constats/transverses/cadrage_dette_modelisation_presence.md) |

---

## ④ Backlogs (pointeurs) — *conteneurs à picorer, jamais l'item ligne à ligne*

> Ces documents agrègent des dettes secondaires (P2/P3). Aucune n'est un chantier piloté : un item n'apparaît en *Actifs* qu'une fois **promu**.

| Backlog | Périmètre | Source faisant foi |
|---------|-----------|--------------------|
| **Climatisation / hystérésis** | ~~H2 (VMC seuils OFF morts)~~ **absorbé → C35** (2026-07-21, ce backlog n'en est plus propriétaire), H3a/H3b (aération), D10, blocages contextualisés (ex-N5 **requalifiée métier** par arbitrage 2026-07-03 — Lot B du plan post-revalidation) · (H1 clos, cf. ⑤ ; D13 traité 2026-07-03, gardes F2/F3 en CI ; **D5 promu → C13** ; **D-tuile : clôture arbitrée 2026-07-03**, écriture backlog au Lot C) | [`04_chantiers/climatisation/backlog_climatisation_hysteresis.md`](04_chantiers/climatisation/backlog_climatisation_hysteresis.md) · [`03_plans_action/climatisation/plan_action_post_revalidation_climatisation.md`](03_plans_action/climatisation/plan_action_post_revalidation_climatisation.md) |
| **ECS** | Durcissement CI (DESINF-1/2, CI-1/2/3, OFF-5) ; bucket Medium (collecte avant décision) | [`04_chantiers/ecs/backlog_ecs.md`](04_chantiers/ecs/backlog_ecs.md) |
| **Aération** | Résiduels de clôture : factorisation décision, `unique_id` étage, dashboard diagnostic | [`05_clotures/aeration/cloture_aeration_recommandation.md`](05_clotures/aeration/cloture_aeration_recommandation.md) (§5) |
| **Précipitations** | Résiduels P3 (proposition d'audit) : hystérésis `intention_pluie_forte` (comparaison `>=` nue → chatter au seuil) · **parité CI du palmarès pluie** (contrat STRICT `pluie_palmares.md` sans checker dédié, vs `check_palmares_temperature_*`) — **relève de C14**. _(Résolus par C16 : double représentation Zigbee dans `maj_sensor/on` — entrée désormais consommatrice de l'évidence ; bande Netatmo 0/0,1 — sémantique unifiée `>0`. Résolu le 2026-07-15 : alignement noms fichier↔entité — `intention_pluie_detectee.yaml`→`intention_pluie_forte.yaml`, `volets_auto_pluie.yaml`→`fermeture_volets_pluie.yaml` ; renommage seul, aucune entité ni comportement modifié, aucune convention créée en doctrine.)_ | [`01_rapports/meteo/audit_precipitations_domaine.md`](01_rapports/meteo/audit_precipitations_domaine.md) |

> **Vérification candidate — faite (2026-06-17).** Conformité runtime de `clim/06_doctrine_blocages.md` (patron `_reel`/`_actif`) : **runtime conforme** (les deux blocages appliqués suivent le patron ; dette fenêtres/absence fidèle au runtime). Un seul écart **documentaire mineur** (description périmée du voyant `clim_bloquee` en §5/§8), **corrigé dans le contrat** au même commit. Dette fenêtres/absence inchangée (renommage `_reel` différé, hors périmètre). **Aucun chantier ① créé.** Source : [`01_rapports/documentation/triage_recalibre_post_bluetti.md`](01_rapports/documentation/triage_recalibre_post_bluetti.md) (§4).

---

## ⑤ Clos récents — *trace (≈ 90 j), pour mémoire*

| Sujet | Clôturé / absorbé le | Preuve | Source |
|-------|----------------------|--------|--------|
| Doctrine de solvabilité probatoire des chantiers Arsenal (C31) | 2026-07-20 | **CLOS — conforme.** Les **4 critères du §9** satisfaits : doctrine rédigée, mergée et propriétaire unique (Lot 1) ; **3 raccords du §6** réalisés (Lot 2) ; doctrine **appliquée à 4 cas réels** (C29, C27, C28, C4 — §6 bis), au-delà du minimum de deux ; **une décision effectivement changée** (C29 — réserve requalifiée *sans objet* puis close). Critères **documentaires**, solvables sans preuve terrain. Suites régularisées le même jour : qualification portée chez **C27/C28** (R-QUALIF-2), réserve de surveillance **C4 levée** (R-QUALIF-3). | [`chantier_doctrine_solvabilite_probatoire.md`](04_chantiers/transverses/chantier_doctrine_solvabilite_probatoire.md) · [`solvabilite_probatoire.md`](../architecture/03_doctrines/solvabilite_probatoire.md) |
| Couleurs d'icônes des tuiles de navigation — reliquat section ⚙️ Système (D-NAV-COULEUR) | 2026-07-19 | **Soldé — Option A (neutralisation) + refonte du menu.** Dernier reliquat du dossier (le menu ☰ Navigation était déjà harmonisé : 5 dynamisées / 3 neutralisées). Section ⚙️ Système refondue **12 → 6 tuiles** (`18_lovelace/dashboards/navigation.yaml`, commits `d9a221b5`→`efd6ae49`→`7756e6d4`, **déployé + vérifié live HA**) : suppressions (Automations, Scripts, Dashboards), factorisations (Intégrations+Entités → « Paramètres » `/config/dashboard` ; États+Templates+YAML → « Outils Dev » `/developer-tools`), survivantes neutralisées au **gris de base NAV** `rgb(158, 158, 158)`. **Reboot HA neutralisé** — renversement de doctrine assumé (l'orange n'est plus une « affordance action sensible » : une couleur chaude invite au clic d'une action destructive → gris discret, garde-fou = `confirmation:`). Plus **aucune** couleur d'icône figée hors palette dans `navigation.yaml` ; grille 3×2 pleine. **Changelog non créé** (artefact de release). | [`04_chantiers/lovelace/cadrage_couleurs_icones_navigation.md`](04_chantiers/lovelace/cadrage_couleurs_icones_navigation.md) (§2 septies) |
| Température intérieure multi-capteurs — abstention textuelle `unknown` → indisponibilité native (C29) | 2026-07-19 | **Clôturé — documentaire, réserve terrain différée. Arc complet livré.** Ouverture #432, consignation contractuelle L1 #433 (`consolidation.md` v1.5 + `stabilisation.md` v1.2 — §6 abstention native), runtime + checkers L2/L3 #434. **Conformité contrat → runtime → checkers** : les deux familles numériques (`temperature_brute_consolidee_<zone>` / `temperature_stabilisee_<zone>`, `device_class: temperature`) s'abstiennent désormais via `availability` (`unavailable`) au lieu d'émettre la chaîne `{{ 'unknown' }}` en `state`. **Émission textuelle `{{ 'unknown' }}` structurellement supprimée** (0 occurrence sur les deux fichiers, YAML parsé, **enforcée par le test T6** des checkers `check_consolidation` / `check_stabilisation`). **Matrice statique 19/19** (`availability == state numérique` sur : sources valides, source unique, divergence, mémoire vivante, mémoire expirée, retour de source) ; test négatif T6 (ancien idiome rejeté, natif exigé) ; checkers de domaine + connexes verts. **Invariants préservés** : TTL mémoire 1800 s, fusion, départage, EWMA, restauration/`this`, valeurs nominales, consommateurs aval (traitent déjà `unavailable` ≡ `unknown`). Idiome de remplacement (rendu vide + `availability`) déjà prouvé **LIVE sur HA Core 2026.7.2** (C27/C28, `temperature_min_chambres`). **Réserve honnête — NON bloquante** : confirmation **naturelle, non provoquée** de l'absence d'erreur `template.validators … expected a number` lors d'une prochaine abstention réelle (**preuve terrain différée**) ; la suppression causale étant structurelle, cette réserve **ne remet pas en cause la clôture**. **✅ Réserve CLOSE le 2026-07-20** — qualification « réserve sans objet » (doctrine §3), requalification effective au titre du Lot 3 de C31. | [cadrage C29](02_conception/meteo/cadrage_abstention_native_temperature_multi_capteurs.md) · [consolidation.md](../contrats/meteo/temperature_interieure/consolidation.md) · [stabilisation.md](../contrats/meteo/temperature_interieure/stabilisation.md) |
| Agrégats de température MIN/MAX des chambres et leur restitution sur le dashboard Arsenal (C27) | 2026-07-19 | **Clôturé — arc complet livré.** Contrats **Lot 2A** (#413), reconsolidation (#414), **Lot 2B** (#415) consignés. **Agrégats honnêtes/abstinents** (R2, #420) → `unavailable` sur mort des trois observations. **Défense fonctionnelle complète** (C28 L3A #418 + L3B #419 + R2 #420) : plus de valeur figée mensongère. **Conformité backend/UI finale (#422)** : frontière basse **publiée** `sensor.seuil_interieur_on_chauffage_applique` (abstinente) ; **catégories backend O3** `sensor.categorie_thermique_{min,max}_chambres` (`froid`/`dans_plage`, `chaud`/`dans_plage`, abstention native, aucun cross-entity) ; cartes « backend décide, UI restitue » — **suppression des lectures de helpers, des recalculs UI et du cross-entity** ; **palette Exception 2 étendue** (froid `rgba(144,202,249,0.25)` / dans_plage `rgba(76,175,80,0.2)` / chaud `rgba(244,67,54,0.2)`) ; **gris indispo `rgba(158,158,158,0.1)` prioritaire**. **Les six écarts opposables du Lot 2B sont résolus (#422).** Verrou : test **T4** `check_ui_runtime_colors` + mutation-tests ; **CI verte** ; implantation dashboard (`arsenal.yaml`) inchangée. **Réserve honnête** : panne réelle simultanée des trois thermomètres **non provoquée, preuve terrain différée** (partagée avec C28) — conformité établie par preuve statique + checker + simulation. **Qualifiée (2026-07-20) : réserve opportuniste / L5 — non bloquante**, périmètre propre C27 (R-QUALIF-2). | [chantier C27](04_chantiers/transverses/chantier_temperature_min_max_chambres_dashboard_arsenal.md) · [contrat Lot 2B](../contrats/meteo/temperature_interieure/restitution_chambres_etage.md) |
| Machine COOL/HEAT sur observation de température inexploitable (C28) — défense aval + honnêteté amont | 2026-07-18 | **Défense fonctionnelle complète.** Doctrine consignée (#416 ouverture, #417 consolidation L1/L2). **L3A** — défense aval, PR **#418** : révocation immédiate de l'admissibilité COOL/HEAT sur besoin `unknown`/`unavailable` (runtime + boot fail-closed) → `clim_target_mode = off`. **L3B** — honnêteté amont, PR **#419** : seuils sans repli numérique, 4 franchissements + 2 besoins abstinents, retour dans la bande d'hystérésis → `off` **sans résurrection d'un ancien `on`**. **Interlock fermé par C27 R2, PR #420** (agrégats MIN/MAX abstinents). `unknown`/`unavailable` **ne sont plus assimilés à `false`**. **Preuve live** de l'idiome `availability`/`this` réalisée sur **HA Core 2026.7.2** (sonde isolée, supprimée) ; **checkers et CI verts** (`check_climatisation_admissibilite`, `check_climatisation_seuils_cool`) ; mutation-tests Sandbox ; simulation logique. **Panne réelle simultanée des trois thermomètres : NON PROVOQUÉE — preuve terrain explicitement DIFFÉRÉE** (critères §12 n'exigent pas de panne artificielle). **Qualifiée (2026-07-20) : réserve opportuniste / L5 — non bloquante**, périmètre propre C28 (R-QUALIF-2). **DRY, Guard, exécution inchangés.** | [chantier C28](04_chantiers/climatisation/chantier_besoin_clim_observation_inconnue.md) · [admissibilité](../contrats/climatisation/capteurs/admissibilite/00_admissibilite.md) · [seuils](../contrats/climatisation/capteurs/seuils_et_franchissements/10_sensors_seuils.md) |
| Restitution UI de l'indisponibilité — Chauffage, 3 cartes surface principale (C26) | 2026-07-18 | **Clôturé (documentaire) — patch UI mergé (#406) ; preuve statique acquise ; preuve visuelle non requise ; observation terrain différée.** Écart **CH-DIAG-08 / F1** de l'audit d'exposition diagnostique (#404), seul écart de verdict, strictement UI, sans conséquence fonctionnelle : sur la surface principale, `carte_chauffage_intention` / `carte_chauffage_synthese` / `carte_chauffage_decision` restituaient une source déjà consommée indisponible en état métier valide (libellé brut / gris neutre `0.2` / **faux rouge d'incohérence**) au lieu du **gris indispo `rgba(158,158,158,0.1)` prioritaire** (R6). **Correctif (#406)** : garde d'indisponibilité prioritaire (ensemble aligné exemplaire ; `Indisponible` / `Données indisponibles` / `mdi:shield-off`) évaluée avant toute restitution sémantique. **Revue de clôturabilité (2026-07-18, lecture seule, critères §9 du chantier — aucune exigence ajoutée)** : **preuve statique `ACQUISE`** (6 checkers UI verts, dont I1 garde indisponibilité / I2 masquage ; garde prioritaire sur les 3 cartes ; états disponibles inchangés ; pas de faux rouge — garde `decision` avant cohérence ; pas de gris neutre pour l'indispo ; **F2/F3/F4/O-1 intacts**) ; **preuve visuelle `NON ACQUISE — NON REQUISE POUR LA CLÔTURE`** (aucune capture disponible, non provoquée ; §9 ne subordonne pas la clôture à une indisponibilité réelle) ; **observation terrain `DIFFÉRÉE — NON PROVOQUÉE`**. Conclusion : **CLÔTURABLE AVEC OBSERVATION TERRAIN DIFFÉRÉE** — une observation terrain future ne conditionne pas la validité de la clôture documentaire. Ouverture #405 · patch UI #406 · traçabilité #407. **Périmètre strictement UI (3 cartes)** ; aucun autre constat traité. | [`04_chantiers/chauffage/chantier_restitution_indisponibilite_ui.md`](04_chantiers/chauffage/chantier_restitution_indisponibilite_ui.md) · [`01_rapports/chauffage/audit_exposition_diagnostics_chauffage.md`](01_rapports/chauffage/audit_exposition_diagnostics_chauffage.md) |
| Sécurisation des paramètres ECS — suppression des fallbacks numériques artificiels (C24) | 2026-07-17 | **Écart I1 résorbé — matrice de clôture 9/9 SATISFAIT (C5 avec réserves explicites).** Chaîne de livraison #396 (contrat ballon) → #397 (Lot 1A `temperature.yaml`) → #398 (Lot 1B.1 `reset_verrou_cycle`) → #399 (Lot 1B.2 `cycle.yaml`) → #400 (Lot 2 capteur sœur `consigne_effective` + contrat dédié) → #401 (verrouillage CI local ECS, contrôle **T14** + correctif A1 `log/fin.yaml`). Producteurs sécurisés `unknown` sans valeur valide (aucune sentinelle numérique) ; consommateurs décisionnels durcis (`is_number`/provenance) ; capteurs `tmax` conformes sous invalidité (`availability`→`unavailable`≠0). **Réserves assumées (pas un reste à corriger)** : anciens `0.0` Recorder pré-correctif purgés naturellement à 30 j ; `log/debut.yaml`/`log/temperature_max.yaml` conformes mais non intégralement couverts par T14 ; preuve contractuelle/structurelle/oracle, **sans** rendu Jinja HA réel. **Hors périmètre non rouverts** : variable morte `consigne_reelle`, boost enfant, D2/D3, UI, généralisation transverse de T14, nettoyage historiques. **Clôture chantier-scopée — le domaine ECS reste non clôturé.** | [chantier](04_chantiers/ecs/chantier_securisation_parametres_ecs.md) · [clôture](05_clotures/ecs/cloture_c24_securisation_parametres_ecs.md) · [audit ECS](01_rapports/ecs/audit_exposition_diagnostics_ecs.md) |
| Climatisation / Ventilation — « auto constructeur » récurrent (dérive `fan_mode → auto`) | 2026-07-17 | **Cause racine corrigée à la source + validation terrain (utilisateur).** Sujet **audit-driven, hors numérotation C** (jamais entré en ① — l'audit avait établi la chaîne Arsenal **saine** et le correctif **hors Arsenal**). Levier **M2 / VENT-AUTO-3** : course *poll-after-set* dans `fujitsu_airstage/climate.py` (`async_refresh()` immédiat après écriture sur API à cohérence différée → relecture de l'ancien `auto` → redéclenchement P3 → flapping + martèlement API). Correctif **dans le dépôt `ha_airstage`** (fork `antoinevalentinHA/ha_airstage`, branche `arsenal-stable`), **vendoré** : **écriture optimiste** `apply_optimistic_update()` factorisée dans `entity.py`, appliquée dans `climate.py` (fan/hvac/temp/swing/preset) **et** `switch.py`, en remplacement du re-poll immédiat ; tracé dans `PATCHES_ARSENAL.md`. **M2 passe d'« hypothèse forte » à confirmé** (disparition du symptôme = preuve §5 de l'audit). **Validation terrain 2026-07-17** : plus de retombées régulières sur `auto`. **Chaîne Arsenal inchangée** — filets **conservés** : P3 défend toujours le reset firmware **M1** (VENT-AUTO-2, `cool↔dry↔heat`, inévitable côté appareil) ; miroir silence + P1/P2 intacts ; **VENT-AUTO-4** (`for:` sur P3) non appliqué (M2 suffit). Documentation seule, **aucun runtime/contrat/YAML Arsenal touché**. | [`05_clotures/climatisation/cloture_ventilation_auto_constructeur.md`](05_clotures/climatisation/cloture_ventilation_auto_constructeur.md) · [`01_rapports/climatisation/audit_ventilation_auto_constructeur_recurrent.md`](01_rapports/climatisation/audit_ventilation_auto_constructeur_recurrent.md) |
| Notifications — cycle de vie des persistantes (C15) | 2026-07-15 | **Validation terrain acquise — voie « live contrôlé » du plan de clôture (§2/§3), zéro instrumentation.** Reboot volontaire pendant un **cycle lave-vaisselle réel** : flag `on` à 21:09:29 (aucun retrait), `homeassistant_start` **22:01:30**, `persistent_notification.create(lave_vaisselle_cycle)` **22:02:17 = +47 s** ≈ le `delay: "00:00:45"` de `systeme_stable` (`10120000000018`) ⇒ re-projection portée par le trigger `systeme_stable → on`, soit l'**idiome canonique** réclamé par NOTIF-03 et la course (restore ↔ chargement d'automation) qu'il neutralise ; **flag encore actif** à la sauvegarde (22:22). **Confondant de re-détection exclu par les données seules** : **aucun `input_boolean.turn_on` entre le reboot et le `create`** (dernier à 21:09:29, ~52 min avant) ⇒ flag **restauré**, pas redétecté par `debut.yaml` (30 W / 3 min, incompatible avec +47 s). **Contrefactuel dans la même fenêtre** : **7** reboots pré-correctif survenus cycle actif → **aucun acte** (notification perdue = défaut NOTIF-03 d'origine) vs **1** post-correctif → `create` ⇒ **avant/après contrôlé** (mêmes entité et condition de départ, seul le correctif diffère). Réserve du 2026-07-13 (« absence de la séquence décisive ») **levée**. **Portée** : établit l'idiome sur le véhicule de preuve **admis par le protocole** (`lave_vaisselle_cycle` **ou** `buanderie_cycle` — idiome partagé) ; les autres projections (présence, panne secteur, babysitting, pré-confort, alarme, VMC, voiture, bluetti) restent couvertes par la **validation statique** (`check_notifications_contracts` T1–T6, vert) — le protocole n'exigeait pas de terrain par instance. **Non bloquants, hors périmètre** : durcissement CI du checker (angles morts §4) = lot ultérieur optionnel ; `panne_internet` = **reprise différée assumée** (L4, aucun changement runtime). **Constat connexe consigné, sans effet sur la clôture** : le contrat `electromenager.md` (v1.0, 2026-06-07) est **en retard sur le runtime** (attribue encore `create`/`dismiss` à `debut`/`fin`, ignore `10080000000006`/`10080000000007`) → **lot documentaire séparé** à arbitrer. | [`01_rapports/notifications/preuve_terrain_c15_survie_persistantes_reboot.md`](01_rapports/notifications/preuve_terrain_c15_survie_persistantes_reboot.md) · [`01_rapports/notifications/audit_notifications_domaine.md`](01_rapports/notifications/audit_notifications_domaine.md) · [`03_plans_action/notifications/plan_action_notifications.md`](03_plans_action/notifications/plan_action_notifications.md) |
| Rain Bird — sémantique de santé du pont (C18) | 2026-07-13 | **Lot 4 validation terrain acquise.** T1 PASS : `sensor.rain_bird_pont_sante`=`ok`, `donnees_disponibles`/`donnees_fraiches`=`on`, **Wi-Fi RSSI -77 / BLE -80 dBm (≤ -75)** — santé nominale confirmée malgré RSSI sous l'ancien seuil, attributs RSSI toujours exposés (correctif Lot 3 #346 validé en réel). T5 : `preconditions_runtime`=`on` (ble/batterie/wifi ok) ; `arrosage_intention`=`off` (hors_fenetre/repos) ; `dernier_effectif` présent et cohérent (`2026-07-12T19:34:28+00:00`) — **aucune non-régression apparente**. T3/T4 (degrade/indisponible par historique) **opportunistes, non bloquants** (logique prouvée V3, verdict non-gating V1). Trace §6 remplie. **Arbitrages connexes D-C18-C / D-C18-D portés en ③ (ligne `D-C18-CD`, dormant)** — la clôture C18 ne les tranche pas. | [`04_chantiers/arrosage/protocole_validation_c18_sante_pont.md`](04_chantiers/arrosage/protocole_validation_c18_sante_pont.md) · [`04_chantiers/arrosage/chantier_semantique_sante_pont_rain_bird.md`](04_chantiers/arrosage/chantier_semantique_sante_pont_rain_bird.md) · [`../../12_template_sensors/arrosage/pont_sante.yaml`](../../12_template_sensors/arrosage/pont_sante.yaml) |
| Paramètres invalides — hotfix `today_at` sur helpers heure-seule (régression PARAM-02) | 2026-07-09 | La migration PARAM-02 avait appliqué `as_datetime(...)` à des `input_datetime` **heure-seule** (`has_date:false` / `has_time:true`, état `"HH:MM:SS"`). Or `as_datetime("HH:MM:SS")` rend **`None`** (aucune date) → `eclairage` levait `TypeError: '<' not supported between NoneType` (9 occurrences runtime, capteur cassé) et `climatisation` (égalité `None == None` **toujours vraie**) posait un **faux positif silencieux « blocage permanent »**. Correctif : `as_datetime(...)` → `today_at(...)` sur les **22** appels `eclairage` (state + 7 attributs + cause) et les **6** appels `climatisation` (inv9 + attribut + cause) — `today_at` ancre l'heure sur le jour courant et rend des datetimes comparables ; gardes d'indisponibilité inchangées. `vacances` (helpers `has_date:true`) laissé en `as_datetime` (correct). Contrat mis à jour (v1.2) : règle de parsing explicitée par nature de helper (date → `as_datetime` / heure-seule → `today_at`), exception lexicale VISITE documentée. **Vérifié** : rendu Jinja `eclairage` 3/3 scénarios (valide / ordre inversé / indisponible, state + cause) + attribut isolé + **crash de l'ancien `as_datetime` reproduit** ; `climatisation` blocage permanent 2/2 + faux positif ancien reproduit ; `check_parametres_invalides` **vert 13/13**, `check_visite_contracts` **vert**, `check_registre_chantiers` vert, YAML valide. **2 fichiers runtime + 1 contrat + registre.** | [`01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md`](01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md) (PARAM-02) |
| Paramètres invalides — migration `as_datetime` des comparaisons horaires (PARAM-02) | 2026-07-09 | Suppression des comparaisons directes de chaînes ISO temporelles (interdit du contrat) : **21** comparaisons `eclairage` (`X < Y`) et **3** égalités `climatisation` (`[:5] ==`) migrées vers `as_datetime(...)` gardé. Comparaisons **numériques** intactes (`ext_heat < ext_cool` non touché). **Conflit inter-contrats découvert en CI et tranché** : `modes_maison` surveille `input_datetime.visiteur_*` et le contrat **VISITE** (`check_visite_contracts`) interdit toute logique temporelle (`as_datetime`…) sur ces entités hors du capteur canonique `12_template_sensors/presence/visite.yaml` → `modes_maison` **conservé en comparaison lexicale gardée** (licite pour du time-only zéro-padé 24 h) comme **exception assumée**, documentée en en-tête (priorité à la centralisation temporelle visite). **Vérifié** : rendu Jinja (ordre + égalité + fail-closed, `as_datetime` mocké **levant sur non-parsable** → court-circuit prouvé, aucun crash) ; `check_parametres_invalides` **vert 13/13** **et** `check_visite_contracts` **vert** ; YAML valide. **3 fichiers runtime**, aucun contrat/checker modifié. PARAM-03 (en-tête canonique clim) reste ouvert. | [`01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md`](01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md) (PARAM-02) |
| Paramètres invalides — durcissement checker T13 fail-closed (PARAM-04) | 2026-07-09 | Ajout du test **T13** à `check_parametres_invalides_contracts.py` : toute source non-numérique (`input_datetime`/`input_boolean`) lue dans le `state` doit être testée pour indisponibilité (`<var> in ['unknown', 'unavailable', …]`, littéral **ou** via variable liste type `in indispos`) ; `is_state('input_datetime\|boolean…')` dans le state est proscrit (collapse silencieux). Comble l'angle mort qui laissait passer PARAM-01 (T8 ne captait que `float(0)`/`int(0)` littéraux). **Vérifié** : checker vert **13/13** sur les 8 capteurs ; régression prouvée — T13 détecte 3 violations sur l'ancien `climatisation` (2 bindings datetime non gardés + `is_state` direct), 0 sur le fichier corrigé. **1 fichier** (checker), aucun runtime/contrat modifié ; compteur de checkers inchangé (test ajouté à un checker existant). PARAM-02/03 restent ouverts. | [`01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md`](01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md) (PARAM-04) |
| Paramètres invalides — fail-closed `climatisation` inv9 (PARAM-01) | 2026-07-09 | Correctif issu de l'audit `parametres_invalides` : les 3 sources non-numériques de l'invariant « blocage horaire » (`clim_blocage_horaire_actif`, `clim_heure_blocage_autom_on/off`) étaient **hors garde d'indisponibilité** → une borne horaire absente n'était pas signalée (fail-**open**) ou signalée à tort « blocage permanent » (2 bornes absentes). Correctif : indisponibilité court-circuitée **avant** le slicing `[:5]` dans `state`/`cause`, attribut `blocage_horaire_permanent` durci (plus de faux positif), 3 attributs `*_indisponible` ajoutés. **Vérifié** : test de rendu Jinja 7/7 scénarios conformes (fail-open et faux positif corrigés) + `check_parametres_invalides` vert 12/12 + YAML valide. **1 fichier runtime** (`12_template_sensors/system/integrite_reglages/climatisation.yaml`), aucun contrat/checker modifié. PARAM-02/03/04 restent ouverts. | [`01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md`](01_rapports/parametres_invalides/audit_parametres_invalides_domaine.md) (PARAM-01) |
| Rafraîchissement global des compteurs du registre de couverture (C12) | 2026-07-01 | Recompte fiable post-HINIT (#208) : contrats **267→290**, doctrines **10→12**, checkers **68→71**, workflows **72→75**. §3 rafraîchi ; couplage **1:1 rétabli** (71 checkers ↔ 71 workflows de contrat = 70 `contracts_*` + 1 exception de nommage `clim_ventilation_contracts.yml`) + 4 orchestrateurs = 75, 0 orphelin ; matrice §4 (69→71 : Climatisation +ventilation, Météo +palmarès min journalière haute) et §5 (60/71 non filtrés, 11/71 filtrés `paths:`) réconciliés ; §8 journal daté. Mesure documentaire seule — aucun runtime/checker/workflow/contrat touché. `docs_lint` + `check_registre_chantiers` verts | [`REGISTRE_COUVERTURE_VERIFICATION.md`](REGISTRE_COUVERTURE_VERIFICATION.md) (§3 · §8 journal 2026-07-01) |
| Alarme — stabilisation atomique de l'absence sécurité (C9) | 2026-06-29 (phases 1 & 2) | **Phase 1** (v16.0.5/v16.1.0) : cause = race d'armement au front OFF de `binary_sensor.presence_famille_securite` ; décision = projection atomique `binary_sensor.presence_famille_securite_absence_confirmee_alarme` (`delay_on 5 min`) ; armement migré, ancien trio déclassé. **Validation runtime post-déploiement acquise le 2026-06-29** (front OFF → armement +5 min, plus de race ~10 ms). **Phase 2** (commit `c4637ee`, #160) : trio supprimé (timer `presence_famille_securite_absence`, automation `presence/absence_5_min.yaml`, capteur `..._absent_depuis_5_min`) ; seul consommateur résiduel (`binary_sensor.alarme_systeme_coherent`) recâblé en calcul **inline** depuis l'âge du brut (seuil 480 s, comportement préservé) ; projection d'armement laissée réservée (contrat respecté). `check_presence_contracts` + `check_alarme_contracts` verts ; parse YAML OK ; constante morte `ENT_ABSENT_5MIN` retirée du checker | [`../contrats/alarme/30_decision_centrale.md`](../contrats/alarme/30_decision_centrale.md) |
| Mode nuit climatisation / stratégie absence (C8) | 2026-06-29 | Runtime livré en **v16.1.0** (CI verte) : helper `input_boolean.clim_mode_nuit_actif`, capteur `binary_sensor.clim_mode_nuit_effectif`, neutralisation de `clim_blocage_horaire_reel` quand le mode nuit est armé, requalification des 3 sélecteurs COOL (`consigne`, `seuil on/off`) en **régime absence** pendant la plage nocturne, exposition UI (`reglages.yaml`). **Validation terrain acquise (utilisateur) le 2026-06-29** : bascule en régime absence COOL + neutralisation du blocage horaire vérifiées en plage nocturne réelle. Doctrine `06_doctrine_blocages.md` §9 alignée (Statut → réalisé/validé terrain) au même commit | [`06_doctrine_blocages.md` §9](../contrats/climatisation/06_doctrine_blocages.md) · [`../changelog/changelogs/v16/v16_1_0.md`](../changelog/changelogs/v16/v16_1_0.md) |
| Déshumidificateur — seuils potentiellement inversés (H1) | 2026-06-17 (durcissement + reload) | runtime sain avant patch (`ON=78 > OFF=73`) ; patch runtime appliqué + pull HA + reload `input_number` ; valeurs maintenues après reload (`78`/`73`, non clampées) ; invariant structurel `max(OFF)=74 < min(ON)=75` ⇒ inversion `OFF ≥ ON` impossible via l'UI | [`04_chantiers/climatisation/backlog_climatisation_hysteresis.md`](04_chantiers/climatisation/backlog_climatisation_hysteresis.md) |
| Vacances / chauffage effectivité (S-CHAUFFAGE-PRESENCE, VAC-IMP-1) | 2026-06-01 (runtime + contrat) | commits `f2071ac1` + `b2bcbaa0` ; `contrats/chauffage/80_table_decision_canonique.md` aligné sur `vacances_actives` | [`03_plans_action/vacances/plan_action_vacances_chauffage_effectivite.md`](03_plans_action/vacances/plan_action_vacances_chauffage_effectivite.md) |
| ECS — borne désinfection 3.0 → 4.0 | 2026-06-12 | commit `0a602aed` ; `03_input_numbers/ecs/offset.yaml` (`max: 4.0`) | [`01_rapports/ecs/audit_borne_desinfection_3_vers_4.md`](01_rapports/ecs/audit_borne_desinfection_3_vers_4.md) |
| Lovelace — corrections factuelles C1 / C2 | 2026-06 | références fantômes `carte_mode_maison_synthese` et `socle_status_label_xl_interactif` retirées | [`04_chantiers/lovelace/exploitation_audit_19_button_card_templates.md`](04_chantiers/lovelace/exploitation_audit_19_button_card_templates.md) |
| Lovelace — button-card C3 / C4 | 2026-06-17 | C3 : `19_button_card_templates/README.md` (pointeur de découvrabilité) créé. C4 : `carte_mode_toggle` soldé par suppression (v15.9.1), suivi réaligné. Résiduel doc optionnel non ouvert : 2 lignes « réserve » (`socle_etat_reel`, `carte_bruit_seuils_variables`) | [`04_chantiers/lovelace/exploitation_audit_19_button_card_templates.md`](04_chantiers/lovelace/exploitation_audit_19_button_card_templates.md) |
| Couverture normative — backlog soldé (P1→P4) | 2026-06-17 | Tous traités : P1/P2 contrats figés + domaines documentés ; P3 `couleurs`/`boutons`/`statistiques` (faux trou), `12_capteurs_observabilite_pure` (stub retiré), `deshumidificateur/guard §12` (libellé périmé corrigé, helpers déjà implémentés/CI-gated) ; P4 `modes/normal` (faux trou, couvert `vacances.md`). Clôture `05_clotures/` à venir | [`04_chantiers/transverses/etat_avancement_couverture_normative_domaines.md`](04_chantiers/transverses/etat_avancement_couverture_normative_domaines.md) |
| Lovelace — backlog diagnostics soldé | 2026-06-17 | Vannes : V-1 corrigé, V-2 duplication d'affichage documentée, V-3 contractualisé. Chauffage : IMPORTANT-1 duplication d'affichage acceptable, IMPORTANT-2 éditorial fait. Résiduels UI parqués (faible valeur) : sourcing `0.4`/`0.5`, surcouche rouge `coherent` | [`04_chantiers/lovelace/suivi_audit_dashboard_diagnostics_chauffage.md`](04_chantiers/lovelace/suivi_audit_dashboard_diagnostics_chauffage.md) · [`04_chantiers/lovelace/suivi_audit_dashboard_diagnostics_vannes_thermostatiques.md`](04_chantiers/lovelace/suivi_audit_dashboard_diagnostics_vannes_thermostatiques.md) |
| Centralisation des palmarès météo (C7) | 2026-06-18 (merge #16, `0c1eb51f`) | Option B retenue : dashboard dédié `Palmarès météo` + accès hub Navigation (bouton « Rec. météo ») ; Option A bandeau **rejetée** (débordement mobile portrait ; bandeau conservé à 9) ; validé terrain desktop + mobile (reload Lovelace, sans redémarrage HA) ; **composition Lovelace / navigation UI uniquement** — aucun capteur / entité / logique modifié ; sources `Min · Max` et `Précipitations` nettoyées ; dashboard dédié structuré en 3 sections records | [`04_chantiers/lovelace/cadrage_palmares_meteo.md`](04_chantiers/lovelace/cadrage_palmares_meteo.md) |
| Ouvertures — faux fronts à la reconnexion de capteur (OUV-R1) | 2026-06-18 (runtime + contrat poussés) | **Constat** : front d'ouverture fabriqué à la reconnexion d'un ouvrant resté ouvert (`on → off → on`). **Cause** : normalisation N1 (`capteurs_base.yaml`) forçait `unavailable → off` (invariant `global.md §2.2`). **Correctif** : N1 maintient le dernier état valide (`this.state`, repli `off` à froid) — commit `50ae4d0a` ; `global.md §2.2` aligné. **Impact** : front supprimé pour base + agrégats (`contact_entree_fenetre`, `contact_sejour`, `contact_fenetres_maison`) ; ferme **en amont** la cause racine des faux déclenchements alarme (sans modifier l'alarme) ; bénéfique aération ; réconciliés (N3) non concernés (déjà flap-resistant). **Validation** : `yamllint` propre ; `check_redondance_contracts.py` + `check_alarme_contracts.py` verts ; démarrage à froid inchangé. **Dette** : `any_unavailable` (N2) vacant — disponibilité non propagée au-delà de l'attribut N1 (suivi observabilité distinct) ; incohérence de version `v1.1`/`v2.2` dans `global.md §2.2.1` | [`../changelog/changelogs/v16/v16_0_4.md`](../changelog/changelogs/v16/v16_0_4.md) · [`../contrats/ouvertures/global.md`](../contrats/ouvertures/global.md) |
| Libellés courts Android Auto (C6) | 2026-06-19 | Couche `01_customize` (`friendly_name` seul, aucun `icon`, aucun changement runtime). **Lot 1** (commit `b4877674`) : 12 libellés courts (températures min/moy./max chambres-maison, 3 tendances, `clim_mode_local`, `ecs_temperature_ballon_securisee`, 4 scripts volets) — icônes dynamiques non écrasées ; **validé terrain Android Auto le 2026-06-18**. **Lot 2** : `alarm_control_panel.alarme_maison` → « Alarme », `sensor.programme_chauffage` → « Chauffage » (verrou Lovelace `f7e41c05` puis `friendly_name` `01d5e057`) — **validé terrain le 2026-06-18**. **Lot 3** : `binary_sensor.panne_secteur_en_cours` → « Panne secteur » (commit `f0b916e5`, aucun verrou Lovelace requis) — **validation Android Auto confirmée (utilisateur) le 2026-06-19**. Bluetti / UPS écartés d'Android Auto (diagnostic, hors surface conduite). `binary_sensor.ouvertures_maison` / `mouvement_maison` : catégorie A (déjà courts), sans changement | [`../architecture/00_structure_includes/01_customize.md`](../architecture/00_structure_includes/01_customize.md) § *friendly_name & surfaces d'affichage* |
| Présence confort thermique stabilisée — COOL/DRY (C4, volet A) | 2026-06-19 | Validation terrain lecture seule (fenêtre 17→19/06 ; 24 creux de présence brute, 4 < 120 s). **C1 PASS — exercé** : 3 creux courts sous `clim_target_mode = cool`, 0 coupure d'autorisation pendant la fenêtre d'absorption (dont **107,1 s sous COOL absorbé**). **C2 PASS** : latence de réveil 0 s (pas de `delay_on`). **C3 PASS** : aucune relâche anticipée de la stabilisée. **C4** : `T = 120 s` couvre la distribution observée (max 107,1 s). **`T = 120 s` ratifié** avec réserve de surveillance (marge ~13 s). Volet B (*étendre vs confiner hors COOL/DRY*) **hors périmètre**, porté par **D-PRES**. Outil : `tools/investigations/valider_presence_stabilisee_c4.py` (lecture seule). **Surveillance post-clôture (fenêtre 2026-06-22→29, `enquete_presence_meteo.py`)** : 3 impacts cool **tous longs** (~70 min–2 h 15), **0 impact < 120 s** ⇒ non-régression confirmée ; réserve de surveillance **LEVÉE et CLOSE le 2026-07-20** — qualifiée *réserve différée solvable* (doctrine §3), levée sur la **fenêtre achevée 22→29/06 sans impact < 120 s** ; seuil `120 s` inchangé, non-conformité **R-QUALIF-3** corrigée ; impacts longs = absences réelles attendues (hors périmètre — ni court-cycle compresseur, ni D-PRES sauf fausse perte durable avérée) | [`05_clotures/climatisation/cloture_presence_confort_thermique_stabilisee.md`](05_clotures/climatisation/cloture_presence_confort_thermique_stabilisee.md) |
| Double emplacement bouclage ECS (D2) | 2026-06-19 | Résolu de fait : `contrats/ecs/04_bouclage_ecs_sous_systeme.md` est un **renvoi pur** (conversion appliquée en v15.9.1, commit `4dd95f34`), sans doctrine autonome ; `contrats/bouclage.md` (v2.3.0, Actif) = **source de vérité unique**. Hub `ecs` réaligné (mention « conflit actif / STRUCTURANT — OPPOSABLE » retirée) ; anomalie §2.3 close. Aucun fichier déplacé/renommé, aucun runtime touché | [`02_constats/transverses/registre_anomalies_transverses.md`](02_constats/transverses/registre_anomalies_transverses.md) (§2.3) |

---

> **Provenance.** Première version issue de la chaîne inventaire → contre-expertise → rationalisation → conception du cockpit (juin 2026). Volumétrie cible : *Actifs* 3–4 lignes (le cockpit), le reste en référence. Si une ligne *Actifs* dépasse durablement, c'est le signe d'un backlog déguisé : la redescendre en ④.
