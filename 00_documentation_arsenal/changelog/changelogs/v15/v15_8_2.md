# Changelog Arsenal — v15.8.2

_Date : 29/05/2026_

---

# Chauffage

## Décision Centrale

- suppression de la branche Niveau 1 `not is_state('binary_sensor.chauffage_autorise_systeme', 'on')` dans `desired_mode`
- suppression de la même branche dans `reason`
- raison `chauffage_non_autorise` retirée du runtime (plus émise)
- cas blocage post-aération : émission de `blocage_aeration_en_cours`
- `desired_mode` du cas blocage post-aération inchangé (`reduced`)

## Autorisation système

- `binary_sensor.chauffage_autorise_systeme` passe à l'état constant `on`
- capteur réservé à la sécurité système, sans cause active
- `input_boolean.chauffage_blocage_aeration` reclassé en cause Niveau 2, porté par la Décision Centrale
- attributs et icône du capteur conservés

## Diagnostics

- suppression de la branche Niveau 1 `not is_state('binary_sensor.chauffage_autorise_systeme', 'on')` dans `sensor.chauffage_raison_calculee`
- suppression de la même branche dans `sensor.chauffage_mode_calcule`
- cascades diagnostiques réalignées sur la Décision Centrale

## Documentation

- contrat `30_decision_centrale.md` : Niveau 1 marqué catégorie réservée sans cause active
- `chauffage_non_autorise` marquée réservée, non émise
- renommage de la raison `absence_protection_thermique` en `stabilisation_absence`

---

# CI — région décision

## Verdict runtime

- `cli_decision` applique `R-COV-1` au runtime sans l'axiome `AX-D2` (`A=()`)
- retrait de l'import `AXIOMES_D2` dans `cli_decision`
- `AX-D2` et `AXIOMES_D2` conservés pour la fixture
- requalification de la provenance de `AX-D2` en prémisse de fixture

## Tests

- re-snapshot G2 dans `test_lot_2_7` : `R-COV-1 == 0` sur le runtime
- mise à jour des assertions de structure de cascade dans `test_lot_2_1` (9 branches)
- fixture `d2_reason_pre_correction.yaml` inchangée

---

# Fichiers impactés

## Runtime

- `10_scripts/chauffage/decision_centrale.yaml`
- `12_template_sensors/chauffage/autorisation.yaml`
- `12_template_sensors/chauffage/diagnostic/raison.yaml`
- `12_template_sensors/chauffage/diagnostic/mode.yaml`

## CI

- `tools/arsenal_ci/decision/cli_decision.py`
- `tools/arsenal_ci/decision/axiomes.py`
- `tools/arsenal_ci/tests/test_lot_2_7.py`
- `tools/arsenal_ci/tests/test_lot_2_1.py`

## Documentation

- `00_documentation_arsenal/contrats/chauffage/30_decision_centrale.md`
